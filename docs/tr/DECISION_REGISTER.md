# V8 Karar Kaydı v0.1

| ID | Karar | Durum | Kanıt / sonuç |
|---|---|---|---|
| D-001 | Candidate bir emir değildir; her yaşam-döngüsü terminal durumunu kaydet | PROVISIONAL_DECISION | V7 seçim-yanlılığı/state-machine başarısızlıklarıyla desteklenen tasarım çıkarımı. Yalnızca-trade verisine karşı test et. |
| D-002 | Açık saatlerle zaman-noktası, değiştirilemez MarketState kullan | LOCKED_INVARIANT candidate | V7 nedensellik/otorite kontrolleri; sızıntı bir geçerlilik ihlalidir. |
| D-003 | 2–3 deterministik kendi-kendine kapılanan Expert ile başla | PROVISIONAL_DECISION | En küçük atfedilebilir yönlendirme taban çizgisi; Expert'lerin global modelleri yendiğine dair kanıt yok. **D-043 tarafından revize edildi:** 2–3 ilk dilim için bir başlangıç noktasıydı, asla bir tavan değil; çalışma-zamanı Expert sayısı sınırsızdır. |
| D-004 | Başlangıçta router uygulama | REJECTED_OPTION for baseline | Router değerli-candidate geri çağırımı ve bağlayıcı bir compute/gecikme gerekçesi gerektirir. |
| D-005 | Kurallı deterministik Level-1 execution ilk karşılaştırıcıdır | PROVISIONAL_DECISION | V7 kontrolleri uygulamayı destekler, ama sertifikasyon FAIL/BLOCKED olarak kalır. |
| D-006 | Öğrenilmiş execution/RL başlangıç mimarisinden hariç tutulur | REJECTED_OPTION for baseline | V7 kanıtında entegrasyon/state-machine sorunları ve ekonomik sertifikasyon yok. |
| D-007 | Başlangıçta scorer yok | PROVISIONAL_DECISION | Yalnızca tekrarlanabilir sabit-kapsam OOS net faydası deterministik skoru aşarsa kabul et. |
| D-008 | Başlangıçta ranker yok | PROVISIONAL_DECISION | Yalnızca gösterilmiş sermaye/risk çekişmesi ve eşleştirilmiş tahsis kazancı altında kabul et. |
| D-009 | Alpha/execution'ı operasyonel olarak ayrık tut, istatistiksel olarak bağımsız değil | LOCKED_INVARIANT | Ana brief ve denetim bir bağımsızlık iddiasını reddeder. |
| D-010 | Tüm terfi iddiaları kod/veri/config/tohum/defter hash'lerini bağlar | LOCKED_INVARIANT candidate | Proje mühendislik kanıtı; eksik makbuz hükmü engeller. |

| D-011 | Yalnızca-araştırma alımı; borsa (venue) adaptörlerinin emir/imzalama yolu yok | LOCKED_INVARIANT | FEED_INGESTION_SPEC §1; yetenek sınırı, gelenek değil. |
| D-012 | İlk borsa adaptörü Binance USDT-M'dir; adaptör arayüzü borsadan bağımsızdır (venue-agnostic) | PROVISIONAL_DECISION | Corpus/brief örnekleri Binance biçimlidir; ikinci bir borsa tüketicileri değiştirmemelidir. |
| D-013 | Ham kanıt değiştirilemez Parquet'ta; türetilmiş mağazalar DuckDB'de; yalnızca-ekleme (append-only) disiplini | PROVISIONAL_DECISION | PERSISTENCE_REPLAY_SPEC §1; tek-yazar, replay-ağırlıklı araştırma uyumu. |
| D-014 | Bar-güdümlü karar saati varsayılan sürücüdür; olay-güdümlü mod kapılıdır | PROVISIONAL_DECISION | RUNTIME_SCHEDULER_SPEC §1; araştırma için yapı itibarıyla determinizm. |
| D-015 | Tek-süreçli araştırma çalışma zamanı; tek yazar, salt-okunur replay | PROVISIONAL_DECISION | RUNTIME_SCHEDULER_SPEC §3; eşzamanlılık yalnızca bildirilmiş idempotent sınırlarda. |
| D-016 | Simülasyon otoritesi yenilenene dek canlı (live) durumuna ulaşılamaz | LOCKED_INVARIANT | OPERATIONS_SPEC §1; sertifikasyon kaydı FAIL/BLOCKED. |
| D-017 | Aile metaverisiyle beş-seviyeli ontoloji; varyantlar Expert değildir | PROVISIONAL_DECISION | Expert-protocol §1; aile-seviyesi multiplicity düzeltmesi. |
| D-018 | (Enstrüman, yön) başına bir aktif exposure; çakışma -> CAPACITY_REJECTED | LOCKED_INVARIANT candidate | Lifecycle spec §6; çift sayılan PnL ve risk yığılmasını önler. |
| D-019 | Öğrenme çevrimdışı ve registry kapılıdır; çevrimiçi mutasyon yok | LOCKED_INVARIANT | LEARNING_PROTOCOL; politika çalkantısı birincil arıza modudur. |
| D-020 | Karmaşıklık bütçesi: <=3 Expert, <=1 öğrenilmiş bileşen, router/scorer/ranker/RL yok | PROVISIONAL_DECISION | Constitution kural 14; aynı anda tek hareketli parça. **D-043 tarafından revize edildi:** <=3 Expert tavanı çalışma-zamanı kapasitesini kanıt bütçesiyle karıştırdı ve registry tarafından zaten ihlal edilmişti (D-042 sonrası 5 Expert). Router/scorer/ranker/RL yokluğu değişmedi. |
| D-021 | Bir-kez-derleme materializasyonları; eğitim asla ham tape okumaz | PROVISIONAL_DECISION | Dataset spec §5; simülasyonu eğitimden ayırır. |
| D-022 | V7 laboratuvarını `src/v8/simtruth/` olarak ve V7 `tools/data.py`'yi referans uygulama olarak kaynağa al (vendor) | PROVISIONAL_DECISION | ENGINEERING-ONLY: V8'in kendi doğrulaması + yenilenmesine dek otorite FAIL kalır. |
| D-023 | Portföy heat tavanı: heat = stop-risk toplamı (R), sabit korelasyonlu kümeler, küçültme-değil-reddet | LOCKED_INVARIANT candidate | Kullanıcı-onaylı politika; DeMiguel ve diğ. 2009 öğrenilmiş tahsis üzerinde basit deterministik tavanları destekler. (Harici bir sentezin önerdiği D-022 değil, D-023 olarak numaralandırılmıştır — D-022 borsa/satıcı kararıdır.) |
| D-024 | Mekanik alınıp-satılabilirlik maskesi (funding penceresi, spread kuyruğu, DEGRADED durumu) bir data-plane vetosudur | PROVISIONAL_DECISION | Serbestlik derecesi eklemez; öğrenilmiş rejim etiketlerinden (O-015) ayrıdır. |
| D-025 | Yalnızca kesirli Kelly tavanı; tam Kelly yasak | PROVISIONAL_DECISION | Busseti/Ryu/Boyd + MacLean-Thorp-Ziemba; tahmin hatası altında mahvolma-riskidir. |
| D-026 | `episode_key`, `setup_anchor_event_id`'ye sabitlenir, asla bir doğum zaman damgasına değil; CANDIDATE_LIFECYCLE_SPEC §1'de bir kez tanımlanır | LOCKED_INVARIANT candidate | Üç-yönlü bir çelişkiyi çözer (lifecycle spec, expert protocol, `lifecycle.py`). Saate-bağımlı bir anahtar bastırmayı erişilemez kılar, bu yüzden deduplikasyon sessizce hiç devreye girmez. Kod hâlâ kusurlu biçimi uyguluyor — CHANGELOG takibine bakın. |
| D-028 | R, bildirilmiş bir fiyat uzaklığıdır (`risk_unit`), asla bir fiyat oranı değildir; `net_r`/`mae_r`/`mfe_r` R-katlarıdır ve pozitif-olmayan bir birim kapalı-başarısız olur (fails closed) | LOCKED_INVARIANT candidate | Simülatör, geometri, RiskGate heat (D-023) ve spec'lerin tümü bunları R olarak okurken R adları altında kesirli fiyat getirileri hesaplıyordu. Düzeltildi ve golden testlerle kapsandı (stop-out tam olarak −1R − maliyettir; 10× risk genişliğinde özdeş R geometrisi özdeş `net_r` üretir). Simülatör hash'i `canonical-sim-v3`'e yükseltildi, böylece düzeltme-öncesi defterler eşit karşılaştırılamaz. |
| D-029 | Giriş-sonrası tez geçersizliği ayrı bir çıkıştır (`THESIS_INVALIDATED`), asla `STOP` ile birleştirilmez | PROVISIONAL_DECISION | Dondurulmuş spec'te deterministik Expert yüklemi, kapalı barlar üzerinde değerlendirilir; öğrenilmiş bileşen yok (kural 14), yeni lifecycle durumu yok, girdiler gözlemlenemez olduğunda açık-başarısız (fails open) olur. Bölüm-4 rekabetçi-risk ayrıştırmasının "yön konusunda yanılmak" ile "erken ayrılmakta haklı olmak" ayrımını yapabilmesini sürdürür. |
| D-030 | Excursion'lar (`mae_r`/`mfe_r`) ve `ambiguous_bars` her sonuçta kaydedilir | PROVISIONAL_DECISION | Excursion, V7'nin maddi olarak öngörülebilir ölçtüğü tek nicelikti (IC'ler +0.124/+0.152'ye karşı +0.015 işaretli getiri) ve kaynağa alınan (vendored) V7 simülatörü bunu zaten kaydediyor; V8 kurallı simülatörü gerilemişti. Onsuz O-013 yanıtlanamaz ve belirsizlik parantezi ölçülemez. |
| D-027 | `LabReport`, `execution_share` + popülasyon ıraksamasını taşır; ikisi de ekonomik hükmü kapılar (`ATTRIBUTION_UNSAFE_*`) | PROVISIONAL_DECISION | Deterministik kabul, karşı-olgusal-ağırlıklı popülasyonları yapısal kılar, rastlantısal değil. OPE düzeltmesi deterministik filtreleme altında kullanılamaz (sıfır eğilim, eksik destek) ve kabul edilebilir bir finans uygulaması yoktur; programın kapalı-başarısız kuralları o zaman iddianın reddedilmesini gerektirir. Eşikler 2026-08-01'de onaylandı (O-017): `execution_share` tabanı 0.25, iki-örneklemli KS ≤ 0.20, `PREREGISTRATION_V8_SLICE_001` §15'e bağlandı. Kod kapısı Phase-4 deney çalıştırıcısıyla gelir. |
| D-031 | Teknoloji taban çizgisi: Python >= 3.11 önce-stdlib karar yolu, tek-süreçli modül topolojisi, dilim ölçeğinde JSONL yalnızca-ekleme defterleri + değiştirilemez Parquet ham arşiv + DuckDB türetilmiş/araştırma mağazaları (PERSISTENCE_REPLAY_SPEC §1), uv ile pytest, `numpy` yalnızca `simtruth/` ve araştırma araçlarıyla sınırlı | PROVISIONAL_DECISION | ARCHITECTURE_SPEC §3, daha önce dağınık olan teknoloji seçimlerini tek kayıt girdisinde toplar. O-009 yalnızca ölçek-doğrulama deneyini tutar; tasarım eşlemesi artık açık değil. |
| D-032 | Dosya ailesi dondurulmuş bir sözleşmedir: `IMPLEMENTATION_LAYOUT.md` her dosyayı, onun genel arayüzünü ve sahip olduğu sözleşmeyi önceden belirler; yeni dosyalar, yeniden adlandırmalar veya arayüz değişiklikleri CHANGELOG girdileri olan registry kararlarıdır | PROVISIONAL_DECISION | Dikey dilim, herhangi bir yerleşim var olmadan önce dosyalar üretti ve kod/spec sapması zaten üç kez oldu (episode_key, funding, D-024). Önceden belirleme sapmayı sessiz olmak yerine görünür kılar; yerleşim, diğer sürümlenmiş yapıtlar gibi challenger ile evrilir. |
| D-033 | Expert modülleri `src/v8/experts/` içinde yaşar, davranış ailesi başına bir dosya, paket registry'si üzerinden yeniden dışa aktarılır; `marketstate.py` adı korunur; `_code_hash` tüm paketi özyinelemeli kapsar | PROVISIONAL_DECISION | `experts.py` iki pilotun ötesinde ölçeklenmezdi (Phase 3 aileler ekler) ve düz glob alt paket dosyalarını kaçırdı — bir Expert değişikliğini kaçıran kod hash'i raporu çözebilirdi (D-010). `state.py` yeniden adlandırması önerildi ve sahibi tarafından reddedildi: `marketstate.py`, `MarketState` kayıt adını yansıtır. |
| D-034 | D-026 için uygulama sabitlemesi: `setup_anchor_event_id` = Expert'in setup yükleminin geçerli olduğu mevcut ardışık koşunun ilk kapalı barının `event_id`'si, yeni bir 32-bar `history` özellik grubunu (bar-başına OHLC + EMA'lar) tarayarak bulunur; `episode_key` doğum zaman damgasını bırakır; `is_duplicate` görülen çapalarda anahtar-eşitliği olur (zaman penceresi kaldırıldı) | PROVISIONAL_DECISION | D-026'yı durumsuz bar-başına Expert sözleşmesini yeniden tasarlamadan uygulanabilir kılar. 32-bar sınırı belgelenmiştir: pencereden daha eski çapalar kararsızdır ve 6-bar bastırma penceresi onun çok içinde durur. Phase 0-3 özerk yapısı için kaydedildi (`AGENT_RUNBOOK` Adım 1). |
| D-035 | Funding için uygulama sabitlemesi: `ExperimentManifest.funding_rate_r` (varsayılan 0.0) + `funding_hours` (varsayılan 8); `funding_hours`'a bölünebilen tam UTC saatlerinde settlement'lar, `SETTLEMENT_BEFORE_ORDERS` olarak uygulanır, her geçiş başına bir `funding_settled` defter olayı, her settlement başına `net_r` azalır, long'lar pozitif oranları öder; simülatör hash'i `canonical-sim-v4`'e yükselir | PROVISIONAL_DECISION | SIMULATION_TRUTH_SPEC §3 olay sırası #5 normatiftir; golden testler (sınır sayımı, pencere vs tam-tape eşliği) V7 denetiminin istediği kontrollerdir. Varsayılan 0.0, politika değişirken bugünkü sayıları özdeş tutar. `AGENT_RUNBOOK` Adım 2. |
| D-036 | D-024 için uygulama sabitlemesi: manifest sabitleri `max_bar_range_frac = 0.05`, `funding_window_bars = 1`; kabul vetosu `TRADABILITY_MASK_VETO` (`NOT_EXECUTED` karşı-olgusunu korur) çubuk-içi aralık kuyruğunda, `StateQuality == DEGRADED` veya funding penceresi içindeki girişte; sabitler bildirilir, asla uydurulmaz (sızıntı yok) | PROVISIONAL_DECISION | CANDIDATE_LIFECYCLE_SPEC §6 madde 3 normatiftir. Uydurulmuş bir yüzdelik gelecek bilgisini sızdırır; bildirilmiş sabitler maskeyi sıfır-serbestlik-dereceli tutar. `AGENT_RUNBOOK` Adım 3. **2026-08-04'te yeniden adlandırıldı** (D-043 takibi): alan `max_spread_frac` ve veto detayı `SPREAD` idi, ama yüklem `(high-low)/close` — giriş barının çubuk-içi aralığı, asla bir alış-satış spread'i, ki derinliksiz tape bunu ifade edemez. Saf yeniden adlandırma: eşik veya karar değişmedi, golden `candidate_count`/`terminal_distribution` değişmedi, defter hash'i yalnızca `config_hash` alan adlarına dayandığı için yeniden sabitlendi. 0.05'in anlamlı bir seviye olup olmadığı artık O-019 — "bildirildi, asla uydurulmadı" sızıntı itirazını yanıtlar ama eylemsizlik itirazını değil. |
| D-037 | `tools/data.py` sözleşmelerinin stdlib aynasını `tools/vision_backfill.py` içinde kabul et; birebir yeniden kullanım, `data.py`'nin ağır bağımlılıkları (polars/pandera/pyarrow/duckdb) Phase-1 parquet materializasyonuyla kabul edilene dek ertelenir (DATASET_SPEC §5). `pyyaml>=6` yalnızca dev ekstrasına katılır (ortam, karar yolu değil) | PROVISIONAL_DECISION | Adım-4 OPEN_PIN'ini çözer: `data.py`, yasaklı bağımlılıklar olmadan içe aktarımda sert çıkış yapar (O-009 en-küçük-motor kuralı). Ayna aynı belgelenmiş sözleşmeleri uygular (kline sütun sırası, ms->ns dönüşümü, checksum-dosya biçimi), satır içi alıntılanır; üretilen satırlar `store.replay_tape` üzerinden `TapeRow(**r)` ile gidiş-dönüş yapar. |
| D-038 | Phase-1 araç bağımlılıkları `tooling` ekstrası olarak kabul edilir (polars/pyarrow/pandera[polars]/duckdb) `tools/data.py` ve `tools/materialize_views.py` için (parquet materializasyonu, DATASET_SPEC §5); karar yolu (`src/v8/` eksi `simtruth/`) yalnızca-stdlib kalır; saf planlama/checksum yardımcıları dışında hiçbir test veya sözleşme yeni bağımlılıklara dayanmaz | PROVISIONAL_DECISION | D-037'nin erteleme koşulu karşılanmıştır: Phase-1 materializasyonu artık inşa ediliyor (oturum 2: gerçek tape + DuckDB görünümleri, O-009 karar tarafında en küçük motoru tutar). Kabulün kendi OPEN_PIN'inde kaydedildi; operatör burada registry'ye işler. |
| D-039 | Bildirilmiş veri kümesi v0.1: BTCUSDT 1h, kanallar `kline` + bildirilmiş-ama-bekleyen `funding`, dev penceresi 2026-04..06 (hash `8b12707e…`), dondurulmuş OOS = 2026-07-01 sonrası ilk iki yayınlanmış ay + 9-bar uzantısı, tam geçmiş/diğer semboller bildirilmemiş, Tier-A/S bildirilmemiş (O-010). Ölçek beklentileri tablosu ölçülen sayılara bağlanmıştır (DATASET_SPEC §6) | PROVISIONAL_DECISION | Corpus boşluğunu kapatır: hiçbir sözleşme daha önce veri kümesinin ne olduğunu veya hangi ölçekte olduğunu bildirmiyordu; "daha fazla veri" artık bir registry kararıdır, asla sessiz bir indirme değil. Funding kanalı, alım beklerken bildirilir — preregistration §8 revizyonu (0.0 -> tape-güdümlü) bir sonraki data-plane adımıdır, holdout açılana dek yasaldır (§16). |
| D-040 | Yalnızca-v0.1 çerçevesini emekliye ayır; program hedefi kanıt kapıları değişmemiş tam 8-fazlı yol haritasıdır (kurallar 5-6, 12, 14). Yapı önceliği önce Phase 0-4'ü tamamlar — Phase-4 `v8_slice_001` deney çalıştırıcısı + D-027 hüküm kapısı kritik yoldur — ve Phase 5/7 yalnızca kapıları geçtiğinde inşa edilir | PROVISIONAL_DECISION | Sahip kararı 2026-08-01. `docs/PLAN_V8_FULL.md` sprint dökümünü tutar (planlama yapıtı, monograph bölümü değil); `tests/test_decision_path_purity.py` kapılı-bileşen yokluğunu uygulamaya devam eder. Hiçbir ekonomik iddia yapılmaz veya ima edilmez. |
| D-041 | Bildirilmiş dev veri kümesini 12 aya genişlet (BTCUSDT 1h, `2025-07-01`..`2026-07-01`, ~8,760 bar) ve `funding` kanalını tape-güdümlü settlement kablolamasıyla (`funding_settled_r = entry_price × rate / risk_unit`) PIT tape'e al; dev-sonu pozisyonlarının 2026-07-01 sınırı boyunca settlement'a bağlanması için bildirilmiş `2026-07` funding kapsam ufku dahil; holdout çapası değişmedi | PROVISIONAL_DECISION | Sahip kararı 2026-08-01 (tam-program D-040 serisi). O-017 eşikleri 0.25/0.20 sabit kalır — 12-aylık taban çizgisi yalnızca prereg §15 teşhislerini günceller, asla onaylanmış sayıları değil. Prereg §16 ve D-039'un kapsam notuna göre holdout-öncesi yasaldır; prereg §8/§13/§15 revize edildi; dev sonu kesinlikle 2026-07-01 holdout'undan önce kalır. Funding satırları asla özelliklere veya karar döngüsüne girmez. |
| D-042 | `src/v8/experts/liquidity_sweep_reclaim.py`'yi üçüncü pilot olarak kabul et (`liquidity_sweep_reclaim` / `sweep_reclaim`, varyant `a`; kapanış tarafından geri alınan pencereli önceki-düşük süpürmesinden sonra LONG, geri alınan önceki-yüksek süpürmesinden sonra SHORT; `prior_low_ref` tespitte dondurulur ve `geometry_version` dışında tutulur) ve `breakout_retest` + `capitulation` birikim ailelerini türev tape'e dek DATA_BLOCKED olarak kaydet | PROVISIONAL_DECISION | Sahip kararı 2026-08-01 (tam-program D-040 serisi). D-032 dosya-ailesi kuralı: yeni bir `src/v8/experts/` modülü bir registry kararı + CHANGELOG girdisi gerektirir. `failed_breakout`'tan ayrı mekanizma/setup/geçersizlik (EXPERT_PROTOCOL §1 ayrı-Expert testi). Registry YAML'ı + PILOTS + testler birlikte güncellenir; dev materializasyonu yeniden sabitlenir. |
| D-043 | Karmaşıklık bütçesi iki eksene ayrılır (kural 14 yeniden yazıldı): (a) çalışma-zamanı Expert sayısı **sınırsızdır**, yalnızca determinizm ve compute ile sınırlıdır ve bir geçerlilik kısıtı değildir; (b) önceden kaydedilmiş tavan, tek bir dondurulmuş-OOS değerlendirmesinde eşzamanlı iddia taşıyan `behavior_family` sayısına uygulanır ve kural 11'in aile-seviyesi multiplicity düzeltmesine girer. "En fazla bir öğrenilmiş bileşen" **pipeline konumu başına** sınırlandırılmıştır, küresel değil. D-003 ve D-020'yi revize eder | PROVISIONAL_DECISION | Sahip kararı 2026-08-04. Tek sayı, bir mühendislik sorusunu (bar başına kaç modül değerlendirir — sıfır geçerlilik içeriği) istatistiksel bir soruyla (eşzamanlı test edilen kaç bağımsız hipotez) karıştırdı. Tavan zaten ihlal edilmişti: `EXPERTS_REGISTRY.yaml`, D-042 sonrası 5 Expert taşır. Sayıyı uygulayan hiçbir kod yoktu, bu yüzden bu belgesel bir düzeltmedir — ama takip olarak kaydedilen iki gerçek bağlaşımı açığa çıkarır: portföy ölçeğindeki bağlayıcı kısıtlar kural 16 (enstrüman+yön başına bir exposure) ve D-023'tür (`max_heat = 3.0`) ve çekişmeli exposure yuvaları `expert_id` sözlüksel sırasıyla çözülür. Sıra-bağımsızlığın kendisi güvenlidir — `lab.run`, değerlendirmeden önce Expert'leri `expert_id`'ye göre sıralar, bu yüzden çağıranın liste sırası atılır ve defter kararlıdır — ama ayakta kalan eşitlik-boza, Expert'in *adıdır*: bir çekişme fikstüründe ölçüldüğü üzere, davranışsal olarak özdeş iki Expert'ten sözlüksel olarak ilki yuvayı kabaca 2:1 kazanır ve adlar değiştirildiğinde avantaj adı izler (`tests/test_admission_contention.py`). Üç Expert'te deterministik ve zararsızdır; bu kararın izin verdiği sınırsız sayıda ise sessiz bir ad-tabanlı tahsis politikasıdır. Burada çözülmedi: ilkeli bir eşitlik-boza bir ranker'dır, kural 6 / D-008 (O-006 / O-012) tarafından kapılıdır. Biri eklenirse test başarısız olur, o registry kararını zorlar. Scorer ve ML-Expert-challenger farklı pipeline konumlarını işgal eder ve her biri zaten kural 5 tarafından kapılıdır, bu yüzden küresel "asla aynı anda ikisi de" hiçbir istatistiksel kontrol eklemiyordu. |
| D-044 | Preregistration §11'in aile-içi multiplicity kuralı düzeltilir: "bir aile içinde keşfedilen tüm varyantlar tek bir multiplicity birimi sayılır" **yerine**, o ailenin her `variants_evaluated` girdisi üzerinde uygulanan ve ailenin mevcut Bonferroni-düzeltilmiş `α_f` bütçesini harcayan bir Reality-Check-tarzı blok-bootstrap maks-istatistik testi (White 2000 Prosedür RC, `HYPOTHESIS_LAB_PROTOCOL.md`'nin Kaynaklar bölümünde zaten `LITERATURE_SUPPORTED`) **geçer**. Aileler-arası Bonferroni (`α_f = 0.05/F`, önceden kaydedilmiş aileler genelinde) **değişmez** — hâlâ geçerli, yalnızca korelasyon altında muhafazakâr ve henüz varyant araması olmayan F=2 ailede acil değil. `EXPERTS_REGISTRY.yaml` zorunlu bir `variants_evaluated` alanı kazanır (yalnızca bildirilen `variant_id` değil, kaybedilen varyantlar da dahil); `src/v8/statistics.py` bootstrap'ı uygular (`IMPLEMENTATION_LAYOUT.md` güncellendi, D-032). Tek bir N-yapılandırma testinde aileler-arası havuzlama — başlangıçta önerilen daha eksiksiz araç — **uygulanmaz**: aileler ayrık episode ızgaralarında ateşlenir (farklı `episode_key` kümeleri) ve White'ın kurulumu tüm N serisinin ortak bir dizinde hizalanmasını gerektirir; doğru şekilde genişletmek bar-seviyesi bir panel gerektirir (ateşlenmeyen yapılandırmaları sıfır-doldurma), ki bu ad hoc inşa edilmek yerine O-021'e ertelenir | PROVISIONAL_DECISION | Sahip kararı 2026-08-06, holdout-öncesi (prereg §16 uyarınca yasal, D-041 ile aynı dayanak — dondurulmuş holdout açıldıktan sonra onaylanmış bir preregistration'ın hiçbir alanı değişemez, ama holdout-öncesi hiçbir şeye dokunulmadı). Bunu motive eden, tek bir kök nedeni paylaşan iki kusurdu: kural 13'ün ontolojisi ("bir varyant yeni bir Expert değildir") prereg §11'de *istatistiksel* bir iddia ("varyant araması hiçbir şeye mal olmaz") olarak fazla okundu, ki bu yanlıştır — bir aile içindeki en-iyi-N varyant araması, kural 11'in kontrol etmek için var olduğu seçim-yanlılığını tam olarak yeniden başlatır (kurallı örnek Aronson'ın 6,402-kural vakasıdır; aile-bir-sayılır kuralı altında 3 multiplicity birimine — üç tema × bir parametre ızgarası — çöker, gerçek aramayı büyüklük sıraları kadar eksik gösterir ve 3-yönlü Bonferroni altında p=0.82 sonucunu yanlış bir "anlamlı"ya çevirir). Düzeltme, önem kazanmaya başladığı anda dayanmalıdır: Lim çıkarımı ilk kez gerçek aile-içi varyant önerileri üretmek üzeredir ve kusur sessizdir (hiçbir şey başarısız olmaz; en yüksek beklenen varyant hacmi noktasında yalnızca bir yanlış-pozitifi kabul eder). Henüz bir çalıştırıcıya bağlanmadı — `tools/run_experiment.py` (v8_slice_001 çalıştırıcısı, `IMPLEMENTATION_LAYOUT.md` uyarınca Phase-4 yapı hedefi) henüz yok, bu yüzden `statistics.py` yalnızca sentetik veri üzerinde birim-test ediliyor, henüz gerçek bir `LabReport`'a karşı çalıştırılmadı. || D-045 | Preregistration §2 boş hipotezi yeniden-merkezlenir: HAM episode net_R üzerindeki `μ_f ≤ 0`, aynı-exposure pasif bir kıyaslamaya karşı **trendden-arındırılmış** net_R üzerindeki `μ_f ≤ 0` ile değiştirilir (Aronson Ek A: `Corrected = Σ_long(R_i − E[Raw]) − Σ_short(R_i − E[Raw])`). Episode başına kıyaslama, puanlanan pencerenin bar-başına ortalama log fiyat değişimi `μ_log` olmak üzere `sign(direction) × entry_price × (exp(μ_log·h) − 1) / risk_unit`'dir. `CounterfactualOutcome` `entry_price`, `risk_unit_price` ve `market_move_r` kazanır; `simulator.hash()` `canonical-sim-v6` olur. Ham ortalama, trendden-arındırılmışın yanında bir teşhis olarak raporda kalır ve farkları `position_bias_component` olarak yayınlanır. Sinyal ÜRETİMİ dokunulmaz — trendden-arındırılmış değerler yalnızca getiri hesabında vardır (Ch1 p27-28) | PROVISIONAL_DECISION | Sahip kararı 2026-08-06, holdout-öncesi (prereg §16 uyarınca yasal, D-041/D-044 ile aynı dayanak — holdout-öncesi hiçbir şeye dokunulmadı; hiçbir manifest, store veya sonuç defteri yok). Becerisiz bir kuralın beklenen getirisi yalnızca trendden-arındırılmış veride tam olarak sıfırdır (Ch1 p23-27; Ch5 p235; Ch9 p394): trend yapan bir tape üzerinde long-yanlı bir aile, sıfır öngörü gücüyle pozitif beklenen net_R kazanır ve üç pilot da long-yönlü setup'lar taşır. Tohumlu bir yükseliş trendi üzerinde yeniden üretildi (`tests/test_detrended_null.py`): %0/%60/%90/%100 long dolulukta becerisiz plasebo aileler −0.465/+0.122/+0.377/+0.465 ham ortalama net_R gösterir; bunun %77-93'ünü trendden arındırma kaldırır ve `exp(μ·h)` ile `exp(Σ steps)` arasındaki Jensen boşluğu olan ~0.03'lük bir kalıntı bırakır. Alanlar aşağı yönde yeniden türetilmek yerine kaydedilir çünkü bir taslak `atr_ref` bildirmediğinde `simulator.risk_unit`, `entry_price × risk_frac`'a geri düşer, bu yüzden R paydası yalnızca candidate kaydından geri kazanılamaz ve `simulator.run`'ın kendi docstring'i formülün başka bir modülde ikinci bir kopyasını yasaklar. `market_move_r` şimdi, işaretsiz kaydedilir — yalnızca METH-3 (permütasyon testi, EV G-03) ona ihtiyaç duysa da — iki yerine tek hash yükseltmesi. Golden fikstür üzerinde değişiklik-öncesi koda karşı ölçüldü: 21 sonucun tümünün `net_r`'si artı `endpoint`, `label_status`, `data_hash`, `states_hash`, `candidate_count` ve `terminal_distribution` DEĞİŞMEDİ; yalnızca `ledger_hash` kaydı — ki bu, bir kararı değiştirmek yerine kanıt eklediğinin kanıtıdır. Burada ÇÖZÜLMEDİ: Ek A "≈ 0" toleransı (`statistics.invariant_holds`) bilerek uygulanmamıştır ve yükseltir — eşiğin kendisi önceden kaydedilmiş bir sabittir ve sessizce dondurulmuş bir varsayılan yerine açık bir operatör seçimine bırakılır. |
| D-046 | `EXPERTS_REGISTRY.yaml` zorunlu bir `search_universe_size` kazanır: aramayı bir aileye ulaşmak için tükettiği yapılandırmaların TOPLAM sayısı — parametre ızgaraları, atılan gösterge varyantları, yön-işareti seçimi ve asla `variant_id` kazanmayan kaybedenler — ki D-044'ün `variants_evaluated`'ı bunun yalnızca tutulan alt kümesidir ve bu yüzden bir alt sınırdır. `tests/test_expert_registry.py` varlığı ve `search_universe_size ≥ len(variants_evaluated)`'i uygular; `tools/run_experiment.py` her aile için ikisini de yayınlar artı, bildirilen arama, Reality Check'in serisini gerçekten gördüğü varyantları aştığında bir `multiplicity_undercounted` bayrağı. Kayıtlı beş pilotun tümü 1 bildirir | PROVISIONAL_DECISION | Sahip kararı 2026-08-06, holdout-öncesi. Aronson Ch8 p390-391, test edilen her kural ve parametre kombinasyonunun eksiksiz bir kaydını gerektirir; tutulan alt kümeden hesaplanan bir aile p-değeri, aramanın onu aştığı marj kadar tam olarak iyimsedir. Kurallı örnek D-044 altında zaten kaydedildi: 6,402 kuralın 3 multiplicity birimine çökmesi p=0.82'yi yanlış bir geçişe çevirir. Bildirilen 1 bir eksiklik değildir — prereg §4, her dedektör parametresinin (EMA 5/20, ATR 14, 32-bar önceki-yüksek penceresi, vade 8) gerçek dev penceresi var olmadan ÖNCE sentetik tapelere karşı kodda dondurulduğunu ve hiçbir geliştirme araması yapılmadığını kaydeder, bu yüzden sayılacak açıklanmamış bir yapılandırma yoktur. Değer, el-kitabı çıkarım aileleri (24 önerilen Expert, birkaçı bildirilmiş parametre ızgaralı) kayıt olmaya başladığı anda yük taşır; bayrak, eksik sayımın bir ailenin anlamlılığını sessizce şişirmek yerine kayıtta görünür olması için vardır. |
| D-047 | Pozisyon yönetimi ve doldurma politikaları BİLDİRİLMİŞ, isteğe bağlı mekanikler olarak gelir (EXEC-1..6; el kitabının EX-01..EX-12'si): `simulator.py` başabaş (breakeven) rulosu + chandelier takibi kazanır (`breakeven_roll_at_mfe_r`, `breakeven_margin_r` = `round_trip_cost_r`, `trail_stop_atr`, `OpenPosition.stop_level`/`stop_rolled` — uç nokta STOP kalır), scale-out kısmi çıkışı (`scale_out_ratio` > 0 etkinleştirir, `scale_out_at_mfe_r`, NON-TERMİNAL bir olay olarak `StepResult.closed_fraction` < 1.0, `remaining`/`realized_r` kesir-ağırlıklı R muhasebesi), TIME_EXIT uç noktası (`time_exit_bars`, EXPIRY'den ayrı), FILL_AT_LIMIT doldurma politikası (`risk_geometry['limit_price']`'da bariyer girişi, yalnızca-doldurma giriş-barı incelemesi, asla doldurmayan emirler asla girmez) ve bildirilmiş-ama-P2 `pyramid_add_rules` anahtarı (istek üzerine kapalı-başarısız; `midpoint_stop` ilkeli uygulandı + test edildi). `lifecycle.py` yalnızca-ekleme `PositionAction` olayını kazanır (PARTIAL_EXIT). `sim.hash()` → `canonical-sim-v8` | PROVISIONAL_DECISION | Sahip kararı 2026-08-06, holdout-öncesi. Her anahtar isteğe bağlıdır; pilotların dondurulmuş geometrisi hiçbirini bildirmez ve varsayılan geometride step()/run() çıktısı değişiklik-öncesi kodla bayt-bayt özdeştir — golden fikstür üzerinde her iki kod sürümünün icra edilen sonuçlarını farklayarak doğrulandı (icra edilen 12 sonucun tümünün net_r/endpoint/label'i özdeş; data_hash/states_hash/candidate_count 21/terminal_distribution değişmedi; yalnızca ledger_hash kaydı, çünkü her sonuç yeniden-sürümlenmiş simulator_hash'i taşır). CRIT-3/EXEC-6 uyarınca hash/endpoint disiplini: `simulator.py`'deki herhangi bir anlamsal düzenleme her sonucu yeniden-sürümler; uç nokta kelime dağarcığı TIME_EXIT ile genişletilir ve PARTIAL_EXIT asla bir sonuç uç noktası olmayan, terminal-olmayan bir lifecycle olayı olarak belgelenir (CANDIDATE_LIFECYCLE_SPEC §4 rekabetçi-risk ayrıştırması dokunulmaz — kısmi bir çıkış nedeni değildir). Eşikler BİLDİRİLMİŞ sayısal değişmezlerdir, asla uydurulmaz (O-017 yöntemi; CRIT-9: `breakeven_roll_at_mfe_r = 1.5` ve `f = stop_r/(stop_r+target_r)` kitabın değerleridir, dev-penceresi uydurmaları değil). Burada uygulanmadı: O-013'ün kabul kapısı (statik geometriye karşı çoğaltılmış OOS kazancı — bir preregistration eylemi), RM-04 iki-kademeli heat (`risk.py`'de uykuda kanca; bir D-023 alan revizyonu gerektirir, CRIT-2.6), piramitleme ve EX-13 eylem kafesi (P2). D-047, O-013'ün karşılaştırmasının üzerinde çalıştığı mekanikler katmanıdır. |

| D-048 | Risk yönetimi eklemeleri gelir (RISK-1..6; el kitabının RM-01..RM-19'u): `CandidateDraft.size` (RM-01, CRIT-3 — sim.hash() çıktı bayt-özdeşliğinden BAĞIMSIZ olarak canonical-sim-v7'ye yeniden-sürümler), dondurulmuş `DRAWDOWN_BANDS` merdiveniyle `equity.RiskState` (RM-06, O-016 challenger), `trade_units_for` (RM-07, %1 risk -> 100 birim, %20 -> 5), manifest `risk_per_trade`/`min_trades` kapıları (RM-02/RM-08), `size*stop_r` olarak heat (RM-04, size=1.0'da bayt-özdeş), rapor teşhisleri olarak WCS + spread-ayarlı R/r (RM-10/11). Hepsi deterministik, önceden kaydedilmiş eşikler asla uydurulmaz | PROVISIONAL_DECISION | El kitabı Ch28/Ch29; EV_METHODS G-02 kalibrasyonu; değişiklik-öncesi golden fikstüre karşı ölçüldü: 21 sonucun tümünün net_r'si eleman-eleman özdeş, candidate_count/terminal_distribution/data_hash değişmedi (boyutlandırma R'de boyuttan-bağımsızdır) |
| D-049 | Özellik-grafiği uzantısı FG-1..FG-7 (G-01..G-43): `FEATURE_GROUPS` `candle_shape`/`oscillator`/`session`/`positioning` kazanır, `participation`'ı etkinleştirir, `volatility`/`location`'ı genişletir (El kitabı §0'dan birebir formüllerle 73 yeni özellik; sinyal-yok koşulları asla None değil, sayısal nöbetçiler yayar — D-024 DEGRADED vetosu bir veri-bütünlüğü vetosu olarak kalır). `FEATURE_GRAPH_VERSION` yeniden hesaplar ve her durum lineage_hash'i yeni özellikleri bağlar | PROVISIONAL_DECISION | El kitabı Ch6/8/9/10/12/13/14/21/26; CRIT-1 (Ch27.2 ATR aralık filtresi swing anlamlılığı üzerinde); değişiklik-öncesine karşı ölçüldü: candidate_count/terminal_distribution/data_hash değişmedi — özellik kanıtı eklendi, hiçbir karar değişmedi |
| D-050 | 24 el-kitabı+kanıt çıkarımı expert ailesi kabul edilir (EXP-01..24; D-042'nin pilot kümesi 28 kod modülüne genişler): her biri kendi-kendine kapılanır, dondurulmuş `variants_evaluated` (D-044) + dürüst `search_universe_size` (D-046) taşır; `breakout_retest` DATA_BLOCKED -> FORMALIZED'e geçer (kod artık var, D-049-of-E-11); `open_interest_divergence` DATA_BLOCKED kalır (mevcut tape'te OI yok; kod kendi-kendine NO_HABITAT'a kapılanır); `capitulation` DATA_BLOCKED kalır (kod yok) | PROVISIONAL_DECISION | El kitabı E-01..E-24 kartları; CRIT-4 (failed_breakout_2b varyantları b..g, varyant 'a' orijinal pilot için ayrıldı), CRIT-6 (ıraksama bildirilen alt küme), CRIT-7 (gizli bekleyen durum yok; rejim kapısı expert-başına önkoşuldur, router değil) |
| D-051 | v2 araştırma alt sistemlerini sürüm kontrolünden emekliye ayır: `research/pipeline_v2/` (v2.3 araştırma-corpus pipeline'ı — corpus, prompts, registry, rounds, workflows, schemas, legacy_v1, scratch) ve `research_base/` (kendi pyproject'ine sahip bağımsız `v8research` paketi) ağaçtan kaldırılır, `research/revision/` görsel-önizleme yapıtları ve `research/text/` corpus kopyalarıyla birlikte; veri kaynakları oldukları üç revizyon-monograph derleyicisi (`tools/build_v8_revision.py`, `tools/generate_gemini_master_html.py`, `tools/generate_10k_gemini_master_html.py`) onlarla birlikte kaldırılır. Yerine tek araştırma kod tabanı olarak `src/v8/` çalışma zamanı (Phase 0-4) geçer | PROVISIONAL_DECISION | Düzenleme 2026-08-06, holdout-öncesi: emekli edilen yollara hiçbir prereg alanı, sözleşme veya kod yolu başvurmaz (grep ile doğrulandı — 615-test paketi ve monograph yapısı etkilenmez). v2.3 defterine tekrar ihtiyaç duyulursa git geçmişinden kurtarılabilir (`a051a20` içinde tanıtıldı) |

| D-052 | Preregistration §9'un blok-boyutu kuralı bar-birimlerinden n-uyarlamalı bir episode-birimi oranına düzeltilir ve blok bootstrap kapalı-başarısız bir `block_size < n` değişmezi kazanır. Kuralın iki-kademeli mekanik biçimi değişmez (0.10'daki lag-1 otokorelasyon kapısı kademeyi seçer); yalnızca kademe DEĞERLERİ değişir: varsayılan `round(n**(1/3))`, kapı ateşlendiğinde ikiye katlanır, `n // 2`'de sert tavanlanır. Önceki 24 / 168 sabitleri, bir episode-dizinli `net_R` serisine uygulanan bar-sayılarıydı ("bir gün" / "bir hafta" 1h bar) — §9'un kendi düzyazısında görünür bir birim hatası ("24 episode-blok (bir gün)"). Yeniden örnekleme sayısı, alfa'dan bağımsız seçilen bir sabit olmaktan çıkar: `resamples_for_alpha(alpha)`, `int(N * alpha) >= 100` olan en küçük N'i döndürür, önceden kayıtlı 2000'e tabanlanır, böylece tek-yanlı sınır, hangi aile sayısı yürürlükteyse o sayıda kararlı bir sıra istatistiğidir. Kural `v8.statistics.select_block_size` ve `tools/run_experiment._block_size` içinde çoğaltılmıştı; araç artık modüle devreder, tek kayıt kuralı yapar | PROVISIONAL_DECISION | Kusur mekanik olarak yeniden üretildi: `block_size >= n` olduğunda dairesel örnekleyici tüm n dizininin döngüsel bir dönüşünü yayar, bu yüzden her yeniden örnek her öğeyi tam olarak bir kez içerir ve `block_bootstrap_means` 2000 yeniden örneklemenin tümünde TEK bir benzersiz değer döndürür — bootstrap bir nokta kütlesine çöker ve `ci_lower == mu_hat` sıfır genişlikte olur, bu yüzden pozitif ortalamalı ve `n >= MIN_EPISODES`'li her aile yapı itibarıyla H0'ı reddeder. Sabitlenmiş 2026-08-06 dev taban çizgisinde episode'lu 21 aileden 8'i `block >= n`'e sahipti ve tam olarak bir sahte reddetme sonuçlandı (`obv_adl_regime`, n=50, block=168, `ci_lower == mu_hat == +0.0150`). Dejenerelik bir yanlılığın uç noktasıdır, izole bir vaka değil: `block/n ≈ 0.3–0.5`'te (n=45..100'deki her aile) yeniden örnek varyansı zaten eksik bildirilir, reddetmeye doğru yanlılık yaratır. Seçimin sonuç-tarafsızlığı kanıtlanmıştır: ele alınan üç aday kural — (A) yalnızca-kelepçeleme `min(24|168, n//2)`, (B) benimsenen `n**(1/3)` oranı, (C) bar hedefini gözlemlenen medyan episode-arası aralık üzerinden dönüştürme (16–197 bar, 1–5 bloklar verir) — tümü o taban çizgisinde SIFIR H0 reddi verir, bu yüzden hiçbir kural ürettiği hüküm için seçilmiş olamazdı. (A), yalnızca patolojik uç noktayı düzelttiği ve anti-muhafazakâr yanlılığı bıraktığı için reddedilir; (C), aralık-bağımlı, dolayısıyla mekanik olmadığı için reddedilir. Yeniden örnekleme-sayısı kusuru iki-aileli çalıştırıcının kendi `alpha_f`'ında (0.05/2 -> dizin 50) görünmezdi ve yalnızca slate-geneli Bonferroni alfası altında ortaya çıktı; aynı 2000 sınırı `int(2000 * 0.0017857) = 3`'e koydu — 0.18'inci yüzdelik tahmin eden 2000 yeniden örnek ortalamasının 4'üncü-en-küçüğü; alfa-bağlı kural 28-aileli slate için 56000 (dizin 100) ve iki-aileli çalıştırıcı için 4000 verir. Benimsemeden önce ölçülen maliyet: n=1000'de aile başına 4.2 sn, n=100'de 0.6 sn. Sabitlenmiş taban çizgide doğrulandı (öncesi ve sonrası özdeş `ledger_hash` 452d91bcf890, bu yüzden karşılaştırma eşzamanlı expert düzenlemeleriyle kirlenmemiştir): dejenere aileler 8 -> 1 (hayatta kalan n=1'dir, bootstrap'ın yapı itibarıyla varyansı yoktur ve §12 kapısı zaten hüküm verir), H0 reddetmeleri 1 -> 0 ve eski sıfır-genişlikli her satır gerçek bir aralık kazandı (`obv_adl_regime` +0.0150 -> -0.3067). Aralıklar slate genelinde genişledi (örn. `liquidity_sweep_reclaim` -0.267 -> -0.420), eski kuralın yalnızca uç noktada başarısız olmak yerine belirsizliği eksik bildirdiğini doğruladı. 628/628 test geçer. Holdout-öncesi donduruldu; dondurulmuş OOS açılmadan önce onaylandı, holdout sonuçlarını gördükten sonra asla |

| D-053 | 2026-08-06 hata-düzeltme geçişinin davranış değişiklikleri O-023'ün kabul koşuluna göre sürümlenir: `failed_breakout` **v1** kalır (bir hata-tamamlama — düzeltilmiş episode serisi eskisinin dar bir alt kümesidir, 369 -> 76, bu yüzden v1 hatalı kapıyı asla bilinçli bir yapılandırma olarak içermedi); `volume_climax_reversal` ve `fib_projection_reversal` **v2 challenger'ları** olur (düzeltilmiş seri, v1 kapısının üretemeyeceği episode'lar EKLER — climax-bar çapası 67 -> 380, köken-tabanlı uzantılar 2 -> 7 — bu yüzden kendi D-044/D-046 muhasebesi ve taze bir registry girdisi gerektiren yeni davranıştırlar). v1'in hatalı davranışı bu kararla arşivlenir, sessizce değiştirilmez | PROVISIONAL_DECISION | Dondurulmuş OOS, preregistration'ın adlandırdığı sürümde çalışmalıdır (kural 15 / D-023: "sürümler challenger + dondurulmuş-OOS + registry kararı ile evrilir, asla yerinde değil"). v2, ailenin `variants_evaluated` ve `search_universe_size`'ını korur (düzeltme bir hipotez-tamamlama, eşik araması değil — D-044/D-046 değişmez) ama YENİ bir `expert_version` (v2), ki episode_key'e girer, böylece v1 ve v2 episode'ları asla çakışmaz. 2026-08-06'da operatör tarafından seçildi (O-023, seçenek "O-023 kriteri"). holdout-öncesi donduruldu; dondurulmuş OOS açılmadan önce prereg bu aileler için v2'yi adlandırmalıdır |
| D-054 | Expert'ler ihtiyaç duydukları MarketState'i bildirir ve çalışma zamanı tam olarak onu sunar. Üç eklemeli parça: (a) `Expert.intervals` + `Expert.depth`, dondurulmuş bir şartname olarak `requires`'a katılır, `requires` zaten olduğu gibi tüketime karşı denetlenir; (b) yeni bir `src/v8/interval.py`, taban-aralık tape'ini bildirilen herhangi bir tamsayı-katı aralığa toplar ve `MarketState` özellikleri bir aralık ad alanı kazanır (`{sym}.{tf}.{feature}`; taban aralık öneksiz kalır, böylece mevcut her Expert ve test etkilenmez); (c) her Expert, tam olarak bildirilen gruplar x aralıklar x derinliği içeren bir MarketState alırken, karar saati başına BİR kurallı üst-küme durumu kalıcı denetim kaydı olarak kalır — Expert-başına nesne onun bir izdüşümüdür, asla bağımsız inşa edilmiş bir durum değil. Çalıştırma-başı fizibilite kapısı, bildirilen her (aralık, derinlik) çiftini tape'e karşı doğrular ve açık bir Expert-başına `EVALUABLE` / `NOT_EVALUABLE(reason)` hükmü yayar | PROVISIONAL_DECISION | 2026-08-06 expert envanteriyle motive edildi: 27 aileden 12'sindeki bağlayıcı kısıt 1h tape değil, küresel 32-bar `history` sabitlemesidir (O-020) — `ichimoku_cloud` 78 bara ihtiyaç duyar ve 4 varyanttan 3'ü bildirilmiş-ama-değerlendirilmemiştir, `breakout_retest` varyant d uygulanmamıştır, `donchian_breakout` 55-barlık bir çapa taramasından 50-barlığa geri düşer ve `pattern_measuring_objective` desenlerini hiç ifade edemez. Yalnızca `market_profile_value_area` gerçekten daha-yüksek-aralıklı bar yapısına ihtiyaç duyduğu gösterilebilir (`SESSION_BARS = 24`'ü elle kurar); `floor_trader_pivot` zaten mevcut `pivot_points_day` ile karşılanır. Saat başına TEK kalıcı durumu tutmak, her Expert'in saat D'de aynı dünyayı gördüğü özelliğini korur — Expert-başına kalıcı durumlar defteri 27x çoğaltır, `ExpertEvaluation.state_id`'yi paylaşılan bir denetim çapası olarak bozar ve bir Expert'in durumundaki bir sızıntı kusurunu görünmez kılar. Fizibilite kapısı, aynı envanterin açığa çıkardığı arıza modunu kapatır: `donchian_breakout` (384 tetikleme, 0 icra) raporda sinyali olmayan bir aileden ayırt edilemezdi ve altı sıfır-episode'lu aile aslında üç ilgisiz nedene bölünür (eksik veri kanalı, derinlik bloğu, exposure-yuvası çekişmesi). Aralık toplama değişmezleri sabittir: kovalar sabit bir UTC dönemine sabitlenir ve asla tape başlangıcına (aksi halde dev ve holdout pencereleri farklı barlar üretir), toplu `available_time` = SON kurucu taban barının kullanılabilirliği ve kısmi bir kuyruk kovası asla kapalı olarak yayılmaz |

| D-055 | `volume_climax_reversal`, kitabın 2-sigma a/b/c/d kümesinin yanında katı-climax bir challenger varyantı kazanır (`e`, vol_zscore >= 3.0), 2-sigma kapısının ~her barda ateşlendiği O-022 ölçümüne yanıt olarak (8,760 barda 8,272 farklı candidate -> %4.6'lık bir D-027 execution_share; aile kural-16 exposure havuzunu sular ve kendini bloklar). Varyant `e`, her 3-sigma barına sahiptir (satış climax'inden sonra LONG, alım climax'inden sonra SHORT) ve `search_universe_size` 5 ile `variants_evaluated`'a (a,b,c,d,e) katılır | PROVISIONAL_DECISION | Hipotez-seviyesi bir challenger, holdout-öncesi bildirildi ve donduruldu (D-036 disiplini), dev penceresinde SEÇİLMEDİ: dondurulmuş OOS üzerindeki aile-içi Reality-Check (D-044, prereg bölüm 11) `e`'yi a/b/c/d ile hizalı episode serilerinde karşılaştırır ve yalnızca hayatta kalan bir varyant ailenin etkili kapısını sıkılaştırabilir. 3.0 eşiği bildirilmiş bir sabittir (2-sigma 8,272-candidate seli, yanıtladığı ölçülen kusurdur). 663/663 test geçer |

| D-056 | Durum kurucu tam bir hızlı yol kazanır: `build_state`, isteğe bağlı bir sembol-başına `BarSeries` kabul eder (tüm seriler çalıştırma başına bir kez önceden hesaplanır) ve bunları karar saati başına dizinle okur, bar-güdümlü durum ön-yapımını O(N²)'den O(N × pencere)'ye çevirir. Sınırsız `prior_high`/`prior_low` TAM kayan önek maks/min'ini tutar (asla sabit bir pencere değil — 520 barda %83.5 ıraksama ölçüldü); büyüyen-liste lineage hash'leri, önceden hesaplanmış satır-başına kurallı baytlar aracılığıyla tam `sha1_hex(list)` anlamlarını korur; ham manifest, kline == closed olduğunda kapalı-liste özetini yeniden kullanır. Yayılan her değer, lineage, `lineage_hash` ve `state_id` bayt-özdeştir (her barda cached == uncached, `tests/test_state_cache_identity.py`); golden fikstür diff'i, 0 farklı alanla candidate'lar/değerlendirmeler/sonuçlar ve yalnızca kaynak `code_version`'da farklılaşan durumlar gösterir (tüm-dosya hash'i — tasarlanmış davranışı), golden testte yeniden sabitlendi | PROVISIONAL_DECISION | 8-sembol x 4-yıl tape'teki ölçülen duvar O(N²) durum kurmaydı (35,064 bar/sembolde tahmini 11–12 saat vs 8,760'da 280 sn). Düzeltildi: 8,760 bar 280 sn -> 12 sn; BTCUSDT 4y (35,064 bar, funding dahil tam lab biçimi) 67 sn; 8-sembol projeksiyonu ~9 dk. Durum başına kalan O(N), tam büyüyen-liste lineage hash'idir — `input_lineage_hash` anlamlarını değiştirmeden indirgenemez (reddedildi; kimlik hash'leri kımıldamamalı). 668/668 test geçer |
| D-057 | Giriş tetikleyici yüklemi normatiftir ve uygulanır: `risk_geometry['trigger_ref']` bildiren (mutlak, tespitte dondurulmuş) bir taslak yalnızca kitabın kapanış-doğrulamasında girer — `trigger_side` 'CLOSE_ABOVE'/'CLOSE_BELOW' (Ch14.2 "yalnızca tetikleyicinin ÖTESİNDEKİ bir KAPANIŞTA giriş"). `lab.py` PHASE 2, PENDING -> TRIGGERED'den önce onu değerlendirir; doğrulanmamış candidate'lar ateşlenene, geçersizleşene veya epilog sürelerini bitirene dek PENDING kalır. `candlestick_reversal` pilottur ve `trigger_side` bildirir. Tetikleyicisi olmayan Expert'ler `entry: NEXT_BAR_CLOSE` (koşulsuz) tutar | PROVISIONAL_DECISION | Issue #62/#67. Düzeltme-öncesi tetikleyici fiyatı hesaplanıyor ve atılıyordu: kapanış tetikleyicinin ötesini DOĞRULAMASA da 27 candlestick candidate'ından 16'sı tetiklendi. Bu, literatür giriş kapısını geri getirir; tetikleyici-vs-koşulsuz A/B `.audit/` ve CHANGELOG'da ölçülür. Bir ranker TANITILMAZ (kural 6/14; D-008 değişmedi) — bu bildirilmiş giriş olayıdır, bir candidate sıralaması değil |
| D-058 | Dondurulmuş bir yapısal stop (`risk_geometry['stop_ref']`, mutlak fiyat), bildirildiğinde statik stoptur; `stop_r × unit` geri düşüştür. `stop_r`, bildirilmiş R-katı anlamını korur (heat = size × stop_r, D-023, değişmedi); yapısal seviyedeki bir stop-out dürüst mesafedir | PROVISIONAL_DECISION | Issue #63. Düzeltme-öncesi, expert süpürülen uç noktayı / desen seviyesini dondursa bile stop `entry ± stop_r × ATR`'de duruyordu (33/33 candlestick taslağında ortalama 0.44R ıraksaması; icraların %37.3'ü yalnızca olumsuz excursion ile durduruldu). Yapısal-stop A/B `.audit/` içinde |
| D-059 | Giriş-öncesi geçersizlik geri düşüşü, SINIRSIZ önek uçları olan ve hiçbir ref dondurmayan expert'ler için kapıyı ölü kod yapan tüm-barlar `prior_high`/`prior_low` durum özellikleri yerine PENCERELİ bir uç nokta kullanır (doğumdan önceki 32 bar) | PROVISIONAL_DECISION | Issue #66. Ref'siz 6 expert için 2,067 taslakta 7 geçersizlik ateşlemesi ölçüldü (tüm-zamanlar uç noktası eski bir sivri uçla sabitlenmişti). 32-bar penceresi dondurulmuş-ref kuralıyla eşleşir (failed_breakout / liquidity_sweep). Önceki barı olmayan bir doğumda kapalı-başarısız korunur |
| D-060 | Exposure çekişmesi altında (enstrüman-yön başına bir aktif exposure, D-018), hayatta kalan candidate, `expert_id` alfabetik değerlendirme sırasıyla değil, episode_key hash sırasıyla seçilir | PROVISIONAL_DECISION | Issue #68. Düzeltme-öncesi alfabetik-ilk expert, çekişmeli 303 yuvanın 295'ini kazandı (%97.4) ve icra edilen alt küme ortalama setup'tan 1.83× daha kötüydü. Candidate-hash sırası deterministiktir, ekonomik olarak tarafsızdır ve açıkça bir ranker DEĞİLDİR (kural 6/14, O-006/O-012 değişmedi): örtük ad-tabanlı ranker kaldırılır, resmileştirilmez |
| D-061 | Kurallı simülatör, `step()`/`run()` girişinde risk_geometry'yi doğrular: pozitif-olmayan `target_r`/`stop_r` ve `expiry_bars < 1` kapalı-başarısız olur | LOCKED_INVARIANT candidate | Issue #70. Düzeltme-öncesi `target_r=-1`, −1.07R'lik bir kaybı TARGET uç noktası olarak kaydediyordu (herhangi bir isabet-oranı / kâr-faktörü istatistiğinde bir kazanç) ve `stop_r=0` kabul ediliyordu. Derinlemesine-savunma: expert'lerin kendi korumaları kalır; simülatör son savunma hattıdır, böylece gelecekteki bir expert defteri sessizce kirletemez |
| D-062 | RM-11 fizibilite kapısı yalnızca-raporluktur: maliyet-bozulmuş başabaş kazanma oranı (w_min) gerçekleşen kazanma oranını aştığında, çalıştırma bir FEASIBILITY notu taşır. RR=1.0 `target_r` (27 expert'ten 19'u), yapısal bir hedef (ölçülen hareket / kanal genişliği / pivot) var olana dek bir DESIGN_INFERENCE'dır | PROVISIONAL_DECISION | Issue #64. Düzeltme-öncesi başabaş (~%53.5) ile gerçekleşen (~%45.3) kazanma oranı arasındaki 7.6-puanlık boşluk hesaplanıyordu (RM-11) ama asla gerçekleşen orana karşı yüzeye çıkarılmıyordu. Hüküm değişikliği değil ve bir geometri taraması değil (optimizasyon açıkça çözüm değil) |
| D-063 | `EXCESS_COST_THRESHOLD_R` ateşlemesi bir FEASIBILITY beyanı olarak raporlanır ("bu geometri bu maliyette işlem yapamaz") ve borsa şemasından varsayılan `round_trip_cost_r` türetmesi bir denetim kalemi olarak kaydedilir | PROVISIONAL_DECISION | Issue #69. 0.10R ≈ 4.8 bps dev penceresinde < gerçekçi bir taker gidiş-dönüşü (6 bps); maliyet 0.125'te lab işlem yapmayı reddeder (0 icra, 6,209 excess_cost reddi). Kapı tasarlandığı gibi çalışır; kusur, varsayılan maliyetin tam altında oturması ve bu yüzden asla ateşlenmemesiydi |
| D-064 | `make_synthetic_tape` boşluk-duyarlı teşhisler için bir `continuous` varyant kazanır (open = önceki kapanış ± küçük hareket); eski varsayılan, sabitlenmiş golden/sözleşme testleri için bayt-özdeş kalır ve varsayılanı çevirmek, 30-dosyalık test patlama yarıçapı tek bir planlı geçişte yeniden sabitlenene dek ertelenir | PROVISIONAL_DECISION | Issue #72. Düzeltme-öncesi sentetik tape, barların ~%73'ünde TR > (H−L) boşlukları uyduruyordu; gerçek BTCUSDT 1h'de ~%0.6 (open==prev_close %0 vs %51.7). Continuous varyant, uydurulmuş-boşluk oranını ~%3.7'ye düşürür. Teşhis kuralı: mekanik/ekonomik her şey için gerçek tape |
| D-066 | NEDEN-NEGATİF teşhis motoru `tools/diagnostic.py` içinde yaşar, karar yoluna karşı SALT-OKUNUR'dur (asla bir store/registry/authority dosyası yazmaz — yabancı bir yazma `DiagnosticWriteError` yükseltir) ve bir hüküm enum'u ile `AUTHORITY: NONE — DIAGNOSTIC ONLY` raporları üretir, tüm karşı-olgular TEK sabit giriş kümesinde | PROVISIONAL_DECISION | Bir teşhis modülü karar-yolu kod hash'ini kımıldatmamalıdır (D-032): bir `src/v8/diagnostic.py`, kararları asla etkilemeyen bir modül için her sabitlenmiş manifesti geçersiz kılardı, bu yüzden `tools/` içinde oturur (`_code_hash` dışında). Spec: 9 bölüm + hüküm. İlk gerçek-tape çalıştırması: MECHANICAL_FLOOR — sevk edilen sinyal rastgele girişlerden ayırt edilemez; bir teşhis bulgusu, ekonomik bir iddia DEĞİL (otorite NONE kalır). 693/693 test geçer |
| D-065 | Kilitli evren Binance USD-M kripto kalıcılarında (perps) kalır; hisse senetleri (NASDAQ, BIST) bir ERTELENMİŞ araştırma eksenidir, bir Phase-0..4 teslimatı değil. Kapsam önerisi (2026-08-07) bir borsa uzantısı yerine O-026 olarak kaydedilir, çünkü O-011'in kapısı kilitli evrendeki kapsam başarısızlığını kapsar, genişlik talebini değil — ve hisse senetleri yapısal olarak farklıdır (funding/mark/index/premium tape'leri yok, oturum takvimi, kurumsal eylemler, farklı maliyet modeli). Hisse senetleri için data-plane keşfi, yalnızca-araştırma olarak paralelde İLERLEYEBİLİR (kural 11), ama O-026'nın kabul koşulları karşılanana dek hiçbir hisse senedi tape'i kurallı bir veri kümesine giremez, hiçbir `src/v8/` karar-yolu modülü onu okuyamaz ve hiçbir preregistration onu adlandıramaz: Phase-4 taban-durum kapısından sağ kurtulan bir aile + kendi multiplicity kontrolleri altında çapraz-varlık çoğaltımı + kural 9 kaynak otoritesi | PROVISIONAL_DECISION | Sahip kararı 2026-08-07. İlk kanıt kapısından önce kapsam genişlemesini önler: programın kendi kuralları (kural 12), Phase-4'te hiçbir aile maliyet-sonrası sinyal göstermezse çürütme programının durduğunu söyler ve bu taban durum ölçülmeden ikinci bir varlık sınıfı eklemek, hayatta kalan bir hipotez eklemeden kanıt disiplinini sulandırır. Veri-otorite kısıtı şimdi gerçek ve bağlayıcıdır: NASDAQ'ın ücretsiz arşivleri Binance Vision'ın checksum-doğrulanmış değişmezliğinden yoksundur (kural 9) ve BIST ticari bir sağlayıcı veya otorite-kabul-edilemez bir kazıyıcı gerektirir. Geri alınabilir — O-026'nın deneyi geçerse, hisse senetleri asla yapılandırma ile değil, taze bir registry kararı ile kabul edilir |
| D-099 | **Hesaplama ritüel değil, bütçelenmiş karardır.** Beş saniyeyi aşması beklenen bir hesaplamadan veya zaten yeşil olan bir kontrolün tekrarından önce değiştirebileceği kararı ve çözdüğü yeni semantik riski belirt. Yalnızca beklenen marjinal karar değeri toplam maliyetini (çalışma süresi, kullanıcı gecikmesi, araç/bağlam yükü ve tekrar) aşıyorsa çalıştır. | PROVISIONAL_DECISION | Zorunlu semantik-sınır kapıları yine çalışır: değişmiş doğruluk semantiği, determinizm/parity/kimlik sınırları, güvenlik/authority/veri-bütünlüğü davranışı ve açık handoff kapıları. En küçük ayırt edici kontrol seçilir; yalnızca doküman/format değişiminde paket tekrar edilmez; tam handoff paketi en fazla bir kez; yeşil sonuç sonrası ek doğrulama 60 saniyeyi aşarsa mevcut kanıt ve kalan belirsizlik raporlanır. `COMPUTATION_BUDGET_POLICY.md` belgesine bak. |
| D-100 | **Python oracle sınırı kilitlendi.** `src/v8/` hash'e bağlı, dondurulmuş tarihsel parity oracle'ıdır; istek ve doğrulama yolu `v8-core`'a aittir. Python yalnızca açıkça adlandırılmış legacy araştırma tooling'i, monograph üretimi ve sınır denetimleri için çalışabilir. Oracle ağacındaki değişiklik registry kararı, changelog girdisi ve yeni hash gerektirir; CI kayıtsız değişiklikleri ve Rust gate içindeki Python pytest/oracle çağrılarını reddeder. | LOCKED_INVARIANT candidate | `docs/legacy/PYTHON_ORACLE_LOCK.json`, `docs/legacy/PYTHON_ORACLE_POLICY.md` ve `tools/audit_python_boundary.py`. Oracle, bağımsız S0–S7 referansını yok etmemek için korunur; bu karar her legacy Python aracının zaten taşındığını iddia etmez. |
| D-101 | **Target Oracle O0–O1, Rust-yalnızca sözleşme ve ölçüm-çerçevesi altkatmanıdır.** `v8-core/src/oracle/` tam olarak üç-rollü taksonomiye, tipli ret sözlüğüne, sürümlendirilmiş maliyet-sonrası UtilityContract doğrulamasına, PIT InformationSet adaptörüne, bellek-içi kimlik-taşıyan artifaktlara ve açıkça kayıtlı ilkeller/şablonlar/ızgaralar üzerinde Expert-bağımsız sonlu bir dilbilgisine sahiptir. Grammar Candidate kimlikleri ayrı bir etki alanındadır ve asla `CandidateRegistry`'ye girmez. | PROVISIONAL_DECISION | `TARGET_ORACLE_SPEC` §§1–7, §17–19. Uygulama; destek/otorite sınıflandırmasını, kapsamı, kanıt-demeti serileştirmesini, raporları, Scout'u, pişmanlık atfını, Target-politika optimizasyonunu, L3 icrasını ve ekonomik hükümleri kasıtlı olarak dışarıda bırakır. |
| D-102 | **Target Oracle O2–O3, Rust'ta destek/otorite sınıflandırmasını, temsili kapsam mutabakatını ve v8.eval.v1 makbuzlarını uygular.** `v8-core/src/oracle/{support.rs,authority.rs,coverage.rs}`, candidate/aksiyon desteğini nokta tahminleri uydurmadan dikey `CounterfactualAuthority` ve `OracleOutcome` değerlerine sınıflandırır. `reconcile_coverage`, aynı-olay sevk edilmiş `ExpertEval` tekliflerine karşı temsili kapsamı ölçer, `population_hash` bağlar ve `OpportunityUniverseVersion`, `OracleEvaluationRecord` ve `CoverageReceipt` bulgu kayıtlarını doğrudan `v8.eval.v1` kanıt demetine `NO_ECONOMIC_CLAIM` etiketiyle kaydeder. Geliştirme CLI alt komutu `oracle-coverage` sunulur. | PROVISIONAL_DECISION | `TARGET_ORACLE_SPEC` §5.8, §8.1–§8.5, §9.1–§9.4, §16.1–§16.3, §17.5, §18.1–§18.2; Issue #155. Pişmanlık atfı (O4) ve politika optimizasyonu (O5) kasıtlı olarak ertelenmiştir. |
| D-118 | **Nicemlenmiş hesaplama uygulanabilirlik sınırı ve denetimi.** V8, kurallı bit kimlikleri, sabit indirgeme sırası ve skaler/SIMD/backend parite kontrolleriyle deterministik bir IEEE-754 `f64` hesaplama sistemi olarak kalır. Düşük-bit / tamsayı / sabit-nokta nicemlemesi, açık bir challenger teklifi sayı formatını, scale/zero-point'i, yuvarlamayı, kırpmayı, accumulator genişliğini, kalibrasyon popülasyonunu ve dondurulmuş-OOS hata metriklerini önceden kaydedene kadar varsayılan olarak yoktur. `docs/tr/QUANTIZED_COMPUTATION_AUDIT.md` bu sınırı güncel literatüre dayandırır (arXiv:1712.05877, arXiv:2202.07513, arXiv:2408.05148, arXiv:2608.12259). | PROVISIONAL_DECISION | Issue #199; `COMPUTE_CORE_SPEC` §2, `PARITY_AND_IDENTITY_SPEC` §1. Saf deterministik f64 hesaplama tabanını hız veya ekonomik iddia olmadan korur. |
| D-119 | **Güçlü tipli hata taksonomisi ve aday yaşam döngüsü sonlu durum makinesi.** Çalışma zamanı değerlendirmesindeki gevşek string hata yayılımını `v8-core/src/error.rs` altındaki `V8CoreError` enum'ı ile değiştir (`Scheduler`, `InvalidCandidateTransition`, `DataStreaming`, `Checkpoint`, `PathSanitization`, `Serialization`, `Io`, `QuantInvariant`). `CANDIDATE_LIFECYCLE_SPEC` §2 ile uyumlu olarak `v8-core/src/candidate.rs` içindeki `legal_typed` aracılığıyla `CandidateState` yaşam döngüsü geçişlerini zorunlu kıl. | PROVISIONAL_DECISION | Issue #208; `COMPUTE_CORE_SPEC` §4, `CANDIDATE_LIFECYCLE_SPEC` §2. Tip güvenliğini garanti eder ve derleme zamanında durum makinesi geçiş doğrulaması sağlar. |
| D-120 | **Çoklu özetli kriptografik mimari, yol temizleme ve telemetri cepheleri.** `v8-core/src/hash.rs` içindeki özetleme yüzeyini, tarihsel `SHA-1` kimlik özetlerini bayt-özdeş koruyarak `BLAKE3` (yüksek verim) ve `SHA-256` (çarpışmaya dirençli denetim) destekleyecek şekilde yükselt. `v8-core/src/path_security.rs` içinde derinlik-sınırlı yol geçişi savunması `sanitize_path` uygula. `v8-core/src/telemetry.rs` içinde yapılandırılmış `tracing` ve `metrics` cephelerini entegre et. | PROVISIONAL_DECISION | Issue #209; `PARITY_AND_IDENTITY_SPEC` §3, `FEED_INGESTION_SPEC` §5. Girdi yollarını güvenceye alır ve geçmiş hash'leri geçersiz kılmadan kriptografik doğrulamayı genişletir. |
| D-121 | **Bellek-haritalı sıfır-kopyalama tape akışı ve ikili IPC serileştirmesi.** Yüksek hacimli JSONL tape alımı sırasında $O(1)$ sınırlı bellek yükü sağlamak için `v8-core/src/data.rs` içinde `memmap2` üzerinden `Dataset::from_mmap_path` ekle. Sıfır kopyalı ikili durum transferi ve kalıcılık için `bincode` üzerinde standartlaş. | PROVISIONAL_DECISION | Issue #210; `DATASET_SPEC` §5, `PERSISTENCE_REPLAY_SPEC` §1. Çoklu gigabaytlık tarihsel yeniden oynatma çalışmalarında bellek sıçramalarını önler. |
| D-122 | **Simülasyon durumu checkpointing, devam motoru ve çoklu mimari sürüm boru hattı.** Güvenli kesinti ve `--resume` semantiğini desteklemek için `v8-core/src/checkpoint.rs` içinde atomik checkpointing (`SimulationCheckpoint`, tmp-yazma ve atomik yeniden adlandırma) ekle. `x86_64-unknown-linux-gnu` ve `aarch64-apple-darwin` için otomatik çoklu hedefli GitHub Actions CI/CD sürüm iş akışını (`.github/workflows/release.yml`) devreye al. | PROVISIONAL_DECISION | Issue #211; `OPERATIONS_SPEC` §2, `PERSISTENCE_REPLAY_SPEC` §4. Uzun süreli simülasyonlarda hata toleransı ve otomatik sürüm dağıtımı sağlar. |
| D-123 | **Kanonik Kanser Taksonomisi ve Mega Hareket Kampanyası Mimarisi.** 6 Kanonik Kök Kanseri (Lot Diskretizasyon Felci, Kuyruk Budayan Çıkış, Uzman Huni Çöküşü, Rejim Körlüğü, Mekanik Taban Yetersizliği, Veri Körlüğü) tescil et ve 05–06 Şubat 2026 BTC çöküş/yükseliş dalgasını kurallı zemin gerçeği (Ground Truth) olarak kabul et. Quantization-Aware Risk Bütçelemesi, Kuyruk Yakalama Verimliliği (TCE), Aday-Kampanya Kümelemesi ve Boyd Dinamik Likidite Tabanını ($CashFloor_t$) onayla. | LOCKED_INVARIANT | `docs/tr/CANONICAL_CANCERS_AND_MEGA_MOVE_AUDIT.md`, `VENUE_AND_CAPITAL_SIMULATION_SPEC` §10; arXiv:1603.06183, arXiv:2602.11708. Sınırsız aday üretimi ile fiziksel sermaye icrası arasındaki boşluğu kapatır. |
| D-124 | **ACCP v2.0 YAML Tabanlı Ajan Kodlama İletişim Protokolü.** 27 resmi rapor türünün tamamında katı YAML kaynak profilini (`.accp.yaml`) kademeli destek seviyeleriyle (`known`, `template_available`, `schema_lite`, `schema_strict`, `gate_blocking`) benimse. Çalışma zamanının ham metin/markdown okumasını yasakla; derlenmiş JSON çıktılarını (`.compiled.json`, `.gate-verdict.json`, `.route-signal.json`) zorunlu kıl. | LOCKED_INVARIANT | `docs/tr/ACCP_V2_SPEC.md`, `WORK_ITEM_POLICY.md` §1–4. Deterministik, şema-doğrulamalı ve kapı-denetlenebilir ajan iletişimini garanti eder. |
| D-125 | **Tarihsel Piyasa Arketipleri Kaydı ve Çoklu Epizot Alegorik Denetim Süiti (ALLEGORY-001).** Değerlendirme ve denetim motorunu tekil tarihsel tarihlerden 4 süper-sınıfa (Yönsel Fırsat, Zorunlu Akış / Stres, Düşük Fırsat / Çekişmeli, Portföy / Türevler) ayrılmış 12 kanonik piyasa arketipine (A01–A12) genişlet. Zorunlu negatif kontrol (anti-alegori) kalibrasyon çiftleri, ex-ante ve ex-post kısıtlanmamış / sermaye-kısıtlı regret ayrıştırması ve `NO_ECONOMIC_CLAIM` etiketli deterministik `allegory_scorecard.json` çıktısı ile sıfır-hindsight epizot sınırlarını zorunlu kıl. | PROVISIONAL_DECISION | `WORK_ITEM_POLICY.md` v1.2, `TARGET_ORACLE_SPEC` §9; arXiv:2607.19497, arXiv:2608.03616, arXiv:2208.01445. Tekil tarih anlatı aşırı uyumunu (overfitting) önler ve bilimsel arketip yanlışlamasını kurar. |
| D-126 | **Maliyet-Bilinçli No-Trade Bölgesi, Kalıcı Çok-Çubuklu Kampanya Kümeleme ve Lot-Güvenli Risk Değişmezleri (KZ-018 / KZ-008 / KZ-009 / KZ-007 / VERIFY-002).** P0 Hayatta Kalma ve Fırsat Yakalama Mimarisini onayla: (1) Beklenen brüt hareket işlem maliyetlerinin altındayken veya başarısız kampanya soğuma süresindeyken gereksiz işlem açmayı (churn) engelleyen KZ-018 No-Trade Bölgesi; (2) 7 bağımsız mekanizma ailesi üzerinden uyumlu yön ve geçersiz kılma havzasını birleştiren KZ-008 Kalıcı Çok-Çubuklu Kampanya Kümeleme (korele kanıt enflasyonunu önler); (3) Asgari yasal lotun SADECE `minimum_executable_risk <= allowed_campaign_risk` durumunda açıldığı, aksi halde kapalı-başarısız olduğu KZ-009 Lot-Güvenli İcra (`MIN_EXECUTABLE_RISK_EXCEEDS_BUDGET`); (4) Sabit 2.0R hedeflerini kaldıran KZ-007 Dinamik Süren Stop Çıkışları; (5) VERIFY-002 gerçek veri kampanya-düzeyi pişmanlık ayrıştırması. | KİLİTLİ_DEĞİŞMEZ | `docs/issues/ISSUE_KZ018_CHOP001_COST_AWARE_NO_TRADE_REGION.md`, `VENUE_AND_CAPITAL_SIMULATION_SPEC` §10; arXiv:2606.00060, arXiv:2407.13547, arXiv:1308.5658, arXiv:1705.00109, arXiv:2210.00807, arXiv:2207.07578. Büyük hareket yakalamayı korurken komisyon kaybını ve önlenebilir lot iptallerini ortadan kaldırır. |

Durum sözlüğü kasıtlıdır: `LOCKED_INVARIANT` geçerliliği sınırlar;
`PROVISIONAL_DECISION` geri alınabilir; `REJECTED_OPTION` adı geçen taban
çizgiden hariç tutulmuştur, sonsuza dek çürütülmüş değildir. Hiçbiri kârlılık
anlamına gelmez.
