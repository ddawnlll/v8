# V8 Decision Register v0.1

| ID | Decision | Status | Evidence / consequence |
|---|---|---|---|
| D-001 | Candidate is not an order; log every lifecycle terminal state | PROVISIONAL_DECISION | Design inference supported by V7 selection-bias/state-machine failures. Test against trade-only data. |
| D-002 | Use point-in-time immutable MarketState with explicit clocks | LOCKED_INVARIANT candidate | V7 causal/authority controls; leakage is a validity breach. |
| D-003 | Begin with 2–3 deterministic self-gating Experts | PROVISIONAL_DECISION | Smallest attributable routing baseline; no evidence experts beat global models. **Revised by D-043:** 2–3 was a starting point for the first slice, never a cap; the runtime Expert count is unbounded. |
| D-004 | Do not implement a router initially | REJECTED_OPTION for baseline | Router needs valuable-candidate recall and a binding compute/latency rationale. |
| D-005 | Canonical deterministic Level-1 execution is the initial comparator | PROVISIONAL_DECISION | V7 controls support the practice, but certification remains FAIL/BLOCKED. |
| D-006 | Learned execution/RL is excluded from the initial architecture | REJECTED_OPTION for baseline | V7 evidence has integration/state-machine issues and no economic certification. |
| D-007 | No scorer initially | PROVISIONAL_DECISION | Admit only on repeatable fixed-coverage OOS net utility versus deterministic score. |
| D-008 | No ranker initially | PROVISIONAL_DECISION | Admit only under demonstrated capital/risk contention and paired allocation gain. |
| D-009 | Keep alpha/execution operationally separated, not statistically independent | LOCKED_INVARIANT | The master brief and audit reject an independence claim. |
| D-010 | All promotion claims bind code/data/config/seed/ledger hashes | LOCKED_INVARIANT candidate | Project engineering evidence; missing receipt blocks a verdict. |
| D-011 | Research-only ingestion; venue adapters have no order/signing path | LOCKED_INVARIANT | FEED_INGESTION_SPEC §1; capability boundary, not convention. |
| D-012 | First venue adapter is Binance USDT-M; adapter interface is venue-agnostic | PROVISIONAL_DECISION | Corpus/brief examples are Binance-shaped; a second venue must not change consumers. |
| D-013 | Raw evidence in immutable Parquet; derived stores in DuckDB; append-only discipline | PROVISIONAL_DECISION | PERSISTENCE_REPLAY_SPEC §1; single-writer, replay-heavy research fit. |
| D-014 | Bar-driven decision clock is the default driver; event-driven mode is gated | PROVISIONAL_DECISION | RUNTIME_SCHEDULER_SPEC §1; determinism by construction for research. |
| D-015 | Single-process research runtime; single writer, read-only replay | PROVISIONAL_DECISION | RUNTIME_SCHEDULER_SPEC §3; concurrency only at declared idempotent boundaries. |
| D-016 | Live status is unreachable until the simulation authority is renewed | LOCKED_INVARIANT | OPERATIONS_SPEC §1; certification record is FAIL/BLOCKED. |
| D-017 | Five-level ontology with family metadata; variants are not Experts | PROVISIONAL_DECISION | Expert-protocol §1; family-level multiplicity correction. |
| D-018 | One active exposure per (instrument, direction); conflict -> CAPACITY_REJECTED | LOCKED_INVARIANT candidate | Lifecycle spec §6; prevents double-counted PnL and risk pile-up. |
| D-019 | Learning offline and registry-gated; no online mutation | LOCKED_INVARIANT | LEARNING_PROTOCOL; policy churn is the primary failure mode. |
| D-020 | Complexity budget: <=3 Experts, <=1 learned component, no router/scorer/ranker/RL | PROVISIONAL_DECISION | Constitution rule 14; one moving part at a time. **Revised by D-043:** the <=3 Expert cap conflated runtime capacity with the evidence budget and was already breached by the registry (5 Experts after D-042). The router/scorer/ranker/RL absence is unchanged. |
| D-021 | Compile-once materializations; training never reads raw tape | PROVISIONAL_DECISION | Dataset spec §5; separates simulation from training. |
| D-022 | Vendor V7 lab as `src/v8/simtruth/` + V7 `tools/data.py` as reference implementation | PROVISIONAL_DECISION | ENGINEERING-ONLY: authority stays FAIL until V8's own verification + renewal. |
| D-023 | Portfolio heat cap: heat = stop-risk sum (R), fixed correlated clusters, reject-not-downsize | LOCKED_INVARIANT candidate | User-approved policy; DeMiguel et al. 2009 supports simple deterministic caps over learned allocation. (Numbered D-023, not D-022 as an external synthesis proposed — D-022 is the vendor decision.) |
| D-024 | Mechanical tradability mask (funding window, spread tail, DEGRADED state) is a data-plane veto | PROVISIONAL_DECISION | Adds no degrees of freedom; distinct from learned regime labels (O-015). |
| D-025 | Fractional Kelly cap only; full Kelly forbidden | PROVISIONAL_DECISION | Busseti/Ryu/Boyd + MacLean-Thorp-Ziemba; risk-of-ruin under estimation error. |
| D-026 | `episode_key` is anchored to `setup_anchor_event_id`, never to a birth timestamp; defined once in CANDIDATE_LIFECYCLE_SPEC §1 | LOCKED_INVARIANT candidate | Resolves a three-way contradiction (lifecycle spec, expert protocol, `lifecycle.py`). A clock-dependent key makes suppression unreachable, so deduplication silently never fires. Code still implements the defective form — see CHANGELOG follow-up. |
| D-028 | R is a declared price distance (`risk_unit`), never a price ratio; `net_r`/`mae_r`/`mfe_r` are R-multiples and a non-positive unit fails closed | LOCKED_INVARIANT candidate | The simulator computed fractional price returns under R names while geometry, RiskGate heat (D-023) and the specs all read them as R. Fixed and covered by golden tests (stop-out is exactly −1R − cost; identical R geometry at 10× risk width yields identical `net_r`). Simulator hash bumped to `canonical-sim-v3` so pre-fix ledgers cannot compare equal. |
| D-029 | Post-entry thesis invalidation is a distinct exit (`THESIS_INVALIDATED`), never merged into `STOP` | PROVISIONAL_DECISION | Deterministic Expert predicate in the frozen spec, evaluated on closed bars; no learned component (rule 14), no new lifecycle state, fails open when inputs are unobservable. Keeps the section-4 competing-risk decomposition able to separate "wrong about direction" from "right to leave early". |
| D-030 | Excursions (`mae_r`/`mfe_r`) and `ambiguous_bars` are recorded on every outcome | PROVISIONAL_DECISION | Excursion was the only quantity V7 measured as materially predictable (ICs +0.124/+0.152 vs +0.015 signed return) and the vendored V7 simulator already records it; the V8 canonical simulator had regressed. Without it O-013 cannot be answered and the ambiguity bracket cannot be measured. |
| D-027 | `LabReport` carries `execution_share` + population divergence; both gate the economic verdict (`ATTRIBUTION_UNSAFE_*`) | PROVISIONAL_DECISION | Deterministic admission makes counterfactual-dominated populations structural, not accidental. OPE correction is unavailable under deterministic filtering (zero propensity, deficient support) and has no admissible finance application; the program's fail-closed rules then require declining the claim. Thresholds ratified 2026-08-01 (O-017): `execution_share` floor 0.25, two-sample KS ≤ 0.20, bound into `PREREGISTRATION_V8_SLICE_001` §15. Code gating lands with the Phase-4 experiment runner. |
| D-031 | Technology baseline: Python >= 3.11 stdlib-first decision path, single-process module topology, JSONL append-only ledgers at slice scale + immutable Parquet raw archive + DuckDB derived/research stores (PERSISTENCE_REPLAY_SPEC §1), pytest via uv, `numpy` confined to `simtruth/` and research tooling | PROVISIONAL_DECISION | ARCHITECTURE_SPEC §3 consolidates the previously scattered technology choices into one register entry. O-009 keeps only its scale-validation experiment; the design mapping is no longer open. |
| D-032 | File family is a frozen contract: `IMPLEMENTATION_LAYOUT.md` predetermines every file, its public interface, and its owning contract; new files, renames, or interface changes are registry decisions with CHANGELOG entries | PROVISIONAL_DECISION | The vertical slice produced files before any layout existed, and code/spec drift already occurred three times (episode_key, funding, D-024). Predetermination makes drift visible instead of silent; the layout evolves by challenger like every other versioned artifact. |
| D-033 | Expert modules live in `src/v8/experts/`, one file per behavior family, re-exported through the package registry; `marketstate.py` name is retained; `_code_hash` covers the whole package recursively | PROVISIONAL_DECISION | `experts.py` would not scale past the two pilots (Phase 3 adds families), and the flat glob missed subpackage files — a code hash that misses an Expert change would unbind the report (D-010). The `state.py` rename was proposed and rejected by the owner: `marketstate.py` mirrors the `MarketState` record name. |
| D-034 | Implementation pin for D-026: `setup_anchor_event_id` = `event_id` of the first closed bar of the current consecutive run where the Expert's setup predicate holds, found by scanning a new 32-bar `history` feature group (per-bar OHLC + EMAs); `episode_key` drops the birth timestamp; `is_duplicate` becomes key-equality on seen anchors (time-window removed) | PROVISIONAL_DECISION | Makes D-026 executable without redesigning the stateless per-bar Expert contract. The 32-bar bound is documented: anchors older than the window are unstable, and the 6-bar suppression window sits far inside it. Registered for the Phase 0-3 autonomous build (`AGENT_RUNBOOK` Step 1). |
| D-035 | Implementation pin for funding: `ExperimentManifest.funding_rate_r` (default 0.0) + `funding_hours` (default 8); settlements at integer UTC hours divisible by `funding_hours`, applied `SETTLEMENT_BEFORE_ORDERS`, one `funding_settled` ledger event per crossing, `net_r` reduced per settlement, longs pay positive rates; simulator hash bumps to `canonical-sim-v4` | PROVISIONAL_DECISION | SIMULATION_TRUTH_SPEC §3 event order #5 is normative; the golden tests (boundary count, window vs full-tape parity) are the V7 audit's demanded controls. Default 0.0 keeps today's numbers identical while the policy changes. `AGENT_RUNBOOK` Step 2. |
| D-036 | Implementation pin for D-024: manifest constants `max_bar_range_frac = 0.05`, `funding_window_bars = 1`; admission veto `TRADABILITY_MASK_VETO` (kept `NOT_EXECUTED` counterfactual) on intrabar-range tail, `StateQuality == DEGRADED`, or entry within the funding window; constants are declared, never fitted (no leakage) | PROVISIONAL_DECISION | CANDIDATE_LIFECYCLE_SPEC §6 item 3 is normative. A fitted percentile would leak future information; declared constants keep the mask zero-degree-of-freedom. `AGENT_RUNBOOK` Step 3. **Renamed 2026-08-04** (D-043 follow-up): the field was `max_spread_frac` and its veto detail `SPREAD`, but the predicate is `(high-low)/close` — the entry bar's intrabar range, never a bid-ask spread, which the depth-free tape cannot express. Pure rename: no threshold or decision changed, golden `candidate_count`/`terminal_distribution` unchanged, ledger hash re-pinned only because `config_hash` keys on field names. Whether 0.05 is a meaningful level is now O-019 — "declared, never fitted" answers the leakage objection but not the inertness one. |
| D-037 | Accept the stdlib mirror of `tools/data.py` contracts inside `tools/vision_backfill.py`; literal reuse is deferred until `data.py`'s heavy deps (polars/pandera/pyarrow/duckdb) are admitted with Phase-1 parquet materialization (DATASET_SPEC §5). `pyyaml>=6` joins the dev extra only (environment, not the decision path) | PROVISIONAL_DECISION | Resolves the Step-4 OPEN_PIN: `data.py` hard-exits at import without the forbidden deps (O-009 smallest-engine rule). The mirror implements the same documented contracts (kline column order, ms->ns conversion, checksum-file format), cited inline; produced rows round-trip `TapeRow(**r)` through `store.replay_tape`. |
| D-038 | Phase-1 tooling deps admitted as the `tooling` extra (polars/pyarrow/pandera[polars]/duckdb) for `tools/data.py` and `tools/materialize_views.py` (parquet materialization, DATASET_SPEC §5); the decision path (`src/v8/` minus `simtruth/`) stays stdlib-only; no test or contract depends on the new deps except pure planning/checksum helpers | PROVISIONAL_DECISION | D-037's deferral condition is met: Phase-1 materialization is now being built (session 2: real tape + DuckDB views, O-009 keeps the smallest engine on the decision side). Recorded at the admission's own OPEN_PIN; the operator registers it here. |
| D-039 | Declared dataset v0.1: BTCUSDT 1h, channels `kline` + declared-but-pending `funding`, dev window 2026-04..06 (hash `8b12707e…`), frozen OOS = first two published months after 2026-07-01 + 9-bar extension, full history/other symbols not declared, Tier-A/S not declared (O-010). Scale expectations table bound to measured numbers (DATASET_SPEC §6) | PROVISIONAL_DECISION | Closes the corpus gap: no contract previously declared what the dataset is or at what scale; "more data" is now a register decision, never a silent download. The funding channel is declared with ingestion pending — the preregistration §8 revision (0.0 -> tape-driven) is the next data-plane step, legal until the holdout is opened (§16). |
| D-040 | Retire the v0.1-only framing; the program target is the full 8-phase roadmap with the evidence gates unchanged (rules 5-6, 12, 14). Build priority completes Phases 0-4 first — the Phase-4 `v8_slice_001` experiment runner + D-027 verdict gating are the critical path — and Phases 5/7 are built only when their gate passes | PROVISIONAL_DECISION | Owner decision 2026-08-01. `docs/PLAN_V8_FULL.md` holds the sprint breakdown (planning artifact, not a monograph section); `tests/test_decision_path_purity.py` keeps enforcing gated-component absence. No economic claim is made or implied. |
| D-041 | Expand the declared dev dataset to 12 months (BTCUSDT 1h, `2025-07-01`..`2026-07-01`, ~8,760 bars) and ingest the `funding` channel into the PIT tape with tape-driven settlement wiring (`funding_settled_r = entry_price × rate / risk_unit`), including a declared funding coverage horizon of `2026-07` so end-of-dev positions settle across the 2026-07-01 boundary; holdout anchor unchanged | PROVISIONAL_DECISION | Owner decision 2026-08-01 (full-program D-040 series). O-017 thresholds 0.25/0.20 stay fixed — the 12-month baseline updates prereg §15 diagnostics only, never the ratified numbers. Legal pre-holdout per prereg §16 and D-039's scope note; prereg §8/§13/§15 revised; dev end stays strictly before the 2026-07-01 holdout. Funding rows never enter features or the decision loop. |
| D-042 | Admit `src/v8/experts/liquidity_sweep_reclaim.py` as the third pilot (`liquidity_sweep_reclaim` / `sweep_reclaim`, variant `a`; LONG after a windowed prior-low sweep reclaimed by the close, SHORT after a prior-high sweep reclaimed; `prior_low_ref` frozen at detection and excluded from `geometry_version`) and register the `breakout_retest` + `capitulation` backlog families as DATA_BLOCKED until derivatives tape | PROVISIONAL_DECISION | Owner decision 2026-08-01 (full-program D-040 series). D-032 file-family rule: a new `src/v8/experts/` module requires a register decision + CHANGELOG entry. Distinct mechanism/setup/invalidation from `failed_breakout` (EXPERT_PROTOCOL §1 separate-Expert test). Registry YAML + PILOTS + tests updated together; the dev materialization is re-pinned. |
| D-043 | Complexity budget splits into two axes (rule 14 rewritten): (a) runtime Expert count is **unbounded**, limited only by determinism and compute, and is not a validity constraint; (b) the preregistered cap applies to the `behavior_family` count simultaneously carrying a claim on one frozen-OOS evaluation, entering rule 11's family-level multiplicity correction. "At most one learned component" is scoped **per pipeline position**, not globally. Revises D-003 and D-020 | PROVISIONAL_DECISION | Owner decision 2026-08-04. The single number conflated an engineering question (how many modules evaluate per bar — zero validity content) with a statistical one (how many independent hypotheses are simultaneously under test). The cap was already breached: `EXPERTS_REGISTRY.yaml` carries 5 Experts after D-042. No code enforced the count, so this is a documentary correction — but it exposes two real couplings recorded as follow-ups: the binding constraints on portfolio scale are rule 16 (one exposure per instrument+direction) and D-023 (`max_heat = 3.0`), and contested exposure slots are resolved by `expert_id` lexicographic order. Order-independence itself is safe — `lab.run` sorts Experts by `expert_id` before evaluating, so the caller's list order is discarded and the ledger is stable — but the surviving tie-break is the Expert's *name*: measured on a contention fixture, the lexicographically first of two behaviorally identical Experts wins the slot roughly 2:1, and the advantage follows the name when the names are swapped (`tests/test_admission_contention.py`). Deterministic and harmless at three Experts; at the unbounded count this decision permits it is a silent name-based allocation policy. Not resolved here: a principled tie-break is a ranker, gated by rule 6 / D-008 (O-006 / O-012). The test fails if one is added, forcing that registry decision. Scorer and ML-Expert-challenger occupy different pipeline positions and are each already gated by rule 5, so the global "never both at once" added no statistical control. |
| D-044 | Preregistration §11's within-family multiplicity rule is corrected: "all variants explored inside a family count as one multiplicity unit" is **replaced** by a Reality-Check-style block-bootstrap max-statistic test (White 2000 Procedure RC, already `LITERATURE_SUPPORTED` in `HYPOTHESIS_LAB_PROTOCOL.md`'s Sources section) applied across every `variants_evaluated` entry of that family, spending the family's existing Bonferroni-corrected `α_f` budget. The cross-family Bonferroni (`α_f = 0.05/F` across preregistered families) is **unchanged** — still valid, only conservative under correlation, and not urgent at F=2 families with no variant search yet. `EXPERTS_REGISTRY.yaml` gains a required `variants_evaluated` field (losing variants included, not just the reported `variant_id`); `src/v8/statistics.py` implements the bootstrap (`IMPLEMENTATION_LAYOUT.md` updated, D-032). Cross-family pooling into one N-configuration test — the fuller instrument originally proposed — is **not** implemented: families fire on disjoint episode grids (different `episode_key` sets), and White's construction requires all N series aligned on a common index; correctly extending it needs a bar-level panel (zero-filling non-firing configurations), which is deferred to O-021 rather than built ad hoc | PROVISIONAL_DECISION | Owner decision 2026-08-06, pre-holdout (legal per prereg §16, same basis as D-041 — no field of a ratified preregistration may change after the frozen holdout opens, but nothing has been touched pre-holdout). Two defects motivated this, sharing one root cause: rule 13's ontology ("a variant is not a new Expert") was over-read in prereg §11 as a *statistical* claim ("variant search costs nothing"), which is false — best-of-N variant search inside one family reintroduces exactly the selection bias rule 11 exists to control (the canonical illustration is Aronson's 6,402-rule case, which collapses to 3 multiplicity units — three themes × a parameter grid — under a family-counts-as-one rule, understating the true search by orders of magnitude and turning a p=0.82 result into a false "significant" one under a 3-way Bonferroni). The fix must hold at the moment it starts mattering: Lim extraction is about to produce genuine within-family variant proposals for the first time, and the flaw is silent (nothing fails; it just admits a false positive at the point of highest expected variant volume). Not yet wired into a runner — `tools/run_experiment.py` (the `v8_slice_001` runner, Phase-4 build target per `IMPLEMENTATION_LAYOUT.md`) does not exist yet, so `statistics.py` is unit-tested on synthetic data only, not yet exercised against a real `LabReport`. || D-045 | The preregistration §2 null is re-centered: `μ_f ≤ 0` on RAW episode net_R is replaced by `μ_f ≤ 0` on net_R **detrended against a same-exposure passive benchmark** (Aronson Appendix A: `Corrected = Σ_long(R_i − E[Raw]) − Σ_short(R_i − E[Raw])`). Per episode the benchmark is `sign(direction) × entry_price × (exp(μ_log·h) − 1) / risk_unit`, with `μ_log` the mean per-bar log price change of the window being scored. `CounterfactualOutcome` gains `entry_price`, `risk_unit_price` and `market_move_r`; `simulator.hash()` becomes `canonical-sim-v6`. The raw mean stays in the report as a diagnostic beside the detrended one, and their difference is published as `position_bias_component`. Signal GENERATION is untouched — detrended values exist only in return calculation (Ch1 p27-28) | PROVISIONAL_DECISION | Owner decision 2026-08-06, pre-holdout (legal per prereg §16, same basis as D-041/D-044 — nothing has been touched pre-holdout; no manifest, store or outcome ledger exists). A no-skill rule has expected return exactly zero only on detrended data (Ch1 p23-27; Ch5 p235; Ch9 p394): on a trending tape a long-biased family earns positive expected net_R with zero predictive power, and all three pilots carry long-direction setups. Reproduced on a seeded uptrend (`tests/test_detrended_null.py`): zero-skill placebo families at 0%/60%/90%/100% long occupancy show raw mean net_R of −0.465/+0.122/+0.377/+0.465, of which detrending removes 77-93%, leaving a ~0.03 residual that is the Jensen gap between `exp(μ·h)` and `exp(Σ steps)`. The fields are recorded rather than re-derived downstream because `simulator.risk_unit` falls back to `entry_price × risk_frac` when a draft declares no `atr_ref`, so the R denominator is not recoverable from the candidate record alone, and `simulator.run`'s own docstring forbids a second copy of the formula in another module. `market_move_r` is recorded now, unsigned, although only METH-3 (permutation test, EV G-03) needs it — one hash bump rather than two. Measured against the pre-change code on the golden fixture: all 21 outcomes' `net_r`, plus `endpoint`, `label_status`, `data_hash`, `states_hash`, `candidate_count` and `terminal_distribution`, are UNCHANGED; only `ledger_hash` moved, which is the evidence this added evidence rather than changing a decision. NOT resolved here: the Appendix A "≈ 0" tolerance (`statistics.invariant_holds`) is deliberately unimplemented and raises — the threshold is itself a preregistered constant and is left to an explicit operator choice rather than a silently frozen default. |
| D-046 | `EXPERTS_REGISTRY.yaml` gains a required `search_universe_size`: the TOTAL number of configurations the search consumed to reach a family — parameter grids, discarded indicator variants, the direction-sign choice and losers that never earned a `variant_id` — of which D-044's `variants_evaluated` is only the retained subset and therefore a lower bound. `tests/test_expert_registry.py` enforces presence and `search_universe_size ≥ len(variants_evaluated)`; `tools/run_experiment.py` publishes both per family plus a `multiplicity_undercounted` flag when the declared search exceeds the variants whose series the Reality Check actually saw. All five registered pilots declare 1 | PROVISIONAL_DECISION | Owner decision 2026-08-06, pre-holdout. Aronson Ch8 p390-391 requires a complete record of every rule and parameter combination tested; a family p-value computed from the retained subset is optimistic by exactly the margin the search exceeded it. The canonical illustration is already recorded under D-044: 6,402 rules collapsing to 3 multiplicity units turns p=0.82 into a false pass. The declared 1 is not an omission — prereg §4 records that every detector parameter (EMA 5/20, ATR 14, the 32-bar prior-high window, expiry 8) was frozen in code against synthetic tapes BEFORE the real development window existed and no development search was performed, so there is no undisclosed configuration to count. The value becomes load-bearing at the moment the handbook-extraction families (24 proposed Experts, several with declared parameter grids) start registering; the flag exists so an undercount is visible in the record instead of silently inflating a family's significance. |
| D-047 | Position management and fill policies land as DECLARED, optional mechanics (EXEC-1..6; the handbook's EX-01..EX-12): `simulator.py` gains the breakeven roll + chandelier trail (`breakeven_roll_at_mfe_r`, `breakeven_margin_r` = `round_trip_cost_r`, `trail_stop_atr`, `OpenPosition.stop_level`/`stop_rolled` — endpoint stays STOP), the scale-out partial exit (`scale_out_ratio` > 0 enables, `scale_out_at_mfe_r`, `StepResult.closed_fraction` < 1.0 as a NON-TERMINAL event, `remaining`/`realized_r` fraction-weighted R accounting), the TIME_EXIT endpoint (`time_exit_bars`, distinct from EXPIRY), the FILL_AT_LIMIT fill policy (barrier entry at `risk_geometry['limit_price']`, fill-only entry-bar inspection, never-filling orders never enter), and the declared-but-P2 `pyramid_add_rules` key (fails closed on request; `midpoint_stop` primitive implemented + tested). `lifecycle.py` gains the append-only `PositionAction` event (PARTIAL_EXIT). `sim.hash()` → `canonical-sim-v8` | PROVISIONAL_DECISION | Owner decision 2026-08-06, pre-holdout. Every key is optional; the pilots' frozen geometry declares none, and on default geometry step()/run() output is byte-identical to pre-change code — verified by diffing the executed outcomes of both code versions on the golden fixture (all 12 executed outcomes' net_r/endpoint/label identical; data_hash/states_hash/candidate_count 21/terminal_distribution unchanged; only ledger_hash moved, because every outcome carries the re-versioned simulator_hash). Hash/endpoint discipline per CRIT-3/EXEC-6: any `simulator.py` semantic edit re-versions every outcome; the endpoint vocabulary is extended with TIME_EXIT and PARTIAL_EXIT is documented as a non-terminal lifecycle event, never an outcome endpoint (CANDIDATE_LIFECYCLE_SPEC §4 competing-risk decomposition untouched — a partial is not an exit cause). Thresholds are DECLARED numeric literals, never fitted (O-017 method; CRIT-9: `breakeven_roll_at_mfe_r = 1.5` and `f = stop_r/(stop_r+target_r)` are the book's, not dev-window fits). Not implemented here: O-013's admission gate (replicated OOS gain vs static geometry — a preregistration act), RM-04 two-tier heat (dormant hook in `risk.py`; requires a D-023 domain revision, CRIT-2.6), pyramiding and the EX-13 action lattice (P2). D-047 is the mechanics layer O-013's comparison runs on. |

| D-048 | Risk management additions land (RISK-1..6; the handbook's RM-01..RM-19): `CandidateDraft.size` (RM-01, CRIT-3 — sim.hash() re-versions to canonical-sim-v7 REGARDLESS of output byte-identity), `equity.RiskState` with the frozen `DRAWDOWN_BANDS` ladder (RM-06, O-016 challenger), `trade_units_for` (RM-07, 1% risk -> 100 units, 20% -> 5), manifest `risk_per_trade`/`min_trades` gates (RM-02/RM-08), heat as `size*stop_r` (RM-04, byte-identical at size=1.0), WCS + spread-adjusted R/r as report diagnostics (RM-10/11). All deterministic, preregistered thresholds never fitted | PROVISIONAL_DECISION | Handbook Ch28/Ch29; EV_METHODS G-02 calibration; measured against the pre-change golden fixture: all 21 outcomes' net_r element-wise identical, candidate_count/terminal_distribution/data_hash unchanged (sizing is size-independent in R) |
| D-049 | Feature-graph extension FG-1..FG-7 (G-01..G-43): `FEATURE_GROUPS` gains `candle_shape`/`oscillator`/`session`/`positioning`, activates `participation`, extends `volatility`/`location` (73 new features, verbatim formulas from the Handbook §0; no-signal conditions emit numeric sentinels, never None — the D-024 DEGRADED veto stays a data-integrity veto). `FEATURE_GRAPH_VERSION` re-computes and every state lineage_hash binds the new features | PROVISIONAL_DECISION | Handbook Ch6/8/9/10/12/13/14/21/26; CRIT-1 (Ch27.2 ATR range filter on swing significance); measured against pre-change: candidate_count/terminal_distribution/data_hash unchanged — feature evidence added, no decision changed |
| D-050 | The 24 handbook+evidence extraction expert families are admitted (EXP-01..24; D-042's pilot set extends to 28 code modules): each self-gates, carries frozen `variants_evaluated` (D-044) + honest `search_universe_size` (D-046); `breakout_retest` moves DATA_BLOCKED -> FORMALIZED (code now exists, D-049-of-E-11); `open_interest_divergence` stays DATA_BLOCKED (no OI on the current tape; code self-gates to NO_HABITAT); `capitulation` remains DATA_BLOCKED (no code) | PROVISIONAL_DECISION | Handbook E-01..E-24 cards; CRIT-4 (failed_breakout_2b variants b..g, variant 'a' reserved for the original pilot), CRIT-6 (divergence declared subset), CRIT-7 (no hidden pending state; regime gate is per-expert precondition, not a router) |
| D-051 | Retire the v2 research subsystems from version control: `research/pipeline_v2/` (v2.3 research-corpus pipeline — corpus, prompts, registry, rounds, workflows, schemas, legacy_v1, scratch) and `research_base/` (standalone `v8research` package with its own pyproject) are removed from the tree, along with `research/revision/` visual-preview artifacts and `research/text/` corpus copies; the three revision-monograph builders whose data sources they were (`tools/build_v8_revision.py`, `tools/generate_gemini_master_html.py`, `tools/generate_10k_gemini_master_html.py`) are removed with them. Superseded by the `src/v8/` runtime (Phases 0-4) as the single research codebase | PROVISIONAL_DECISION | Housekeeping 2026-08-06, pre-holdout: no prereg field, contract, or code path references the retired paths (verified by grep — the 615-test suite and monograph build are unaffected). Recoverable from git history (introduced in `a051a20`) should the v2.3 ledger be needed again |

| D-052 | Preregistration §9's block-size rule is corrected from bar-units to an n-adaptive episode-unit rate, and the block bootstrap gains a fail-closed `block_size < n` invariant. The rule's two-tier mechanical shape is unchanged (lag-1 autocorrelation gate at 0.10 selects the tier); only the tier VALUES change: `round(n**(1/3))` by default, doubled when the gate fires, hard-capped at `n // 2`. The prior constants 24 / 168 were bar-counts ("one day" / "one week" of 1h bars) applied to an episode-indexed `net_R` series — a unit error, visible in §9's own prose ("24 episode-blocks (one day)"). The resample count stops being a constant chosen independently of alpha: `resamples_for_alpha(alpha)` returns the smallest N with `int(N * alpha) >= 100`, floored at the preregistered 2000, so the one-sided bound is a stable order statistic at whatever family count is in force. The rule had been duplicated in `v8.statistics.select_block_size` and `tools/run_experiment._block_size`; the tool now delegates to the module, making one rule of record | PROVISIONAL_DECISION | Defect reproduced mechanically: when `block_size >= n` the circular sampler emits a cyclic rotation of all n indices, so every resample contains each element exactly once and `block_bootstrap_means` returns ONE unique value across all 2000 resamples — the bootstrap collapses to a point mass and `ci_lower == mu_hat` at zero width, so any family with a positive mean and `n >= MIN_EPISODES` rejects H0 by construction. On the pinned 2026-08-06 dev baseline 8 of 21 families with episodes had `block >= n`, and exactly one spurious rejection resulted (`obv_adl_regime`, n=50, block=168, `ci_lower == mu_hat == +0.0150`). Degeneracy is the endpoint of a bias, not an isolated case: at `block/n ≈ 0.3–0.5` (every family with n=45..100) the resample variance is already understated, biasing toward rejection. Outcome-neutrality of the choice is evidenced: the three candidate rules considered — (A) clamp-only `min(24|168, n//2)`, (B) the adopted `n**(1/3)` rate, (C) converting the bar target through observed median inter-episode spacing (16–197 bars, giving blocks of 1–5) — all yield ZERO H0 rejections on that baseline, so no rule could have been selected for the verdict it produces. (A) is rejected because it fixes only the pathological endpoint and leaves the anti-conservative bias; (C) is rejected as spacing-dependent, hence not mechanical. The resample-count defect was invisible at the two-family runner's own `alpha_f` (0.05/2 -> index 50) and appeared only under the slate-wide Bonferroni alpha, where the same 2000 put the bound at `int(2000 * 0.0017857) = 3` — the 4th-smallest of 2000 resample means estimating a 0.18th percentile; the alpha-tied rule yields 56000 (index 100) for the 28-family slate and 4000 for the two-family runner. Cost measured before adoption: 4.2 s per family at n=1000, 0.6 s at n=100. Verified on the pinned baseline (identical `ledger_hash` 452d91bcf890 before and after, so the comparison is uncontaminated by concurrent expert edits): degenerate families 8 -> 1 (the survivor is n=1, where a bootstrap has no variance by construction and the §12 gate already withholds a verdict), H0 rejections 1 -> 0, and every former zero-width row gained a real interval (`obv_adl_regime` +0.0150 -> -0.3067). Intervals widened across the slate (e.g. `liquidity_sweep_reclaim` -0.267 -> -0.420), confirming the old rule understated uncertainty rather than merely failing at the endpoint. 628/628 tests pass. Frozen pre-holdout; ratified before the frozen OOS opens, never after seeing holdout results |

| D-053 | The 2026-08-06 bug-fix pass's behavior changes are versioned per O-023's admission condition: `failed_breakout` stays **v1** (a bug-completion — the fixed episode series is a strict subset of the old, 369 -> 76, so v1 never contained the buggy gate as a deliberate configuration); `volume_climax_reversal` and `fib_projection_reversal` become **v2 challengers** (the fixed series ADDS episodes a v1 gate could not produce — climax-bar anchor 67 -> 380, origin-based extensions 2 -> 7 — so they are new behavior needing their own D-044/D-046 accounting and a fresh registry entry). v1's buggy behavior is archived by this decision, not silently replaced | PROVISIONAL_DECISION | The frozen OOS must run on the version the preregistration names (rule 15 / D-023: "versions evolve by challenger + frozen-OOS + registry decision, never in place"). v2 keeps the family's `variants_evaluated` and `search_universe_size` (the fix is a hypothesis-completion, not a threshold search — D-044/D-046 unchanged) but a NEW `expert_version` (v2), which enters the episode_key so v1 and v2 episodes never collide. Operator-chosen on 2026-08-06 (O-023, option "O-023 kriteri"). frozen pre-holdout; the prereg must name v2 for these families before the frozen OOS opens |
| D-054 | Experts declare the MarketState they need, and the runtime serves exactly that. Three additive parts: (a) `Expert.intervals` + `Expert.depth` join `requires` as frozen specification, audited against consumption like `requires` already is; (b) a new `src/v8/interval.py` aggregates the base-interval tape into any declared integer-multiple interval, and `MarketState` features gain an interval namespace (`{sym}.{tf}.{feature}`; the base interval stays unprefixed so every existing Expert and test is unaffected); (c) each Expert receives a MarketState containing exactly its declared groups x intervals x depth, while ONE canonical superset state per decision clock remains the persisted audit record — the per-Expert object is a projection of it, never an independently built state. A run-start feasibility gate validates every declared (interval, depth) against the tape and emits an explicit per-Expert `EVALUABLE` / `NOT_EVALUABLE(reason)` verdict | PROVISIONAL_DECISION | Motivated by the 2026-08-06 expert inventory: the binding constraint on 12 of 27 families is not the 1h tape but the global 32-bar `history` pin (O-020) — `ichimoku_cloud` needs 78 bars and has 3 of 4 variants declared-but-not-evaluated, `breakout_retest` variant d is unimplemented, `donchian_breakout` falls back from a 55-bar to a 50-bar anchor scan, and `pattern_measuring_objective` cannot express its patterns at all. Only `market_profile_value_area` demonstrably needs true higher-interval bar structure (it hand-rolls `SESSION_BARS = 24`); `floor_trader_pivot` is already served by the existing `pivot_points_day`. Keeping ONE persisted state per clock preserves the property that every Expert saw the same world at clock D — per-Expert persisted states would multiply the ledger 27x, break `ExpertEvaluation.state_id` as a shared audit anchor, and make a leakage defect in one Expert's state invisible. The feasibility gate closes the failure mode the same inventory exposed: `donchian_breakout` (384 triggers, 0 executions) was indistinguishable in the report from a family with no signal, and the six zero-episode families in fact split across three unrelated causes (absent data channel, depth block, exposure-slot contention). Interval aggregation invariants are fixed: buckets anchored to a fixed UTC epoch and never to tape start (otherwise dev and holdout windows produce different bars), aggregate `available_time` = the availability of the LAST constituent base bar, and a partial trailing bucket is never emitted as closed | 

| D-055 | `volume_climax_reversal` gains a strict-climax challenger variant (`e`, vol_zscore >= 3.0) alongside the book's 2-sigma a/b/c/d set, in response to the O-022 measurement that the 2-sigma gate fires on ~every bar (8,272 distinct candidates on 8,760 bars -> a 4.6% D-027 execution_share; the family floods the rule-16 exposure pool and blocks itself). Variant `e` owns every 3-sigma bar (LONG after a selling climax, SHORT after a buying climax) and joins `variants_evaluated` (a,b,c,d,e) with `search_universe_size` 5 | PROVISIONAL_DECISION | A hypothesis-level challenger, declared and frozen pre-holdout (D-036 discipline), NOT selected on the dev window: the within-family Reality-Check on the frozen OOS (D-044, prereg section 11) compares `e` against a/b/c/d on their aligned episode series, and only a surviving variant may tighten the family's effective gate. The threshold 3.0 is a declared constant (the 2-sigma 8,272-candidate flood is the measured defect it answers). 663/663 tests pass |

| D-056 | State builder gets an exact fast path: `build_state` accepts an optional per-symbol `BarSeries` (all series precomputed once per run) and reads them by index per decision clock, turning the bar-driven state pre-build from O(N²) to O(N × window). Unbounded `prior_high`/`prior_low` keep the EXACT running prefix max/min (never a fixed window — measured 83.5% divergence at 520 bars); growing-list lineage hashes keep exact `sha1_hex(list)` semantics via precomputed per-row canonical bytes; the raw manifest reuses the closed-list digest when kline == closed. Every emitted value, lineage, `lineage_hash` and `state_id` is byte-identical (cached == uncached on every bar, `tests/test_state_cache_identity.py`); the golden fixture diff shows candidates/evaluations/outcomes with 0 differing fields and states differing only in provenance `code_version` (whole-file hash — its designed behavior), re-pinned in the golden test | PROVISIONAL_DECISION | The measured wall on the 8-symbol x 4-year tape was O(N²) state building (est. 11-12 h at 35,064 bars/symbol vs 280 s at 8,760). Fixed: 8,760 bars 280 s -> 12 s; BTCUSDT 4y (35,064 bars, exact lab shape incl. funding) 67 s; 8-symbol projection ~9 min. The residual O(N) per state is the exact growing-list lineage hash — irreducible without changing `input_lineage_hash` semantics (rejected; the identity hashes must not move). 668/668 tests pass |
| D-057 | Entry trigger predicate is normative and enforced: a draft declaring `risk_geometry['trigger_ref']` (absolute, frozen at detection) enters only on the book's close-confirmation — `trigger_side` 'CLOSE_ABOVE'/'CLOSE_BELOW' (Ch14.2 "entry only on a CLOSE beyond the trigger"). `lab.py` PHASE 2 evaluates it before PENDING -> TRIGGERED; unconfirmed candidates stay PENDING until they fire, invalidate, or the epilogue expires them. `candlestick_reversal` is the pilot and declares `trigger_side`. Experts without a trigger keep `entry: NEXT_BAR_CLOSE` (unconditional) | PROVISIONAL_DECISION | Issue #62/#67. Pre-fix the trigger price was computed and discarded: 16/27 candlestick candidates triggered despite the close NOT confirming beyond the trigger. This restores the literature entry gate; the trigger-vs-unconditional A/B is measured in `.audit/` and the CHANGELOG. A ranker is NOT introduced (rule 6/14; D-008 unchanged) — this is the declared entry event, not a candidate ranking |
| D-058 | A frozen structural stop (`risk_geometry['stop_ref']`, absolute price) is the static stop when declared; `stop_r × unit` is the fallback. `stop_r` keeps its declared R-multiple meaning (heat = size × stop_r, D-023, unchanged); a stop-out at the structural level is the honest distance | PROVISIONAL_DECISION | Issue #63. Pre-fix the stop sat at `entry ± stop_r × ATR` even when the expert froze the swept extreme / pattern level (mean 0.44R divergence on 33/33 candlestick drafts; 37.3% of executions stopped by adverse excursion alone). Structural-stop A/B in `.audit/` |
| D-059 | The pre-entry invalidation fallback uses a WINDOWED extreme (the 32 bars before birth) instead of the all-bars `prior_high`/`prior_low` state features, which are UNBOUNDED prefix extremes and made the gate dead code for experts that freeze no ref | PROVISIONAL_DECISION | Issue #66. Measured 7 invalidation fires across 2,067 drafts for the 6 ref-less experts (the all-time extreme is pinned by an old spike). The 32-bar window matches the frozen-ref convention (failed_breakout / liquidity_sweep). Fail-closed on a birth with no prior bars is preserved |
| D-060 | Under exposure contention (one active exposure per instrument-direction, D-018), the surviving candidate is chosen by its episode_key hash order, not by `expert_id` alphabetical evaluation order | PROVISIONAL_DECISION | Issue #68. Pre-fix the alphabetically-first expert won 295/303 contended slots (97.4%) and the executed subset was 1.83× worse than the average setup. Candidate-hash order is deterministic, economically neutral, and explicitly NOT a ranker (rule 6/14, O-006/O-012 unchanged): the implicit name-based ranker is removed, not formalized |
| D-061 | The canonical simulator validates risk_geometry at `step()`/`run()` entry: non-positive `target_r`/`stop_r` and `expiry_bars < 1` fail closed | LOCKED_INVARIANT candidate | Issue #70. Pre-fix `target_r=-1` booked a −1.07R loss as a TARGET endpoint (a win in any hit-rate / profit-factor statistic) and `stop_r=0` was accepted. Defense-in-depth: the experts' own guards stay; the simulator is the last line so a future expert cannot silently pollute the ledger |
| D-062 | RM-11 feasibility gate is report-only: when the cost-degraded breakeven win rate (w_min) exceeds the realized win rate, the run carries a FEASIBILITY note. RR=1.0 `target_r` (19/27 experts) is a DESIGN_INFERENCE until a structural target (measured move / channel width / pivot) exists | PROVISIONAL_DECISION | Issue #64. Pre-fix the 7.6-point gap between breakeven (~53.5%) and realized (~45.3%) win rate was computed (RM-11) but never surfaced against the realized rate. Not a verdict change and not a geometry sweep (optimization is explicitly not the fix) |
| D-063 | `EXCESS_COST_THRESHOLD_R` firing is reported as a FEASIBILITY statement ("this geometry cannot trade at this cost"), and the default `round_trip_cost_r` derivation from the venue schema is recorded as an audit item | PROVISIONAL_DECISION | Issue #69. 0.10R ≈ 4.8 bps on the dev window < a realistic taker round trip (6 bps); at cost 0.125 the lab refuses to trade (0 executed, 6,209 excess_cost rejects). The gate works as designed; the defect was that the default cost sat just under it so it never fired |
| D-064 | `make_synthetic_tape` gains a `continuous` variant (open = prior close ± small move) for gap-sensitive diagnostics; the legacy default stays byte-identical for the pinned golden/contract tests, and flipping the default is deferred until the 30-file test blast radius is re-pinned in one scheduled pass | PROVISIONAL_DECISION | Issue #72. Pre-fix the synthetic tape fabricated TR > (H−L) gaps on ~73% of bars vs ~0.6% on real BTCUSDT 1h (open==prev_close 0% vs 51.7%). The continuous variant drops the fabricated-gap fraction to ~3.7%. Diagnostics convention: real tape for anything mechanical/economic |
| D-066 | The WHY-IS-IT-NEGATIVE diagnostic engine lives in `tools/diagnostic.py`, is READ-ONLY against the decision path (never writes a store/registry/authority file — a foreign write raises `DiagnosticWriteError`), and produces `AUTHORITY: NONE — DIAGNOSTIC ONLY` reports with a verdict enum, all counterfactuals on ONE fixed entry set | PROVISIONAL_DECISION | A diagnostic module must not move the decision-path code hash (D-032): a `src/v8/diagnostic.py` would invalidate every pinned manifest for a module that never affects decisions, so it sits in `tools/` (outside `_code_hash`). Spec: 9 sections + verdict. First real-tape run: MECHANICAL_FLOOR — the shipped signal is indistinguishable from random entries; a diagnostic finding, NOT an economic claim (authority stays NONE). 693/693 tests pass |
| D-065 | The locked universe stays Binance USD-M crypto perps; equities (NASDAQ, BIST) are a DEFERRED research axis, not a Phase-0..4 deliverable. The scope proposal (2026-08-07) is recorded as O-026 rather than a venue extension, because O-011's gate covers coverage failure on the locked universe, not a request for breadth — and equities differ structurally (no funding/mark/index/premium tapes, session calendar, corporate actions, different cost model). Data-plane exploration for equities MAY proceed in parallel as research-only (rule 11), but no equities tape may enter a canonical dataset, no `src/v8/` decision-path module may read it, and no preregistration may name it until O-026's admission conditions are met: a family surviving the Phase-4 base-case gate + cross-asset replication under its own multiplicity controls + rule 9 source authority | PROVISIONAL_DECISION | Owner decision 2026-08-07. Prevents scope creep before the first evidence gate: the program's own rules (rule 12) say the falsification program stops if neither family shows after-cost signal at Phase-4, and adding a second asset class before that base case is measured dilutes the evidence discipline without adding a surviving hypothesis. The data-authority constraint is real and binding now: NASDAQ's free archives lack Binance Vision's checksum-verified immutability (rule 9), and BIST requires a commercial provider or an authority-inadmissible scraper. Reversible — if O-026's experiment passes, equities are admitted by a fresh registry decision, never by configuration |

| D-067 | The simulator gains an OPTIONAL bps-of-notional cost form alongside the flat R charge: `CanonicalSimulator(round_trip_cost_bps=X)` and `RunManifest.round_trip_cost_bps`, resolved at ONE point (`CanonicalSimulator.cost_r(entry_price, unit)`) that every `net_r` site now calls instead of reading the scalar. `cost_R = (bps/1e4) * entry_price / risk_unit`. `None` keeps the flat form, and the flat path is byte-identical (verified by diffing every outcome's `net_r`/`endpoint`/`entry_price`/`risk_unit_price` plus the whole `LabReport` minus hashes, before vs after — identical). The cost FORM binds into `sim.hash()` ('flat' vs 'bps:X'), so a flat ledger and a bps ledger can never compare equal | PROVISIONAL_DECISION | `round_trip_cost_r` is denominated in R, therefore invariant to the R unit: widening the risk unit rescales stop and target but leaves the charge untouched. The standing "widen R to dilute the cost" reasoning — true of a real venue fee — is a no-op in the flat model, so an R-widening experiment could not be measured at all. A venue charges a fraction of notional; only the bps form makes the R unit and the cost move together. This registers the MEASUREMENT INSTRUMENT only: no geometry changed, no expert changed, and the default remains flat 0.07R. 733/733 tests pass |
| D-068 | The diagnostic's per-expert exit grid becomes a target x horizon CROSS (`EXIT_TP_GRID` x `EXIT_EXPIRY_GRID` = 20 cells, plus `no_sl` and `trail_1atr` as structural probes outside the cross). The selected maximum is reported as `exit_cross` carrying `n_cells_searched`, the naive p and a Bonferroni-corrected p — never as a bare "best exit". The portfolio section reports BOTH the per-expert max (labelled a selection-inflated upper bound) and a single fixed cell chosen once for all experts | PROVISIONAL_DECISION | The prior grid was one-dimensional: `tp_4r` moved the target to 4R while pinning the shipped 8-bar expiry. The report's own horizon section shows mean favorable excursion reaches ~4R near 48 bars, so that cell measured "target converted to expiry" and its loss said nothing about whether a 1:4 geometry works — the exit diagnosis was unfalsifiable. Katz & McCormick's Standard Exit Strategy (Encyclopedia of Trading Strategies, 2000, ch. 13) moves target and horizon together (1 ATR stop / 4 ATR target / 10 bar limit); the grid must be able to express that shape. LITERATURE_SUPPORTED for the grid's SHAPE only — no geometry is adopted, and any adoption is a preregistered challenger (rule 12) |
| D-069 | The decision table is evidence-gated and non-prescriptive. (a) `_observations` replaces `_main_problem`: every entry carries `supported: bool`, computed from `_min_n_for(nets, effect)` — the same 95%-CI sample floor section 8 already applies to segment cells — and an UNSUPPORTED observation is printed but may never label a row or drive a verdict. (b) The prescriptive action column ("Disable long", "Add regime filter", "Rework TP/SL", "Rebuild the expert", "Optimize execution") is replaced by an observation column stating the measured quantity and its sample support; a regression test asserts those strings never return. (c) A new coverage section reports zero-setup experts and defined-but-unregistered variant classes. (d) `KEEP` — the strongest statement the table can make — additionally requires `n >= _min_n_for(nets, zero_cost_edge)`: enough trades to resolve the edge being claimed. Under-powered candidates are held at `INVESTIGATE` ("not enough yet"), never demoted to a failure | PROVISIONAL_DECISION | Three self-contradictions in the shipped report. First, section 8 refused to score a segment cell below its min-N while the decision table simultaneously emitted "Add regime filter" from an n=19 cell. Second, the action column named the best corner of a 44-cell (expert x symbol x timeframe) search — the selection is what inflates it, so a diagnostic that prescribes it is doing the data mining it exists to detect (Aronson, Evidence-Based Technical Analysis 2007, ch. 6). Third, 48 Expert subclasses are defined but only 27 registered, and 5 registered experts produced zero setups; both gaps were invisible, so a row labelled with a family name read as a statement about the family while measuring one variant — a long-only variant `a` shows SHORT n=0, which looks like a broken direction and is the variant's definition (Aronson's rising-market long-only artifact, ~ch. 5). AUTHORITY unchanged: NONE |
| D-070 | `run_multi` is deterministic in `--processes`: the (symbol x timeframe) job list is built once by a pure `plan_cells`, and each cell's seed is derived from its POSITION in that fixed enumeration | PROVISIONAL_DECISION | Defect found while wiring the cost form: the parallel branch seeded `seed + i` while the sequential branch used a bare `seed`, so `--processes 1` and `--processes 4` produced different output for every cell but the first. Determinism is the one property this program cannot trade away (constitution: no wall clock in replay, the three clocks never collapsed); a runner whose results depend on a scheduling flag silently breaks it |
| D-071 | The V8 x Recoverable Regret v0.2 Phase-0 measurement instrument is admitted as `tools/regret.py` (+ `tools/regret_reference.py`), a READ-ONLY consumer of a completed `Lab` store — it never re-runs `Lab.run`, never creates a second Candidate population, and touches no Expert/geometry/verdict. Pipeline: CandidateSnapshot join (re-derives `episode_key`, never a stored edge) -> PIT lineage assertion -> ledger reconciliation (`Replay(C, a_actual, M) == observed`) -> `LegalActionManifest` (NO_TRADE + the actual action seeded first + a small declared target_r x expiry_bars grid, `pyramid_add_rules` and `direction` structurally excluded) -> `ModelDerivedOutcomeCube` (`cube.jsonl`, refuses rather than accepts a degenerate-future/censored/no-entry cell) -> `LegalHindsightGap` (`regret.jsonl`, ties reported never broken, abstains on any non-OK potentially-maximizing cell). Two small additive `src/v8/` repairs were required to unblock it: `AppendOnlyLog.close()` (Windows `os.replace` cannot rename over a still-open handle — this was also the pre-existing `tests/test_funding_wiring.py` failure, previously misdiagnosed as an environment artifact) and `.as_posix()` path keys in `_code_hash`/`_tooling_hash` (the identical source hashed differently on Windows vs POSIX). `Lab.run` additionally persists `report.json` alongside `manifest.json` (additive only, not part of `ledger_hash`) so a completed store is self-describing about its own `risk_gate_hash` without re-running the lab. Phase 0 computes NO statistics — no slicing, no multiplicity, no attribution, no recoverability; it produces a reconciliation verdict, a cube, and a per-Candidate gap, nothing else, and every number is labelled MODEL_DERIVED with no economic authority | PROVISIONAL_DECISION | Full ACCP evidence chain: RIR-V8RR-001 -> FER-V8RR-002 -> FDR-V8RR-003 -> FCR-V8RR-004 (`reports/accp/v8-rr-v02-phase0/`). Certification evidence, all real command output: golden synthetic fixture (15 candidates) reconciles 12/12 exact at 1e-12; a real 12-month single-symbol BTCUSDT 1h store (1,532 candidates, built from a freshly downloaded, checksum-verified 6-symbol multi-instrument tape — `research/tape/multi-1h-dev/`, 144 archives, 59,130 rows, 0 duplicates, monotonic, 0 venue-sequence gaps) reconciles 754/754 exact at 1e-12 with 0 deviation on every field — closing the FCR's own flagged "measured only on synthetic data" gap; PIT lineage clean (0 violations) on both; the v0.2 invariant `hindsight >= actual` holds with zero negative gaps across 543 COMPUTED candidates (12 synthetic + 531 real); an independently-derived reference walk (written from `SIMULATION_TRUTH_SPEC` text, imports nothing from `v8.simulator`) agrees with the canonical simulator on 150 Hypothesis-generated randomized paths; five fault-injection cases (TP-shortened axis attribution, cost-doubling isolation, direction-flip structural illegality, habitat-randomization structural non-claim, missing-evidence explicit refusal) behave as specified. 751/751 tests pass (18 new: `test_regret_phase0.py`, `test_regret_faults.py`, `test_regret_reference.py`), up from 730/733 at HEAD before this session's three repairs. Lives in `tools/` (D-032 boundary: a research instrument, not decision-path runtime — mirrors `tools/diagnostics.py`'s write discipline and population-boundary caveat, since the two engines' Candidate populations are NOT interchangeable) |

| D-072 | Phase 1 (Candidate-local opportunity accounting, `tools/regret_phase1.py`) and Phase 2 (systematicity discovery, `tools/regret_phase2.py`) of the V8 x Recoverable Regret v0.2 program are run over the full certified multi-symbol dev dataset (9,218 Candidates: 6 symbols x 3 pilot Experts x 12-month trimmed dev window, D-071's tape). Phase 1 is a pure join + pre-declared descriptive breakdown (label `MODEL_DERIVED_DESCRIPTIVE_NOT_YET_GATED`, zero statistics). Phase 2 implements the frozen contract `FCR-V8RR-007` by REUSING `src/v8/statistics.py` in full (`select_block_size`, `bootstrap_ci`, `effective_independent_episodes`, `practical_significance`, `expected_false_positives`/`effective_search_size`) — zero new estimator code. The declared 72-slice discovery family (3 Experts x 6 symbols x 2 directions x 2 estimands) is scored on a chronological discovery half of the dev window (first ~6 months, 4,609 Candidates) and any `CANDIDATE_SYSTEMATIC` slice is queried against the untouched confirmation half (remaining ~6 months, 4,609 Candidates) EXACTLY ONCE | PROVISIONAL_DECISION | 12 of 72 declared slices (16.7%, vs `expected_false_positives(72, 0.05)`=3.6 under the null) reached `CANDIDATE_SYSTEMATIC` on the `mean_legal_hindsight_gap` estimand — none on `mean_actual_vs_no_trade` — and ALL 12 replicated as `SYSTEMATIC_FINDING` on confirmation (0 `FAILED_CONFIRMATION`), with tightly stable point estimates and CIs across both halves (e.g. `trend_pullback\|BTCUSDT\|LONG`: discovery mean 0.677R CI[0.593,0.760], confirmation mean 0.499R CI[0.402,0.594]; `failed_breakout\|BTCUSDT\|SHORT`: discovery mean 1.052R CI[0.965,1.136], confirmation mean 1.005R CI[0.923,1.088]). The pattern is clean and directionally coherent: `trend_pullback` clears the Bonferroni-corrected bar (`alpha_slate` ~0.000694) on its LONG side on all 6 symbols and never on SHORT; `failed_breakout` the mirror on SHORT; `liquidity_sweep_reclaim` clears no slice (mostly `INSUFFICIENT_SUPPORT` — fewer executed episodes per symbol). **This is a `mean_legal_hindsight_gap` finding, NOT a recoverability claim** (v0.2 section 5.3: hindsight opportunity != recoverable opportunity) — it says value was left on the table inside the represented Candidate/action universe under a costed, versioned Replay Model, and that the pattern replicates; it says nothing about whether a decision-time-realizable policy could have selected the better legal action. Phase 3 (`FCR` to be frozen next) is the only instrument that can answer that. Full evidence: `reports/accp/v8-rr-v02-phase0/source/ECR-V8RR-008.accp.yaml` (Phase-1 capsule), `tmp/phase2/{attempts.jsonl,discovery_summary.json,confirmation_results.jsonl,confirmation_summary.json}` (real command output, this session) |

| D-073 | Phase 3 (recoverability evaluation, `tools/regret_phase3.py`, contract `FCR-V8RR-009`) tests all 12 of D-072's confirmed `mean_legal_hindsight_gap` findings against a small, declared, decision-time policy class: `ALWAYS_TRADE` (the deployed policy, included for the `V_A <= V_R` containment requirement) plus 24 `THRESHOLD_GATE` policies (3 declared birth-state features x 2 directions x 4 discovery-half quantiles) that select `NO_TRADE` over the actual action when a birth-time feature crosses a threshold. Selection happens on the discovery half only; `V_A`/`V_R`/`G_R` are estimated on the SAME untouched confirmation half Phase 2 already reserved, with a bootstrap CI (reused `v8.statistics.bootstrap_ci`/`select_block_size`, zero new estimator code) and the same materiality floor (0.05R) as Phase 2 | PROVISIONAL_DECISION | 11 of 12 slices reach `RECOVERABLE_WITHIN_CLASS` (`ci_lower(G_R) > 0` AND `mean(G_R) >= 0.05R`); only `trend_pullback\|SOLUSDT\|LONG` fails (`ci_lower` -0.005, essentially the null boundary — an honest negative, not force-fit). **Stated with maximum precision because this is exactly where overclaiming is easiest: `V_R` (the gated policy''s mean utility) stays NEGATIVE on every single one of the 11 recoverable slices** (e.g. `failed_breakout\|XRPUSDT\|SHORT`: `V_A`=-0.387R, `V_R`=-0.062R, `G_R`=+0.325R CI[0.179,0.468]). This is a real, replicated LOSS-REDUCTION effect from a decision-time-selectable subset of candidates, not a profitability finding — no slice''s gated policy clears zero, so `rule 12`''s block on any profitability/validated-execution claim is untouched. **Second limitation, stated rather than hidden:** the selected feature/direction/threshold is NOT uniform across symbols within an Expert — `trend_pullback` selects `bb_pct_b` (3 symbols, both directions) or `rsi14` (2 symbols) with no consistent sign; `failed_breakout` is more coherent (`bb_pct_b NO_TRADE_BELOW` at threshold ~0.729-0.743 on 4 of 6 symbols independently), but even there BTCUSDT selects the opposite direction (`NO_TRADE_ABOVE` at 0.112) and SOLUSDT selects `adx14` instead. Twelve independently-selected single-feature gates finding no common rule is evidence AGAINST a single clean mechanism and FOR either genuine cross-symbol heterogeneity or discovery-half overfitting that confirmation''s bootstrap CI does not fully protect against (the CI protects the MEAN estimate, not the feature-selection step itself, which is a real limitation of this minimal-first-pass policy class per v0.2 section 6.1''s own caution about the sup over Pi). Classified `NOT_RECOVERABLE_WITHIN_CLASS` for `trend_pullback\|SOLUSDT\|LONG` only (a SCOPED result, this class/this environment — never an absolute unpredictability claim). No representation-gap escalation (a richer phi) is attempted this pass (`FCR-V8RR-009` FT005, deliberately deferred). Full evidence: `tmp/phase3/{phase3_summary.json,recoverability_attempts.jsonl}` (real command output, this session; 300+12 attempt-ledger rows) |
| D-074 | Phase 4 (controlled single-component intervention) targets exactly ONE of D-073''s 11 recoverable slices: `failed_breakout\|SHORT`, because it is the ONE pattern with cross-symbol coherence (4 of 6 symbols independently select `bb_pct_b NO_TRADE_BELOW ~0.73`) rather than the heterogeneous `trend_pullback` pattern D-073 flags as a possible overfit. A NEW registered variant `failed_breakout` **v2** (never mutating the shipped v1 in place, rule 15/D-023) adds ONE declared, frozen pre-entry filter: skip the Candidate (no Candidate emitted) when `bb_pct_b < 0.70` at the setup bar (a round, pre-declared threshold — not the exact per-symbol fitted value, to avoid re-fitting 4 separate thresholds into one challenger). Every other line of the Expert (setup, trigger, invalidation, geometry) is byte-identical to v1 | PROVISIONAL_DECISION | See CHANGELOG for the full preregistered intervention contract (target/challenger/expected-direction/minimum-effect/rejection-criterion/rollback) and the measured result on the certified pipeline |

| D-075 | Diagnostic hot-path pass, output-identical. (a) `_median_atr` is memoised — it is a pure function of the frozen draft set but was recomputed, scanning and SORTING every draft, once per null draft. (b) The walk cache stores `(draft, result)` and verifies identity on hit, and null drafts bypass it entirely: `id()` is unique only among LIVE objects, so a null draft freed right after its walk lets CPython reuse the address, and a (recycled id, same entry_idx, same geometry) key could return a walk taken in the OPPOSITE DIRECTION. (c) `simulator._evolve` replaces `dataclasses.replace` on the `OpenPosition` hot path. (d) `_post_exit_max` is skipped for null walks, which never read it. (e) The tape is parsed once per PROCESS instead of once per cell | PROVISIONAL_DECISION | Measured, not guessed: cProfile on a 540-bar BTCUSDT cell put `_median_atr` at 54.3s of 72.6s (75%), 202,000 calls and 287M dict lookups; then `dataclasses.replace` at 20.4s of the remaining 28.0s (73%), 3.9M calls under 57M getattrs. Single cell 72.6s -> 17.4s (4.2x). Full `--symbols BTCUSDT,ETHUSDT --timeframes 1h,4h --span-days 60 --processes 4`: 102.5s -> 29.2s wall, 245.4s -> 71.2s CPU (3.4x) on the SAME feature set. Output identity is the gate and it holds twice over: the lab economics dump (every outcome's net_r/endpoint/entry_price/risk_unit_price plus the whole LabReport minus hashes) is byte-identical to the pre-change baseline, and the diagnostic's own aggregate net_R_mean and all 22 decision-table rows are identical before vs after. (b) is a CORRECTNESS fix that happened to surface during profiling, not a speed trade. No numpy, no new dependency, no change to the concurrency model — those remain unregistered (D-031 baseline). 749/749 tests pass |
| D-076 | Admit `fib_rsi_bb_confluence` as an exploratory dev-window family (mechanism `confluence_reversion_continuation`, behavior `fib_rsi_bb_confluence`): two variants evaluated in one run — `a` STRICT (all three legs same direction), `b` MAJORITY (at least two of three). Legs reuse registered idioms verbatim (Bollinger 2-SD fade zone, Wilder-RSI dip-and-recover, fib reclaim at 0.786 — the deep ratio is the only standard level that can geometrically co-occur with a fade-zone close, computed pre-window, frozen). 1R:1R:8bar + `atr_ref`; frozen 78.6% level and frozen 3-SD band as invalidation refs (`prior_*_ref` + `lower/upper_3sd_ref`, D-042 pattern). `variants_evaluated` [a, b]; `search_universe_size` 2 (thresholds inherited, declared pre-window) | PROVISIONAL_DECISION | Owner decision 2026-08-10, pre-holdout (prereg §16). D-032 file-family rule: a new `src/v8/experts/` module needs a register decision + CHANGELOG entry. Exploratory dev-window SOLUSDT 1h run (2025-07..2026-06, 8760 bars), tape-driven funding, 10-bps taker cost, zero-cost reference; verdict stays NO_ECONOMIC_CLAIM (no authority receipt, rule 12). Measured result: STRICT fired once (0 executed); MAJORITY fired 159, executed 33, mean net_R -0.253 after cost (90% CI [-0.471, -0.062], entirely negative; -0.158 even at zero cost). The confluence does not beat its own relaxation and neither clears cost. D-026 hardening: `lab._geometry_version` now excludes `lower/upper_3sd_ref` (data-dependent) from episode identity — without it dedup could not fire (179 candidates carried 179 distinct geometry hashes) |
| D-077 | **V8.2 = high-throughput compute substrate + Recoverable Regret evaluation plane.** The runtime splits into a Python control/analysis plane (experiment specs, manifests, preregistration, verdict statistics on reduced tables, reports, diagnostics) and a Rust compute plane owning dataset → features → state → candidates → lifecycle → replay → cube → reduction → cache → ledger, one process and one memory model. `src/v8/` is FROZEN as the parity oracle for the duration of the migration — behaviour-frozen, repaired only for genuine wrong-value bugs, retired to an archived reference under its pinned code hash when V8.2 certifies, never deleted. Migration is staged S0..S5 with a parity gate at every stage. Version semantics are fixed here: V8.0 is the deterministic Expert architecture and canonical simulator; V8.2 is a substrate + measurement release that leaves the decision ontology untouched; a V9 requires an ontology break (learned proposal architecture as baseline, a different Candidate ontology, the system ceasing to be Candidate-constrained, router/scorer entering the policy, or a different epistemic architecture). **Moving to Rust or adding a GPU backend is an implementation change and is explicitly NOT a V9** | PROVISIONAL_DECISION | Contracts: `COMPUTE_CORE_SPEC`, `PARITY_AND_IDENTITY_SPEC`, `LEDGER_FORMAT_SPEC`, `OUTCOME_CUBE_SPEC`, `PREDICATE_IR_SPEC`, `COMPUTE_SCHEDULING_SPEC`, `RECOVERABLE_REGRET_PROTOCOL`; `ARCHITECTURE_SPEC` §3.1 revises the D-031 single-language baseline without retiring it. **Explicitly not justified by necessity:** `PERFORMANCE_AUDIT_V82` §10 records that research scale (100 symbols x 15m) is reachable inside tens of minutes in Python once the defect class in D-083 is removed, so the program's falsification clause on evaluation cost is NOT firing. The decision rests on representation ownership, single-runtime streaming (D-081), and evaluator capability — not on a cost claim. Stating this prevents a motivated reading of the program's own criterion |
| D-078 | **No-callback invariant.** Once an evaluation request enters the compute plane, control does not return to Python until it completes: no Python callback, no Python object, no Python-owned buffer reachable from inside. Any Expert-supplied hook the replay path must consult is expressible in the predicate IR (D-082) or it does not enter the replay path | PROVISIONAL_DECISION | The one V8.0 violation is the post-entry thesis closure passed to `sim.run(..., thesis_valid=...)` by `tools/regret.py`. Measured surface (AST extraction over `src/v8/experts/*.py`): 28 implementations, 567 lines, mean 20, over an eleven-name live-feature vocabulary; 19 of 28 read only `close` against a frozen `risk_geometry` reference, 26 of 28 read only scalars, 2 (`donchian_breakout`, `ichimoku_cloud`) read `history`. A closed surface of this size is compilable, so the invariant costs a bounded one-off and not an open-ended bridge. `COMPUTE_CORE_SPEC` §3 |
| D-079 | **Parity is value-level; identity is bit-encoded.** Two implementations agree for a stage when every emitted value is bit-identical field by field (IEEE-754 bit patterns for floats — `-0.0` distinct from `0.0`, NaN payloads significant; tolerance comparison prohibited anywhere in the parity path). Hashes are excluded from the parity comparison. The V8.2 canonical hash is computed over a representation-independent byte encoding (f64 → 8 IEEE bytes LE, integers → fixed-width two's complement, strings → length-prefixed UTF-8, composites → tag + count + canonical order), digest stays SHA-1. **V8.2 hashes deliberately do not equal V8.0 hashes for identical values**; V8.0 artifacts keep V8.0 hashes and are never re-hashed, and cross-version comparison is done on values via the parity harness, never on identity strings | PROVISIONAL_DECISION | Measured: CPython's `json` encoder and Rust's default `f64` `Display` disagree on **7 of 8** representative values (`1.0`→`1`, `1e+16`→`10000000000000000`, `1e-05`→`0.00001`, `-0.0`→`-0`, `1e+22`, `1.2345678901234568e+17`→`123456789012345680`, `1e-323`→323 decimals; only `0.30000000000000004` matched), CPython 3.14.0 vs rustc 1.96.0-nightly (`PERFORMANCE_AUDIT_V82` §8). Both renderings are shortest-roundtrip correct and differ only in exponent thresholds, so decimal text cannot be the basis of a cross-runtime identity. The rejected alternative — reimplementing CPython `float_repr` in Rust to preserve hash continuity — buys comparability of hash strings across a boundary where values are already compared directly. Gates G1-G6 in `PARITY_AND_IDENTITY_SPEC` §5; G6 (fail-closed behaviour agrees) is not optional |
| D-080 | **Ledger tiering and columnar format.** Every persisted field is classified identity / information / schema / run-constant / derivable; persist identity and information, hoist schema and run-constants into an artifact header, recompute derivables. Three tiers: `IDENTITY_ONLY`, `VALUES` (default for research runs), `FULL` (certification and promotion boundaries). Storage is column-major with dictionary-encoded low-cardinality strings, explicit validity bits, and no decimal text for any number. The header carries `hash_encoding` so a V8.2 artifact can never be read as a V8.0 one | PROVISIONAL_DECISION | Measured: one 8,760-bar single-symbol three-expert run writes **271.8 MB** of `states.jsonl` from a **3.5 MB** input tape (78x). One state record is 31,091 B for 74 features, of which the 74 float values are **~590 B (2%)**; 33% is repeated JSON key names, 15% is a `history` window duplicating tape OHLC into 32 consecutive records, 10% per-feature lineage hashes, 9% duplicated feature names, 9% two timestamps, 6% constant enums (`PERFORMANCE_AUDIT_V82` §7). `OutcomeCubeRow` carries 35 fields of which ~11 are run-constant. **Cost stated rather than hidden:** at `VALUES` tier, forensics on an old run requires checking out the pinned code and replaying the archived tape rather than reading a file; therefore (a) a tape referenced by any retained artifact is itself retained, and (b) any run that will carry a claim is written at `FULL` tier before the claim is made |
| D-081 | **The Outcome Cube is a computation, not necessarily a stored table.** At `VALUES` tier the cube is reduced in flight into per-Candidate accumulators (maximum over OK cells, actual-action value, NO_TRADE value, cell counts by status) and only reduced tables are persisted; at `FULL` tier it is materialized columnar. The reduction must be order-independent and exact — absorbing a Candidate's actions in any order yields identical accumulators — because a scheduling-dependent value would violate backend invariance (D-079 G5). Ties are reported, never broken; the reduction abstains whenever a non-`OK` cell could have been the maximizer | PROVISIONAL_DECISION | Materializing every cell costs ~9.9M rows/symbol at the research target, ~600 GB across 100 symbols as JSONL (`LEDGER_FORMAT_SPEC` §7), and is then read back to compute a handful of accumulators per Candidate. Streaming is also what keeps verdict statistics in the analysis plane: after reduction the plane crossing is order 10^4-10^6 numbers rather than 10^7 rows, so block bootstrap and multiplicity control remain in Python at negligible cost and remain transparent (`COMPUTE_CORE_SPEC` §7). Equivalence gate: streaming and full materialization produce identical reduced tables on the same fixture |
| D-082 | **The post-entry thesis is compiled, not called.** `still_valid` is expressed as a predicate IR (ordered dispatch on geometry-key presence; comparison of a live feature against a frozen `risk_geometry` reference; `FLIP_ON_SHORT` orientation; `MAX`/`MIN` window aggregates for the two `history` cases) and evaluated natively. Fail-open on any absent operand is normative — a missing reference yields "thesis still valid" and price governs the exit, exactly as V8.0 does. An Expert whose thesis cannot be compiled fails CLOSED at compilation time with the offending construct named; it is never silently treated as fail-open and never quietly replaced by a callback. Extending the IR is a registered decision; adding a callback is excluded by D-078 | PROVISIONAL_DECISION | Surface measured by AST (see D-078). The compiled predicate carries a `predicate_version` in the Candidate identity, so a thesis change re-versions every cell that consulted it. Acceptance gate E1-E5 in `PREDICATE_IR_SPEC` §6; **E4 is load-bearing** — outcome bit-identity on the real V8.0 candidate population, because E1-E3 test the predicate in isolation while only E4 proves the counterfactual and executed populations are evaluated under the same exit policy |
| D-083 | **Representation rule: a layer receives a borrowed view of data it does not own.** Copying a window out of an owned buffer is a defect unless the copy is the output. Concretely: a replay cell is passed `(&Dataset, start, end)` and may read no bar outside `[start, min(end, start + expiry_bars + 1)]`; whole-tape indices are built once per dataset, never per clock; cumulative digests advance incrementally; hot-path allocations are pooled or hoisted | PROVISIONAL_DECISION | The rule generalizes **one measured defect class found at three independent sites**, not three incidents. (a) `marketstate.build_state` rebuilds `{id(b): i for ...}` over the whole tape per state per symbol per interval — 77,925,592 `id()` calls in a profiled 8,760-bar run, **4.93 s measured in isolation**, i.e. ~63% of the 7.74 s state layer. (b) `tools/regret.py` slices the entire remaining tape per cube cell: **71.8 µs vs 6.4 µs** of arithmetic it feeds (~92% waste; 21.6 s vs 1.6 s per symbol-year). (c) `AppendOnlyLog.hash` re-serializes the whole parsed ledger — 5 calls, 5.5 s profiled. The scaling curve confirms the exponent drift: doubling bars costs 2.12x → 2.10x → **2.49x** (1,095 → 8,760), exponent ~1.16 overall and ~1.32 on the last doubling (`PERFORMANCE_AUDIT_V82` §§3-7). **The rule is language-independent:** a Rust implementation that clones windows reproduces the defect; ownership makes the correct form natural, it does not confer it |
| D-084 | **Modular kernels; GPU gated on a declared trigger.** Work is expressed as separate kernels K1 (feature series) .. K6 (resampling) with engine-owned buffers between them; a fused kernel spanning features → experts → replay → regret is a REJECTED_OPTION (divergence, register pressure, impractical CPU fallback, one Expert edit recompiling everything, loss of per-stage attribution). Backend and thread count are internal details and appear in no hash and no manifest. Parallelism order: task parallelism across independent units first, SIMD within a kernel second, GPU only past the trigger. **Trigger: a routine request exceeding ~10^9 replay cells** | PROVISIONAL_DECISION | Determinism analysis is per-kernel and corrects an earlier blanket framing: K1/K2 contain floating-point reductions whose reassociation changes results, so their reduction order is declared and fixed and a backend that cannot honour it is ineligible; **K4 is a per-thread sequential walk with no cross-worker reduction and no transcendental**, so bit-identical cross-backend results ARE achievable given FMA contraction off, fast-math off, f64 throughout, and declared NaN normalization. Cell arithmetic from the measured 6.4 µs: the presently scoped research target (100 symbols, 15m, ~10 interventions) is ~10^7 cells per request and does **not** trigger; only an Expert-variant sweep would. Writing kernels in a backend-portable form to defer the decision is legitimate **on one condition — that the portable form's CPU output is competitive with a directly-written CPU kernel, which is an assumption to be measured before adoption, not a premise** (`COMPUTE_SCHEDULING_SPEC` §6) |
| D-085 | Admit `src/v8/fast.py` as a decision-path module: content-addressed caches for repeatable evaluations — `CompleteRunCache` (whole-run ledger restore under a key binding tape, manifest, expert identity, code, tooling and risk-admission policy), `StateMaterializationCache` (canonical MarketState objects, keyed independently of Expert source bytes), `ExpertEvaluationCache` (one Expert's evaluation stream). A hit restores immutable ledgers byte-for-byte via same-filesystem hardlink and never fabricates a report from a partial run; the canonical Lab remains the authority and performs a full replay on a miss | PROVISIONAL_DECISION | D-032 file-family rule: a new `src/v8/` module requires a register decision plus a CHANGELOG entry, and this module was present in the tree untracked. Covered by `tests/test_fast_cache.py`. It is the seed of the V8.2 content-addressed DAG cache, whose missing node is cube level — `(candidate_id, action_id, simulator_hash, data_hash)` → outcome — which is what makes an Expert edit invalidate only that Expert's cells instead of the whole run |

| D-086 | **Variant sweeps are admitted, under anytime-valid error control** (`SWEEP_PROTOCOL`). A sweep evaluates a registered grid of variants of one behaviour family; each variant is one arm and one hypothesis, evidence accumulates as an **e-process**, compute is allocated by successive halving over a growing chronological slice, and the final rejection set is produced by **e-BH on the stopped e-processes**. Requirements S1-S8: grid declared and registered before any evaluation and never widened mid-campaign; e-process construction declared; exploration rule free but recorded; MinBTL pre-check refuses an over-large grid; the confirmation half stays untouched and is queried exactly once (unchanged from D-072); every survivor reported with Deflated Sharpe and PBO as diagnostics; alpha spent across campaigns under an online procedure. Default null is the family's existing registered variant, not zero — a sweep must beat what it proposes to replace | PROVISIONAL_DECISION | **The reason this is now admissible is that one object removes both obstacles at once.** Multiplicity: e-BH controls FDR under **arbitrary dependence** between hypotheses (Wang & Ramdas, arXiv:2009.02824), which matters because sweep variants all read the same tape and plainly violate BH's independence/PRDS assumptions. Adaptivity: FDR control holds for **arbitrary exploration rules and arbitrary stopping times** with e-processes (Xu, Wang & Ramdas, *A unified framework for bandit multiple testing*, NeurIPS 2021), so early elimination — normally selection plus peeking, and therefore invalidating — becomes licensed. Previously the statistically safe sweep was the computationally impossible one and vice versa. **Trap recorded before it bites** (`SWEEP_PROTOCOL` §4): stopping several e-processes at a COMMON stopping time yields e-values only under a shared global filtration, and e-processes are normally built locally per hypothesis (arXiv:2502.08539). Every V8 variant reads the same tape, so the streams are dependent by construction and the naive "halve them all at a shared stopping time" design is exactly the unsafe case; the default resolution is variant-LOCAL stopping times, and using a global one requires an argument in the campaign contract rather than an assertion. **Hard pre-condition:** the Minimum Backtest Length result (Bailey, Borwein, López de Prado & Zhu) — on five years of daily data no more than ~45 variations can be tried before a Sharpe of 1.0 arises by chance — so the admissible trial count is computed from tape length BEFORE the grid is registered, and an over-large grid is refused rather than discounted. **Compute consequence:** successive halving takes the sweep from `O(\|V\| x full-tape)` to ~`O(\|V\| x short-slice + log\|V\| x full)`, one to two orders of magnitude for a 1,000-variant grid, composing with the D-085 DAG cache since extending a survivor's window is incremental. This lands a nominally large sweep back below the ~10^9-cell GPU trigger (D-084), so admitting sweeps does **not** force Experts native from stage S1 and does **not** reopen the GPU question — the statistically correct design is also the cheap one. Blocked on O-032 (the e-process construction for block-dependent episode streams is not settled; no campaign may run until it is declared and tested) |

| D-087 | **V8.2 compute core begins its staged migration with S0: the parity harness and the Dataset layer.** A new `v8-core/` Rust workspace (one binary, modules — `IMPLEMENTATION_LAYOUT` §1.1) implements: (a) the V8.2 canonical hash encoding (`hash.rs` — bit encoding of D-079, digest SHA-1); (b) `Dataset` tape ingest (`data.rs`) mirroring `_validate_tape_rows` verbatim, dedup by (source, event_id) like the store inbox, canonical replay order, per-symbol columnar closed klines; (c) the columnar artifact container (`evidence.rs` — LEDGER_FORMAT_SPEC §3-4: magic `V82LDRG1`, JSON header with `hash_encoding: v8.2-ieee-le`, validity bitmask, fixed-width IEEE-754/two's-complement values, dictionary-encoded strings, no wall clock → two identical requests write byte-identical artifacts); (d) a Python-`json`-compatible tape parser (`jsonx.rs`) because CPython `json.dumps` emits `NaN`/`Infinity` as bare literals that strict JSON rejects and the oracle classifies a NaN OHLC as "non-finite" — not as a parse error; (e) the Python-driven parity harness (`tests/parity/` + `tools/v82_reader.py`) per PARITY_AND_IDENTITY_SPEC §5.2. FMA contraction is disabled crate-wide (`--fp-contract=off`) because CPython does not fuse multiply-add (G5). Crates: `serde`, `serde_json`, `sha1` — no Python runtime, no FFI, no embedded interpreter (D-078) | PROVISIONAL_DECISION | D-032 file-family rule: the new Rust workspace and every new module are recorded here and in the CHANGELOG rather than silently added. **The gate is correctness, not speed:** S0 gate evidence in `reports/parity/S0.md` — tape round-trips and the three clocks survive on every row. 23 Rust unit tests + 16/16 S0 parity tests pass: synthetic (multiple seeds, continuous, degenerate 1-bar/empty), real verified tape (btcusdt-1h-12m full 9,948 rows; a 25,000-row multi-symbol slice of multi-1h-4y), two runs byte-identical (G4), threads=1 vs 8 byte-identical (G5), seven fail-closed classifications matching the oracle (G6). Oracle tree hash pinned at `184fb934…` (any `src/v8/` change invalidates every recorded parity result). No speed claim is made — PERFORMANCE_AUDIT_V82 figures remain the comparison baseline. The G6 error-message comparison normalizes float *rendering* (the one documented runtime divergence, AUDIT §8) while requiring the classification category and row identity to match exactly |

| D-088 | **V8.2 compute core S1: FeatureStore + StateView passes the parity gate.** `v8-core/src/state.rs` ports `build_bar_series` → `FeatureStore` and the cached feature block of `build_state` → `StateView` (pinned byte-identical to the uncached reference by `tests/test_state_cache_identity.py`); all 77 declared features with both warmup representations and the positioning-channel absence rule; V8.2 identities via the D-079 bit encoding. Three CPython 3.14 portability facts are now LOCKED implementation rules for every further migration stage: (a) `sum()` over floats is compensated summation (`_PyFloat_Fsum` = `math.fsum`), NOT a left fold — a fold drifts by ulps on ~20-element windows (measured); `state::fsum` is a verbatim port incl. the special final fold (largest partial first, stop at the first inexact addition) and the half-even tie fix. (b) `x ** 2` is libm `pow(x, 2.0)`, which differs from `x*x` by 1 ulp on some values; LLVM folds `pow(x, 2.0) → x*x` in release, so the exponent is `black_box`'d to force the libm call (COMPUTE_SCHEDULING_SPEC §5: an optimization may not change a value). (c) `x ** 0.5` is libm `pow(x, 0.5)`, which differs from `sqrt(x)` by 1 ulp on some values — `_std_pop` must finish with `powf(0.5)`, not `.sqrt()`. Two usize underflows (`i - period + 1`) fixed to `i + 1 - period` (panic in debug, silent wrap in release) | PROVISIONAL_DECISION | D-032 file-family rule recorded. Gate evidence `reports/parity/S1.md`: 24 Rust unit tests (incl. an fsum battery captured from CPython 3.14) pass; 9/9 S1 parity tests pass — vocabulary match (77 names), synthetic golden/continuous/funding-channel, real verified tapes (btcusdt-1h-12m full 8,760 bars; multi-1h-4y slice), two runs byte-identical (G4), threads=1 vs 8 byte-identical (G5), and the identity mutation property (one OHLC digit on bar 60 changes exactly the consuming states' `state_id`). Hashes are excluded from the parity comparison (D-079 §3); parity is values. No speed claim — the gate is correctness. Multi-interval `build_multi_state` aggregation is not yet in the S1 gate (deferred with the interval port) |

| D-089 | **V8.2 compute core S2: the Predicate IR + ReplayKernel pass the parity gate.** `v8-core/src/experts/predicate.rs` evaluates the compiled post-entry thesis IR (`compare` with FLIP_ON_SHORT, `asym_compare`, `all_of`/`any_of`, ordered `dispatch` incl. geometry-value equality, `guard` for whole-condition fail-open; operands live/live_window/ref/ref_dir/window_agg INCLUSIVE|EXCLUSIVE/window_agg_dir/mean_of2/const). `v8-core/src/simulator.rs` is the `ReplayKernel`, a byte-for-byte port of `sim.run`/`_exit_loop`/`step` (R-multiples, risk_unit, validate_geometry, FILL_AT_BAR_CLOSE/LIMIT, funding scalar+schedule, STOP_FIRST ambiguity, gap-through stops, THESIS_INVALIDATED/TIME_EXIT/EXPIRY, EXEC-1..6, the single net_r formula), reading `&bars[start..end]` bounded to `expiry_bars+1` (OUTCOME_CUBE_SPEC §5) and evaluating the compiled predicate from the feature store — no Python callback (D-078). `tools/predicate_ir.py` compiles all 28 registered still_valid methods into the IR. Two determinism facts are LOCKED: (a) **serde_json's default float parser is not correctly rounded** — `"0.9632136759338213"` parses 1 ulp low (measured), so request-side floats silently drift; `float_roundtrip` is enabled (the tape itself was never affected — jsonx uses std's correctly-rounded parse). (b) **Fail-open is not uniform**: per-operand (dominant close-vs-ref form) vs whole-condition (trend_pullback_depth, rsi_stoch_reversion variant b, bollinger_reversion's close pre-check, gap_exhaustion's either-ref) — the `guard` node captures the difference; and fib_rsi_bb_confluence's prior_low_ref valid-form is GTE (the equality boundary holds), while the 3sd rule is GT | PROVISIONAL_DECISION | D-032 file-family rule recorded (new Rust modules + tools/predicate_ir.py registered). Gate evidence `reports/parity/S2.md`: cargo test 24 passed; 6/6 S2 parity tests — E1/E2/E3 (738-point grid over all 28 experts, present/absent/None per operand, both directions, ref==live boundary), E5 vocabulary closed (11 names + parameterized windows), E4 replay parity on the V8.0 candidate population (endpoint/label_status/horizon/ambiguous exact; net_r/mae_r/mfe_r/entry/unit/market_move bit-identical), G4 byte-identical, G5 thread invariance, G6 fail-closed. Known bounds: the donchian responsive/significant_extreme exit kinds are compiled in the IR table but not exercised by the E1 grid (module-local subclasses); FILL_AT_LIMIT is ported but unexercised (no registered Expert declares limit_price). No speed claim |

| D-090 | **V8.2 compute core S3: the CubeReducer + streaming regret pass the parity gate.** `v8-core/src/regret.rs` implements the `LegalActionManifest` (NO_TRADE at element 0, the ACTUAL action at element 1 by construction, the declared grid `target_r (1,2,3) x expiry_bars (8,24,48)` de-duplicated by action id, `pyramid_add_rules` excluded → cardinality 2), the cell-status classifier (OK/CENSORED/UNDEFINED_FUTURE/NOT_EVALUABLE_ACTION/NO_ENTRY, `MIN_FUTURE_BARS = 1`), and `compute_gap` (best over OK cells incl. NO_TRADE, `GAP_TIE_EPS = 1e-12` ties reported never broken, abstain on CENSORED / no-OK / actual-cell-not-OK — with `actual_utility` None in the actual-cell-not-OK branch exactly as the Python record does). Action and manifest identities are V8.2 bit-encoded (D-079). The `cube` subcommand streams one Candidate at a time: manifest → replay each cell via the S2 ReplayKernel → classify → reduce in memory → emit the `cube-reduced` artifact; cells are never materialized across Candidates (D-081) | PROVISIONAL_DECISION | D-032 file-family rule recorded (new Rust module registered). Gate evidence `reports/parity/S3.md`: cargo test 24 passed; 5/5 S3 parity tests — reduced tables match `tools/regret.py`'s Phase-0 output on every BOUND Candidate (gap_status/actual_utility/best_utility/tie_cardinality/legal_hindsight_gap/abstention_reason exact), gap >= 0 on every COMPUTED Candidate, manifest structure (a_actual element 1), G4 byte-identical, G5 thread invariance, G6 near-tape-end refusal (UNDEFINED_FUTURE) agrees. Identity strings excluded from the value comparison (D-079 §3). No speed claim |

| D-091 | **V8.2 plane split revised: the analysis, verdict and audit planes join the Rust plane; Python is reduced to the frozen oracle.** D-077's two-plane split (Python control/analysis vs Rust compute) is revised at the analysis side: the boundary "Rust computes, Python analyses" (`COMPUTE_CORE_SPEC` §7) is removed. The regret analysis pipeline (`RECOVERABLE_REGRET_PROTOCOL` phases 1-3: candidate-local opportunity accounting, systematicity discovery, recoverability), the verdict statistics (`statistics.py`: block-bootstrap Reality-Check, detrended null, placebo family), the D-086 sweep e-process statistics, and the ledger/report audit checks move into `v8-core` as native stages, evaluated in-process from the columnar ledgers and reduced tables the compute plane itself writes. The artifact file remains the **persistence** boundary (reproducibility, `LEDGER_FORMAT_SPEC` §4 tiers) but is no longer a language crossing: no Python code reads a compute-plane artifact for a verdict. D-078's no-callback invariant extends to "no Python in the request path"; D-084 kernel modularity is untouched. Python retains exactly three roles: (a) the frozen parity oracle `src/v8/` — required by the parity program itself, never deleted (D-077); (b) the vendored `simtruth/` reference lab (authority not renewed, D-022); (c) pre-V8.2 dev/research tooling with no decision-path role (downloads, backfill, handbook extraction, equity analysis, monograph build, the parity test driver) — retired as its Rust equivalent lands. Migration order S0..S5 extends with **S6 (analysis plane)** and **S7 (verdict statistics + report/audit artifacts)**, each with a value-level parity gate against the frozen Python evaluators (`PARITY_AND_IDENTITY_SPEC` §5). Revises D-077's §7 rationale and supersedes D-081's "verdict statistics stay in Python" consequence — the streaming reduction of D-081 still holds, only its Python-side consequence is revoked. Not a V9: the decision ontology, Candidate semantics and epistemic architecture are unchanged (D-077 version semantics) | PROVISIONAL_DECISION | Owner decision 2026-08-12 ("everything in Rust"; the Rust→Python artifact crossing is the cost being removed — analysis should read the compute plane's ledgers natively rather than across a language boundary). Measured surface to be enumerated by the S6/S7 scan: `tools/regret.py` 34 KB + `regret_phase1/2/3.py` ≈ 27 KB + `regret_reference.py` 4.3 KB (parity oracle), `statistics.py` 767 lines, `diagnostics.py` 171 KB (report centre). The decision-path analysis surface is bounded and closed — the same character as the predicate surface that justified D-078/D-082 — so the port costs a bounded one-off, not an open-ended bridge. S6/S7 gates follow the S4/S5 pattern; no speed claim is made, the gate is correctness and ownership |

| D-092 | **V8.2 compute core S4: `CandidateBuffer` + `ExpertPlane` pass the candidate-population parity gate.** All 28 registered Experts are ported to `v8-core/src/experts/*.rs` (issues #77-100), closing the pilot-only 4/28 state D-089 shipped with. Two new modules complete the plane, neither named in `COMPUTE_CORE_SPEC` §6's original module table: `features.rs` (D-053 feature-group projection — an Expert's `FeatMap` carries only its declared `requires`-closure, mirroring `marketstate.py`'s `group_closure`/`project_state`; this is what makes `obv_adl_regime` correctly emit NO_HABITAT when `atr`/`history` fall outside its closure) and `runloop.rs` (the `evaluate` subcommand — the S4 composition point: per bar x per symbol x the full expert dispatch table, D-053-projected FeatMap in, CANDIDATE draft out, D-026 episode-key dedup, into the CandidateBuffer). Issue #101 (four ported pilot `evaluate()` functions hardcoded `"SOLUSDT"` instead of reading the request's symbol, breaking parity on any other tape) is fixed. The Wave-2 exit-kind coverage card (#103) compiled the donchian `responsive`/`significant_extreme` exit kinds — module-local subclasses the S2 E1 grid could not exercise because they are not exported by the registry — and caught two genuine latent S2 predicate-IR bugs in `predicate.rs`, both fixed: an EXCLUSIVE `window_agg` off-by-one (the aggregate must run over `n - 1` bars, since `n` counts the dropped newest bar; 48/202 grid mismatches) and EXCLUSIVE fail-open on short history not matching Python's `len(hist.value) < m + 1` (24/202) | PROVISIONAL_DECISION | D-032 file-family rule: `features.rs` and `runloop.rs` recorded here and in the CHANGELOG; `IMPLEMENTATION_LAYOUT` §1.1/§4 tracks their absence from the original `COMPUTE_CORE_SPEC` §6 table as a documented divergence, not a silent addition. Gate evidence `reports/parity/S4.md`: cargo test 149 passed 0 failed; 5/5 S4 population-gate tests — oracle tree hash pinned, all 3,360 (120 bars x 28 experts) evaluations bit-identical including the 420 CANDIDATE drafts' direction/birth_time/risk_geometry/setup_fingerprint/setup_anchor_event_id, the 209 DETECTED episode-key population and 211 suppressed-duplicate set identical in count and key inputs on both sides, and the loop's summary counters (`n_candidates=2, n_rejected=207, n_suppressed=211`) reconcile to the lab's `candidate_count=209`. **Known, accepted divergence, not a parity target of this gate:** admission timing — the loop admits at DETECTION (never releases exposure slots; no position lifecycle yet), the lab admits at TRIGGER, so the PENDING/REJECTED split differs (2 admitted/207 rejected at detection vs. the lab's 209 reaching PENDING with 134 rejected at trigger); the DETECTED population and the suppression set — this gate's actual subject — are identical. No speed claim |

| D-093 | **V8.2 compute core S5: `EvidenceStore` + the content-addressed DAG cache pass the ledger-identity gate.** `evidence.rs` (issues #108/#109) gains `ArtifactTier` (`IDENTITY_ONLY < VALUES < FULL`) with the tier-honesty rule — `Artifact::add_field` rejects any field whose tier exceeds the artifact's tier as an explicit `TierViolation`, never a silently downgraded or empty column — `RunConstants` (the §3 key set bound into every header), and the `LEDGER_FORMAT_SPEC` §8 six-test battery (`v8-core ledger-check`). `cache.rs` (issue #107) adds the missing cube-level DAG node: `sha1(candidate_id\|action_id\|simulator_hash\|data_hash) -> outcome`, an in-memory map backed by an append-only `cache.jsonl` (insert appends to the log first, so a failed write leaves the map untouched — fail closed). `v8-core cache-check` proves a miss (compute+store+write) and a hit (fresh `CacheStore` over the same log — what a second process would do) write byte-identical artifacts with identical fingerprints | PROVISIONAL_DECISION | D-032 file-family rule recorded (`cache.rs` was already named in `COMPUTE_CORE_SPEC` §6; no divergence). Gate evidence `reports/parity/S5.md`: 15 evidence.rs + 4 cache.rs unit tests; `ledger-check` 6/6 LEDGER_FORMAT_SPEC §8 tests PASS (round-trip, header-completeness, byte-stability, tier-honesty, no-decimal-floats, retention); `cache-check` PASS (computed and cached fingerprints both `9cc58e1f4062205c5da2d0e272a5d45772cd1fbe`); `tests/parity/test_parity_s5.py` 3 passed. No oracle is involved — the §8 battery's value-level semantics are pinned by the unit tests, and the harness runs the shipped binary the way the audit tool would be invoked. **Known limitation, still open:** the cache is not wired into `runloop.rs`'s cube reduction (`write_cube_reduced` calls `regret.rs` directly and never consults `CacheStore`) — wiring the loop to the cache remains a follow-up, not part of this gate or of S4 as landed. No speed claim |

| D-094 | **V8.2 compute core S6: the `Analysis` composition (regret phases 1-3) passes the bit-identical parity gate.** `analysis/{mod,outcome,phase1,phase2,phase3,reconcile}.rs` — six files, not the single `analysis.rs` `COMPUTE_CORE_SPEC` §6 named — implement the `analysis` subcommand: `reconcile.rs` joins each DETECTED candidate transition to its immutable birth draft and replays the ACTUAL action's cube accumulators under the compiled S2 predicate IR; `phase1.rs` is the candidate-local opportunity join; `phase2.rs` is the 72-slice discovery/confirmation family; `phase3.rs` is recoverability. The gate closed in two commits after an initial wiring pass (`5accfc6`) landed the composition running but not bit-identical (939 phase-output divergences against the oracle, `reports/parity/S6.md` as first written). `647c0b0` fixed the dominant cause: `build_snapshots` joined drafts to DETECTED transitions by re-derived `candidate_id` hashes, but V8.2 identities are the D-079 canonical encoding and deliberately differ from the oracle's `sha1_hex(json)` — so on lab-produced store data the join matched nothing (all `UNBOUND_NO_DRAFT`, 508 phase-1 cube fields `None`). The join now binds by the D-026 identity tuple `(expert_id, expert_version, instrument, direction, setup_anchor_event_id)`, encoding-agnostic; reconciliation went from 0/62 to 56/62. `3e13697` closed the remaining two root causes: (a) **clock-bound draft binding** — the all-28 fixture has 45 anchor-tuples with multiple DETECTED candidates (different `geometry_version` as the market moves), which the tuple join alone merged onto the first draft; binding additionally by the DETECTED transition's own `knowledge_time` (= the birth bar, since the lab evaluates each expert once per bar) selects the exact draft the registry kept, closing the last 6 reconciliation mismatches; (b) **`cost_r`/`funding_r` missing from the compute `Outcome`** — the kernel already computes `net_r = realized - cost - funding` but did not expose the two components, so the Phase-1 join carried them as `None`; `Outcome` gained both fields (additive, the 10-field reconciliation surface unchanged), closing 124 Phase-1 divergences | PROVISIONAL_DECISION | D-032 file-family rule: the `analysis/` directory split from the spec's single `analysis.rs` is recorded in `IMPLEMENTATION_LAYOUT` §1.1/§4 as a tracked divergence. Gate evidence `reports/parity/S6.md` (as landed) + reproduced live: `tests/parity/test_parity_s6.py` 1 passed (17s) — reconciliation 62/62 and all three phase outputs (431 opportunity rows, the 72-slice discovery family, the empty-on-both-sides confirmation/Phase-3 surfaces on this fixture) bit-identical to `tools/regret_phase1/2/3.py` on the all-28-expert 200-bar fixture, per PARITY_AND_IDENTITY_SPEC §3 semantics (V8.2-encoded identity strings excluded from the value comparison; floats compared by IEEE-754 bit pattern). cargo test 149 passed. Issue #117 CLOSED. No economic claim, no speed claim |

| D-095 | **V8.2 compute core S7: `Verdict` statistics + report/audit artifacts pass the parity gate.** `statistics/{mod,reality_check,detrended,remaining}.rs` — four files, not the single `statistics.rs` `COMPUTE_CORE_SPEC` §6 named — implement the `verdict` subcommand (issue #128 wiring): block-bootstrap Reality-Check (`reality_check.rs`), the Appendix A detrended-null placebo invariant (`detrended.rs`, issue #124), and the METH-2..6 surface (`remaining.rs`, issue #129: permutation Reality-Check, bootstrap CI, effective independent episodes, regime slices, streak-vs-null, practical significance, expected false positives, effective search size). A new module not named in the original table, `mt19937.rs` (issue #127), reproduces CPython 3.14's Mersenne Twister bit-exactly — `random.Random(int)` seeding via `init_by_array`, `random()` via `genrand_res53` (NOT `getrandbits(53)/2**53`, empirically different bits), and `randrange`/`getrandbits`/`sample` matching `random.py`'s pool-branch Fisher-Yates with no `k==n` short-circuit — because the frozen oracle `statistics.py` draws every bootstrap/permutation resample from `random.Random(seed)` and bit-exact verdict parity requires bit-exact draws. `report.rs` (issue #126) is extended additively: `verdict_path` folds the verdict subcommand's JSON into the report summary (the report's own top-level claim stays `NO_ECONOMIC_CLAIM`, rule 12 — a p-value is not an authority receipt), and `audit_report` audits an existing artifact against the current tape/producer, the path by which the freshness check flags a stale artifact (issue #123) | PROVISIONAL_DECISION | D-032 file-family rule: the `statistics/` directory split and `mt19937.rs` recorded in `IMPLEMENTATION_LAYOUT` §1.1/§4. Gate evidence `reports/parity/S7.md`: cargo test 149 passed (statistics suite 45 passed); `tests/parity/test_parity_s7.py` 3 passed — every value field of the verdict JSON on a fixed episode series + seed bit-identical to `statistics.py` (WRC `observed_max=0.08125, p_value=0.0145, block_size=4`; Appendix A placebo means + `holds`; METH-3 permutation; bootstrap CI; effective independent episodes; regime-slice means; streak p-value; practical-significance note; expected false positives; effective search size), the §9 mechanical block-size rule auto-selecting the same block (4) on both sides, the report round-trip, and the stale-artifact audit correctly flagging `freshness: FAIL` on a tape swap while non-freshness checks still pass. Rule 12 held: verdict and report claims both stay `NO_ECONOMIC_CLAIM`. No economic claim, no speed claim |
| D-096 | **Kernel backend strategy: scalar reference + CPU/SIMD first, GPU gated behind the ~10^9-cell trigger, CubeCL preferred over wgpu if a portable layer is ever adopted.** `v8-core` is single-path scalar today — the S0-S7 gates (D-087..D-095) are correctness gates; `threads` is recorded in the request manifest and tested for invariance (G5), but no task-parallel or SIMD execution exists in the tree (no `std::thread`/rayon/spawn, no `target_feature`, anywhere in `v8-core/src/`). The implementation order is declared: Backend-0 scalar deterministic Rust as the in-core reference — the frozen Python `src/v8/` remains the parity oracle (D-087); Backend-0 is a within-core reference, not a second truth — then Backend-1 CPU + task parallelism + SIMD, and GPU only past the trigger (`COMPUTE_SCHEDULING_SPEC` §6, D-084). If a portable GPU layer is ever adopted it is **CubeCL over wgpu**: CubeCL compiles one kernel IR to CUDA/HIP/Metal/Vulkan/CPU and matches the K1-K6 kernel structure and the "backend is an implementation detail, never kernel semantics" rule (D-084), while wgpu sits at the buffer/bind-group/pipeline/dispatch level — direct wgpu use means writing a mini compute framework and owning the CUDA/HIP/Metal/Vulkan differences ourselves. No CubeCL/wgpu dependency is added now: CubeCL is alpha with breaking changes between minor versions, and O-029's precondition — the portable layer's CPU output measured competitive against a hand-written CPU kernel — precedes any adoption. K4 (replay/cube, no cross-worker reduction) is the only GPU candidate; K1/K2 reduction-order sensitivity (D-084) keeps them CPU-only | PROVISIONAL_DECISION | The f64 contract closes the GPU path before the trigger opens it. K4 requires f64 throughout + FMA contraction off + fast-math off, and GPU fp64 is a wall on every portable route: Metal exposes no fp64 in shaders; Vulkan exposes `SHADER_F64` (native only) but fp64 runs 16-64x slower than f32 (wgpu feature docs); consumer CUDA/HIP (RTX 40/50, RX 7000) execute fp64 at 1/16-1/32 rate. On Apple, CubeCL's Metal/Vulkan paths run through wgpu, so the limitation applies either way. Consequences: (a) ~10^9 cells is a necessary but not sufficient condition — f64-on-GPU economics are doubtful on all consumer backends; (b) the only workload projected to reach the trigger, the Expert-variant sweep, was already brought below it by D-086's successive halving; (c) the backend-neutral interface boundary that keeps the ontology independent of the backend is cheap and worth having when Backend-1 lands, but the GPU backend itself is unlikely to ever be built. D-032 file-family rule: `backend/` and `scheduler.rs`, when added, are recorded here and in the CHANGELOG; no such module exists in the tree today |
| D-097 | **The Rust compute plane is the authoritative implementation; the Python parity oracle is retired from the verification path (operator-ratified 2026-08-14).** `v8-core` (the Rust plane, D-087..D-095) is the sole authoritative implementation. `src/v8` — the frozen Python oracle (D-087) — is retired as the verification reference: it remains in the tree as a historical/legacy corpus but does not run in CI, in any workflow, or in any gate. Verification is Rust-native: `cargo test --release` per change (v8-core unit tests incl. the LEDGER_FORMAT_SPEC §8 battery and COMPUTE_SCHEDULING_SPEC §9 cheap tests), G4/G5 determinism (two runs byte-identical; threads=1 vs N byte-identical), and golden vectors pinned from the S0-S7-verified outputs as the release gate. The Python test suite (`pytest tests`, 864 tests incl. the 63 parity tests) is no longer a gate; `tests/parity/` and the oracle-tree-hash pin mechanism (PARITY_AND_IDENTITY_SPEC §7.5) are superseded by golden-vector hashes | PROVISIONAL_DECISION | The retirement is only coherent because the differential verification it retires already completed: every S0-S7 gate (D-087..D-095, reports/parity/S0-S7.md) closed with the Rust plane byte-identical to a second, independently-implemented reference — that record is the provenance that licenses trusting Rust golden vectors going forward. **Risk, stated honestly — self-verification circularity:** Rust now verifies Rust; the independent reference is gone. Mitigation: golden vectors pin outputs that were independently cross-checked at S0-S7; a golden-hash change must be tied to a documented committed code change (no silent drift); the historical differential record stays in the tree as the trust anchor. **Scope boundary, stated honestly:** the rule covers the verification/execution path — no Python runs in CI, workflows, or gates. Repository tooling outside that path (the monograph build `tools/build_monograph.py`, tape builders, the vendored `simtruth/` V7 lab whose authority is already NOT renewed) is unchanged for now; porting it is follow-up, not part of this decision. Rules 9 and 12 are unchanged: authority receipts now bind to Rust artifacts and their golden-vector hashes; the economic verdict remains NO_ECONOMIC_CLAIM. Defect fixes (issues #61-72 family) land in `v8-core` only; `src/v8` is not edited further. CI: the two pytest steps in `.github/workflows/ci.yml` are removed; cargo + forbidden-name scan + monograph byte-identity probe remain |

| D-098 | **Optional Linux Vulkan f64 K4 backend is implemented behind the backend boundary.** `v8-core/src/backend/gpu.rs` provides a real `wgpu` Vulkan compute path for the static bar-close replay subset; it requires `SHADER_F64`, runs a no-contraction probe before adoption, validates geometry, and exposes CPU fallback for unavailable/unsupported devices. `backend/mod.rs` owns dispatch: `Auto` uses a runtime step threshold plus capability/fill-policy gates, explicit `Gpu` fails closed, and unsupported thesis/trigger/limit/management/pyramiding cells never enter the GPU kernel. Non-Linux builds report unavailable rather than pretending to dispatch. This supersedes D-096's implementation-status sentence (“backend itself is unlikely to ever be built”) without changing D-084's backend-invariance or f64 requirements. | PROVISIONAL_DECISION | Local evidence: default/debug/release builds, GPU-feature tests, strict Clippy, Linux-target check, and CPU/SIMD parity all pass; the Apple host correctly fails closed because it has no Vulkan shader-f64 runtime. A physical Linux Vulkan adapter with `SHADER_F64` is still required to close the runtime GPU golden-parity receipt; no performance or economic claim is made. |

| D-099 | **Computation is a budgeted decision, not a ritual.** Before a computation expected to exceed five seconds, or an already-green check is repeated, state the decision it could change and the new semantic risk it resolves. Run it only when expected marginal decision value exceeds total cost (runtime, user latency, tool/context overhead, and redundancy). | PROVISIONAL_DECISION | Mandatory semantic-boundary gates still run: changed correctness semantics, determinism/parity/identity boundaries, safety/authority/data-integrity behavior, and explicit handoff gates. Prefer the smallest discriminating check; no suite rerun after documentation/format-only changes; full handoff suite at most once; extra post-green verification is limited to 60 seconds before reporting evidence and remaining uncertainty. See `COMPUTATION_BUDGET_POLICY.md`. |

| D-100 | **Python oracle boundary is locked.** `src/v8/` is a frozen, hash-bound historical parity oracle; `v8-core/` owns the request and verification path. Python may run only for explicitly named legacy research tooling, monograph generation, and boundary audits. A changed oracle tree requires a registry decision, changelog entry, and new hash; CI rejects unregistered edits and Python pytest/oracle invocation in the Rust gate. | LOCKED_INVARIANT candidate | `docs/legacy/PYTHON_ORACLE_LOCK.json`, `docs/legacy/PYTHON_ORACLE_POLICY.md`, and `tools/audit_python_boundary.py`. The oracle is retained because deleting it would remove the independent S0–S7 reference; this decision does not claim that every legacy Python tool has already been ported. |

| D-101 | **Target Oracle O0–O1 is a Rust-only contract and measurement-frame substrate.** `v8-core/src/oracle/` owns exactly the three-role taxonomy, typed refusal vocabulary, versioned after-cost UtilityContract validation, PIT InformationSet adapter, in-memory identity-bearing artifacts, and an Expert-independent finite grammar over explicitly registered primitives/templates/grids. Grammar Candidate identities are in a distinct domain and never enter `CandidateRegistry`. | PROVISIONAL_DECISION | `TARGET_ORACLE_SPEC` §§1–7, §17–19. The implementation deliberately omits support/authority classification, coverage, evidence-bundle serialization, reports, Scout, regret attribution, Target-policy optimization, L3 execution, and economic verdicts. O-OR-001 remains open: no production catalog or default grid was invented. Contract tests cover PIT refusal, deterministic G4/G5 identities, universe mutation, direction identity, and lifecycle non-mutation. |

| D-102 | **Target Oracle O2–O3 implements support/authority classification, representational coverage reconciliation, and v8.eval.v1 receipts in Rust.** `v8-core/src/oracle/{support.rs,authority.rs,coverage.rs}` classify candidate/action support into orthogonal `CounterfactualAuthority` and `OracleOutcome` values without fabricating point estimates. `reconcile_coverage` measures representational coverage against same-event shipped `ExpertEval` proposals, binds `population_hash`, and persists `OpportunityUniverseVersion`, `OracleEvaluationRecord`s, and `CoverageReceipt` finding records directly into the `v8.eval.v1` evidence bundle with the explicit label `NO_ECONOMIC_CLAIM`. A development CLI subcommand `oracle-coverage` is exposed. | PROVISIONAL_DECISION | `TARGET_ORACLE_SPEC` §5.8, §8.1–§8.5, §9.1–§9.4, §16.1–§16.3, §17.5, §18.1–§18.2; Issue #155. Regret attribution (O4), Target-policy optimization (O5), L3 execution upgrade (O6), and promotion claims (O7) remain deliberately absent. Contract tests cover all 10 acceptance criteria (PIT rejection, L1/L3 refusal, fill ambiguity, horizon, UNKNOWN propagation, G4/G5 determinism, grid mutation, coverage floor, population hash binding, and regret attribution absence). |

| D-103 | **Contention Dispatch Rule R-ALLOC-001 formalization.** Exposure slot contention and dispatch tie-break ordering is unified into authoritative rule `R-ALLOC-001`: strictly ascending `sha1(Canon(expert_id))`. Contract specifications (`CANDIDATE_LIFECYCLE_SPEC` §6, `RUNTIME_SCHEDULER_SPEC` §5) and decision register are reconciled with the Rust runtime (`v8-core/src/runloop.rs:dispatch_order`). An executable state-space contention fixture and mutation test gate enforce bi-directional trace conformance in CI. | PROVISIONAL_DECISION | Issue #159; `R-ALLOC-001`. Eliminates contract-runtime drift where older prose mentioned lexicographical ordering while runtime used hash sorting. Mutation tests verify that lexicographical ordering fails the conformance gate. |

| D-104 | **Decouple epistemic observation state (`ThesisStatus::Unknown`) from operational action policy (`PositionPolicy::Hold`).** `v8-core/src/experts/predicate.rs` replaces boolean fail-open return with a three-valued epistemic status enum `ThesisStatus { Valid, Invalid { reason }, Unknown(reason) }`. Unobservable or missing features yield `ThesisStatus::Unknown`, decoupling belief uncertainty from empirical thesis validation. Operational policy mapper `evaluate_position_policy(ThesisStatus::Unknown) -> PositionPolicy::Hold` preserves the fail-open baseline without falsely declaring unobserved predicates `Valid`. | PROVISIONAL_DECISION | Issue #160; `PREDICATE_IR_SPEC` §3. Property test gates verify that withholding an observation produces `ThesisStatus::Unknown` and never `ThesisStatus::Valid`. |

| D-105 | **Semantic Intervention Manifest and regret bucket partitioning on counterfactual replay.** `v8-core/src/simulator.rs` introduces typed `InterventionManifest { why_not_traded, counterfactual_intervention, what_was_held_fixed }` bound to counterfactual outcome artifacts. `v8-core/src/regret.rs` partitions regret attribution by `InterventionClass` (`CAPACITY_REJECTED`, `THESIS_INVALIDATED`, `TRADABILITY_MASK_VETO`, `UNSUPPORTED_COUNTERFACTUAL`), blocking unweighted pooling across heterogeneous causal interventions. Unsupported or infeasible counterfactuals emit `UNSUPPORTED_COUNTERFACTUAL` (refusal) rather than fabricating trajectory data. | PROVISIONAL_DECISION | Issue #161; `OUTCOME_CUBE_SPEC` §4, `RECOVERABLE_REGRET_PROTOCOL` §3. Ensures counterfactual simulation preserves structural causal semantics. |

| D-106 | **Decouple observable price behavior from causal mechanism hypotheses.** `docs/EXPERTS_REGISTRY.yaml` and `v8-core/src/experts/base.rs` decouple `behavior_family_id` (observable price patterns) from `mechanism_family_id` (causal hypotheses). Price-only experts carry default `evidence_status: HYPOTHESIS_ONLY`. Upgrading to causal mechanism claims requires an explicit `EvidenceManifest` (`claim`, `identification_strategy`, `required_observables`, `assumptions`, `falsification_tests`). Metamorphic tests verify that mutating mechanism labels preserves bit-identical economic evaluation results. | PROVISIONAL_DECISION | Issue #162; `EXPERT_PROTOCOL` §1, `HYPOTHESIS_LAB_PROTOCOL` §2. Prevents observational price patterns from being reified as proven causal mechanisms without required order-flow/liquidation observables. |

| D-107 | **Purge superseded single-unit multiplicity clause and enforce machine-checkable Authority Ledger.** `docs/contracts/EXPERT_PROTOCOL.md` §1 purges the superseded "all variants count as one multiplicity unit" text and binds within-family multiplicity correction to White's (2000) Reality Check block-bootstrap procedure over `variants_evaluated` (D-044, D-046) in `v8-core/src/statistics/reality_check.rs`. `v8-core/src/authority.rs` provides an executable `AuthorityLedger` validating that every governance domain resolves to exactly one active decision (`resolve_active_rule("multiplicity") -> D-044`). Conflicting active rules fail closed with `CONFLICTING_ACTIVE_AUTHORITY`. | PROVISIONAL_DECISION | Issue #163; `V8_CONSTITUTION` rule 2, `WORK_ITEM_POLICY.md` §Precedence. Monotonicity tests verify that adding null variants never loosens critical thresholds. |

| D-108 | **Decouple independent candidate evaluation from downstream portfolio allocation.** Candidate economic evaluation runs independently of portfolio admission in `v8-core/src/runloop.rs`. All detected candidates receive independent counterfactual replay outcomes. Portfolio allocation is a distinct downstream layer producing structured records tagged with `allocator_policy_id: baseline_riskgate_v1`, `allocator_version: v1`, `allocation_verdict`, `allocation_reason`, and `portfolio_conditional = true`. Contention-losing candidates retain full independent outcomes. | PROVISIONAL_DECISION | Issue #158; `CANDIDATE_LIFECYCLE_SPEC` §1.1, `OUTCOME_CUBE_SPEC` §2. Metamorphic tests verify that candidate economic merit is invariant to expert naming and portfolio contention. |

| D-109 | **R-space diagnostics and USDT-space economics are distinct, non-fungible authorities.** `PriceRiskUnit` (price distance), `RiskBudgetUSDT` (nominal capital at risk), `EffectiveInitialRiskUSDT` (post-lot-step risk), `PnLUSDT` (monetary cashflow), and `NormalizedR` (realized return divided by effective initial risk) are formally decoupled. R is an analytical diagnostic coordinate system; physical account balances and terminal equity are authoritative economics (`VENUE_AND_CAPITAL_SIMULATION_SPEC` §1). | LOCKED_INVARIANT candidate | Eliminates the category error of summing unconstrained dimensionless R units to claim monetary profit. |

| D-110 | **State ontology partitions into four orthogonal, immutable structures.** The monolithic `MarketState` is split into `MarketState` (exogenous prices, funding, volatility, signals), `VenueState` (exchange filters, tick/step size, min notional, leverage brackets), `AccountState` (wallet balance, available margin, equity, fee tier), and `PortfolioState` (positions, open orders, initial/maintenance margin, unrealized PnL). | LOCKED_INVARIANT | `VENUE_AND_CAPITAL_SIMULATION_SPEC` §2. Prevents participant and account state from leaking into market data features. |

| D-111 | **Binance USDⓈ-M is an Environment/VenueContract, not a distinct Oracle role.** Exchange-specific mechanics (PRICE_FILTER, LOT_SIZE, MIN_NOTIONAL, leverage brackets, liquidation margin schedules, tiered maker/taker fees) are encapsulated in versioned `VenueContract` configurations within the existing Oracle taxonomy (`VENUE_AND_CAPITAL_SIMULATION_SPEC` §3). | PROVISIONAL_DECISION | Preserves the three-role Oracle taxonomy (`ParityOracle`, `HindsightOracle`, `TargetOracle`) while enabling venue realism. |

| D-112 | **Hindsight Oracle formally separates Local Candidate Ceiling from Capital-Constrained Portfolio Ceiling.** Unconstrained candidate-bound hindsight (`UNCONSTRAINED_LOCAL_HINDSIGHT_CEILING`, e.g. +490.82R) is strictly a research diagnostic ceiling. Monetary claims require the path-dependent `CAPITAL_CONSTRAINED_PORTFOLIO_HINDSIGHT_CEILING` ($V^*(S_t)$ in USDT) respecting finite initial equity, margin concurrency, and physical order legality. | LOCKED_INVARIANT | `VENUE_AND_CAPITAL_SIMULATION_SPEC` §6. Closes the population and capital divergence between individual trade opportunities and portfolio realities. |

| D-113 | **Economic Capture Ratio strictly requires identical population, venue, capital, cost, and authority contract hashes.** If numerator and denominator differ in cohort population, initial equity, or execution fidelity, the metric is marked `CAPTURE_RATIO_NOT_IDENTIFIABLE`. Comparing unconstrained R with constrained portfolio PnL is forbidden. | LOCKED_INVARIANT | `VENUE_AND_CAPITAL_SIMULATION_SPEC` §9. Prevents invalid ratio claims across mismatched evaluation frames. |

| D-114 | **Multidimensional ExecutionAuthorityProfile replaces the 1D L1/L2/L3 ladder.** Execution realism is parameterized across orthogonal axes: `market_path` (Bar/1m/Trades/L2), `venue_rules` (Generic/Versioned), `fill_authority` (Canonical/Aggressive/Passive/Live), `impact_authority` (None/Exogenous/Reactive), and `account_authority` (Unconstrained/CapitalConstrained/Live). | PROVISIONAL_DECISION | `VENUE_AND_CAPITAL_SIMULATION_SPEC` §8; arXiv LOB simulation literature (2024-2026). |

| D-115 | **Intrabar-unidentified counterfactuals emit partial-identification intervals, not point-valued Oracle truth.** When high and low barriers are both breached on a single bar without sub-bar path data, the outcome is recorded as an `OutcomeBound [Lower, Upper]` (Brownian Bridge / STOP_FIRST bounds) rather than a single fabricated point estimate. | LOCKED_INVARIANT | `VENUE_AND_CAPITAL_SIMULATION_SPEC` §9; Manski partial identification doctrine. |

| D-116 | **Economic certification requires an independent cashflow ledger validator.** Production economic claims require differential reconciliation of every order, fill, commission, funding payment, and terminal wallet balance against an independent reference simulator. | LOCKED_INVARIANT candidate | `VENUE_AND_CAPITAL_SIMULATION_SPEC` §1; ensures monetary accounting has parity verification. |

| D-117 | **Adopt V8 Work-Item, Pull-Request & Merge Governance v1.2 and initiate 10–20 issue measured pilot.** Deploy 5 Issue Forms with universal context-completeness contract (`R#` traceability, reused contracts, invariants, canonical failure semantics, dependency map, OPEN_PIN triggers), PR contract with requirement-traceability matrix, real CODEOWNERS routing (`@ddawnlll`), canonical label catalog (`triage`), exact CI check binding (`check`), and authoritative workflow precedence hierarchy (`WORK_ITEM_POLICY.md`). Pilot tracked in `docs/governance/PILOT_TRACKING_RECORD.md`. | PROVISIONAL_DECISION | `WORK_ITEM_POLICY.md` v1.2; closes deployment gaps and replaces all placeholder tokens with authoritative repository bindings. |

| D-118 | **Quantized-computation applicability boundary and audit.** V8 remains a deterministic IEEE-754 `f64` compute system with canonical bit identities, fixed reduction order, and scalar/SIMD/backend parity checks. Low-bit / integer / fixed-point quantization is absent by default until an explicit challenger proposal preregisters representation, scale/zero-point, rounding, clipping, accumulator width, calibration population, and frozen-OOS error metrics. `docs/audits/QUANTIZED_COMPUTATION_AUDIT.md` (and Turkish edition) ground this boundary in recent literature (arXiv:1712.05877, arXiv:2202.07513, arXiv:2408.05148, arXiv:2608.12259). | PROVISIONAL_DECISION | Issue #199; `COMPUTE_CORE_SPEC` §2, `PARITY_AND_IDENTITY_SPEC` §1. Retains pure deterministic f64 compute baseline without speed or economic claim. |

| D-119 | **Strongly-typed error taxonomy & candidate lifecycle finite state machine.** Replace stringly-typed error propagation across runtime evaluation with `V8CoreError` enum in `v8-core/src/error.rs` (`Scheduler`, `InvalidCandidateTransition`, `DataStreaming`, `Checkpoint`, `PathSanitization`, `Serialization`, `Io`, `QuantInvariant`). Enforce `CandidateState` lifecycle transitions via `legal_typed` in `v8-core/src/candidate.rs` conforming to `CANDIDATE_LIFECYCLE_SPEC` §2. | PROVISIONAL_DECISION | Issue #208; `COMPUTE_CORE_SPEC` §4, `CANDIDATE_LIFECYCLE_SPEC` §2. Eliminates untyped error strings and guarantees compile-time state transition verification. |

| D-120 | **Multi-digest cryptographic architecture, path sanitization & telemetry facades.** Upgrade hashing surface in `v8-core/src/hash.rs` to support `BLAKE3` (high throughput) and `SHA-256` (collision-resistant audit) while preserving bit-exact legacy `SHA-1` identity digests. Enforce depth-bounded path traversal defense `sanitize_path` in `v8-core/src/path_security.rs`. Integrate structured `tracing` and `metrics` facades in `v8-core/src/telemetry.rs`. | PROVISIONAL_DECISION | Issue #209; `PARITY_AND_IDENTITY_SPEC` §3, `FEED_INGESTION_SPEC` §5. Secures input paths and expands cryptographic verification without invalidating historical hashes. |

| D-121 | **Memory-mapped zero-copy tape streaming & binary IPC serialization.** Introduce `Dataset::from_mmap_path` via `memmap2` in `v8-core/src/data.rs` to provide bounded $O(1)$ memory overhead during high-volume JSONL tape ingestion. Standardize on `bincode` for zero-copy binary state transfer and persistence. | PROVISIONAL_DECISION | Issue #210; `DATASET_SPEC` §5, `PERSISTENCE_REPLAY_SPEC` §1. Prevents high-memory spikes on multi-gigabyte historical replay runs. |

| D-122 | **Simulation state checkpointing, resume engine & multi-architecture release pipeline.** Introduce atomic checkpointing (`SimulationCheckpoint` with tmp-write and atomic rename) in `v8-core/src/checkpoint.rs` to support safe interruption and `--resume` semantics. Deploy automated multi-target GitHub Actions CI/CD release workflow (`.github/workflows/release.yml`) for `x86_64-unknown-linux-gnu` and `aarch64-apple-darwin`. | PROVISIONAL_DECISION | Issue #211; `OPERATIONS_SPEC` §2, `PERSISTENCE_REPLAY_SPEC` §4. Enables long-running simulation fault tolerance and automated artifact delivery. |

| D-123 | **Canonical Cancer Taxonomy & Extreme-Move Campaign Architecture.** Ratify the 6 Canonical Root Cancers (Lot Quantization Paralysis, Tail-Clipping Exit, Expert Funnel Collapse, Regime Blindness, Mechanical Floor Deficiency, Data Blindness) and adopt the 05–06 February 2026 BTC crash/pump episode as the canonical ground-truth benchmark. Ratify Quantization-Aware Risk Budgeting, Tail Capture Efficiency (TCE), Candidate-to-Campaign Aggregation, and Boyd Dynamic Liquidity Floor ($CashFloor_t$). | LOCKED_INVARIANT | `docs/audits/CANONICAL_CANCERS_AND_MEGA_MOVE_AUDIT.md`, `VENUE_AND_CAPITAL_SIMULATION_SPEC` §10; arXiv:1603.06183, arXiv:2602.11708. Closes the gap between unconstrained candidate generation and physical capital execution. |

| D-124 | **ACCP v2.0 YAML-Native Agentic Coding Communication Protocol.** Adopt strict YAML native source profile (`.accp.yaml`) across all 27 official report types with staged support levels (`known`, `template_available`, `schema_lite`, `schema_strict`, `gate_blocking`). Prohibit runtime consumption of raw prose/markdown; enforce compiled JSON artifacts (`.compiled.json`, `.gate-verdict.json`, `.route-signal.json`). | LOCKED_INVARIANT | `docs/contracts/ACCP_V2_SPEC.md`, `WORK_ITEM_POLICY.md` §1–4. Guarantees deterministic, schema-validated, and gate-verifiable agentic communication. |

| D-125 | **Historical Market Archetype Registry & Multi-Episode Allegorical Audit Suite (ALLEGORY-001).** Expand the evaluation and audit engine from point-in-time single-date checks into 12 canonical market archetypes (A01–A12) across 4 super-classes (Directional Opportunity, Forced-Flow Stress, Low-Opportunity Adversarial, Portfolio Derivatives). Enforce zero-hindsight episode bounds with mandatory negative control (anti-allegory) calibration pairs, ex-ante vs ex-post unconstrained / capital-constrained regret decomposition, and deterministic `allegory_scorecard.json` artifact generation labeled `NO_ECONOMIC_CLAIM`. | PROVISIONAL_DECISION | `WORK_ITEM_POLICY.md` v1.2, `TARGET_ORACLE_SPEC` §9; arXiv:2607.19497, arXiv:2608.03616, arXiv:2208.01445. Prevents single-date narrative overfitting and establishes scientific archetype falsification. |

| D-126 | **Cost-Aware No-Trade Region, Persistent Multi-Bar Campaign Clustering & Quantization-Safe Risk Invariants (KZ-018 / KZ-008 / KZ-009 / KZ-007 / VERIFY-002).** Ratify P0 Survival and Opportunity-Capture Architecture: (1) KZ-018 Cost-Aware No-Trade Region to suppress churn when expected gross move <= transaction friction or inside failed campaign cooldown; (2) KZ-008 Persistent Multi-Bar Campaign Clustering spanning compatible direction and invalidation basins across 7 independent mechanism families (eliminating correlated evidence inflation); (3) KZ-009 Quantization-Safe Execution where minimum lot is admitted ONLY if `minimum_executable_risk <= allowed_campaign_risk`, failing closed otherwise (`MIN_EXECUTABLE_RISK_EXCEEDS_BUDGET`); (4) KZ-007 Dynamic Trailing Exits eliminating hardcoded 2.0R targets; (5) VERIFY-002 real-tape campaign-level regret decomposition. | LOCKED_INVARIANT | `docs/issues/ISSUE_KZ018_CHOP001_COST_AWARE_NO_TRADE_REGION.md`, `VENUE_AND_CAPITAL_SIMULATION_SPEC` §10; arXiv:2606.00060, arXiv:2407.13547, arXiv:1308.5658, arXiv:1705.00109, arXiv:2210.00807, arXiv:2207.07578. Eliminates catastrophic fee drag and avoidable quantization rejects while preserving large-move recall. |

Status vocabulary is intentional: `LOCKED_INVARIANT` constrains validity;
`PROVISIONAL_DECISION` is reversible; `REJECTED_OPTION` means excluded from the
named baseline, not disproven forever. None means profitability.
