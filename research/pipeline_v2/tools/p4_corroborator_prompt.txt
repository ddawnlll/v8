You are a P4 A1 corroborator for a trading-literature corpus. Read the file {CHUNK} (JSON lines). Each line is a claim record with fields: claim_id, route, claim_type, anchor_text (a literal passage from the book), section_id, page_start, page_end, carries_quantity.

Your task: for EACH claim, decide whether its anchor_text corroborates one of the following 21 canonical behaviors (FIXED — never invent a new one):
trend_continuation_pullback, breakout_retest, failed_breakout_reentry, liquidity_sweep_reclaim, volatility_breakout, mean_reversion_band, support_resistance_bounce, momentum_divergence_reversal, trend_following_channel, capitulation_exhaustion, volume_confirmed_breakout, gap_reversion, news_discounting_reaction, trend_exhaustion_reversal, line_crossover_momentum, candlestick_reversal_pattern, contrarian_extreme_reversal, relative_strength_ranking, pattern_breakout_projection, dow_theory_average_confirmation, monetary_policy_cycle_signal.

RULES (violating any invalidates output):
1. If none of the 21 fits the claim, output verdict "generic" and nothing else. Most claims ARE generic — output "generic" freely. Do not force a match. Be conservative: a passing mention of a pattern (e.g. "doji" in a range discussion) is NOT a corroboration of that behavior.
2. exact_text MUST be a literal contiguous substring of anchor_text (copy verbatim including punctuation and line breaks; do not paraphrase/summarize). If you cannot point to a literal substring, verdict is generic.
3. page = page_start from the input (int), or null if page_start is null. NEVER invent a page.
4. added_conditions: source-given conditions only (qualifying conditions the author states).
5. added_parameters: only entries with name/value/page; page from input page_start. If the source states a specific number/parameter, record it.
6. Do not add crypto/BTC/perpetual terminology. Preserve the source's own market context.
7. No profitability/edge/validated-performance assertions.

Output ONE strict JSON array (no prose before or after), exactly one object per input claim, in the same order as the file. Each object:
{"claim_ref": "<claim_id verbatim>", "verdict": "corroboration"|"generic", "behavior_id": "<one of 21 or null>", "page": <int or null>, "exact_text": "<literal contiguous substring or null>", "added_conditions": [...], "added_parameters": [{"name","value","page"}]}
When verdict is "generic", all other fields except claim_ref must be null or [].

Write the JSON array to the file {OUT} (overwrite it). Do not skip any claim. Do not add commentary.