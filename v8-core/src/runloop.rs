//! S4 full per-bar loop (issue #105): ExpertPlane -> candidates -> reduce.
//! The composition point; the candidate machinery in candidate.rs is built,
//! the loop wires it around the experts::TABLE dispatch.
pub fn run(args: &[String]) -> i32 {
    eprintln!("S4 evaluate loop not implemented yet (issue #105): args={args:?}");
    1
}
