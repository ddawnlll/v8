//! S6 reconciliation (issue #122): CandidateSnapshot join, PIT lineage
//! assertion, ledger reconciliation (Replay(C, a_actual, M) == observed).
pub fn run(args: &[String]) -> i32 {
    eprintln!("S6 reconcile not implemented yet (issue #122): args={args:?}");
    1
}
